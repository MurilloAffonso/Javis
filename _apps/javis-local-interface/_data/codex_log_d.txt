node.exe : Reading prompt from stdin...
No C:\Users\noteacer\AppData\Roaming\npm\codex.ps1:22 caractere:14
+ ...    $input | & "node$exe"  "$basedir/node_modules/@openai/codex/bin/co ...
+                 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: (Reading prompt from stdin...:String) [], RemoteException
    + FullyQualifiedErrorId : NativeCommandError
 
OpenAI Codex v0.139.0
--------
workdir: C:\Users\noteacer\Desktop\javis
model: gpt-5.5
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: xhigh
reasoning summaries: none
session id: 019ed124-b738-77f2-8fbe-a07d224801a1
--------
user
Voc?? ?? um engenheiro frontend s??nior trabalhando no projeto Javis (interface AIOS).

IMPORTANTE ??? FERRAMENTAS: N??O use nenhuma ferramenta MCP (lean-ctx, ctx_*, etc). Use apenas leitura/escrita de 
arquivo nativa. Trabalhe a partir da raiz do repo `C:\Users\noteacer\Desktop\javis`.

CONTEXTO: Leia os arquivos `_apps/javis-local-interface/frontend/index.html`, 
`_apps/javis-local-interface/frontend/style.css` e `_apps/javis-local-interface/frontend/app.js` para entender a 
estrutura atual. O projeto tem 7 views (chat, agents, workflows, room, projects, train, integrations).

TAREFA: Adicione um PAINEL DE ANALYTICS nas views `view-projects` e `view-train`. N??O edite index.html nem app.js de 
forma que quebre funcionalidade existente.

## 1. Analytics no view-projects (Projetos Orquestrados)

No arquivo `frontend/app.js`, modifique a fun????o `renderProjects()` para incluir, ACIMA da grade de projetos 
(`projects-grid`), um bloco de m??tricas globais do orquestrador. Adicione no DOM (via innerHTML) um 
`div.analytics-bar` com:

```html
<div class="analytics-bar" id="analytics-bar">
  <div class="ab-card">
    <div class="ab-val" id="ab-total-tasks">???</div>
    <div class="ab-lbl">tarefas ativas</div>
  </div>
  <div class="ab-card">
    <div class="ab-val" id="ab-agents-online">5</div>
    <div class="ab-lbl">agentes online</div>
  </div>
  <div class="ab-card">
    <div class="ab-val" id="ab-completions">???</div>
    <div class="ab-lbl">completadas hoje</div>
  </div>
  <div class="ab-card">
    <div class="ab-val" id="ab-efficiency">???</div>
    <div class="ab-lbl">efici??ncia</div>
  </div>
  <div class="ab-chart-wrap">
    <canvas id="ab-chart" width="200" height="60"></canvas>
    <div class="ab-chart-lbl">Atividade 7 dias</div>
  </div>
</div>
```

Logo ap??s renderizar o bar, chame `_drawAnalyticsChart()` que desenha um mini sparkline no canvas `ab-chart` 
simulando atividade dos ??ltimos 7 dias (array de n??meros aleat??rios entre 3 e 12, cor `#7c3aed`, linha fina).

Preencha os valores assim:
- `ab-total-tasks`: soma de tarefas de PROJECTS_DATA (campo `tasks` se existir, ou `3`)
- `ab-agents-online`: 5 (hardcoded, mas com classe `ab-val online`)
- `ab-completions`: n??mero aleat??rio fixo (ex: 14)
- `ab-efficiency`: "87%"

## 2. Analytics no view-train (SDR Academy)

No arquivo `frontend/app.js`, AP??S a fun????o `renderTrainingQueue()`, adicione uma fun????o `renderTrainStats()` que 
atualiza um elemento `#train-stats` (que voc?? vai criar no HTML).

No `index.html`, DENTRO de `<div class="train-layout">`, logo ANTES de `<div class="train-queue" id="train-queue">`, 
adicione:

```html
<div class="train-stats" id="train-stats">
  <div class="ts-card"><span class="ts-val" id="ts-total">0</span><span class="ts-lbl">v??deos treinados</span></div>
  <div class="ts-card"><span class="ts-val" id="ts-chars">0k</span><span class="ts-lbl">chars absorvidos</span></div>
  <div class="ts-card"><span class="ts-val" id="ts-agents">0</span><span class="ts-lbl">agentes treinados</span></div>
</div>
```

A fun????o `renderTrainStats()` deve:
- Contar v??deos com `status === 'done'` em `_trainingQueue`
- Somar `chars` de todos os itens done
- Contar agentes ??nicos treinados (set de `item.agent` where done)
- Atualizar os spans com os valores

Chame `renderTrainStats()` dentro de `renderTrainingQueue()` ao final.

## 3. CSS para os novos componentes

No `frontend/style.css`, ao final do arquivo, adicione:

```css
/* ????????? ANALYTICS BAR ????????? */
.analytics-bar {
  display: flex;
  gap: 12px;
  align-items: center;
  background: rgba(124,58,237,0.06);
  border: 1px solid rgba(124,58,237,0.2);
  border-radius: var(--radius);
  padding: 14px 20px;
  margin-bottom: 20px;
}
.ab-card {
  display: flex;
  flex-direction: column;
  align-items: center;
  min-width: 80px;
}
.ab-val {
  font-family: var(--font-mono);
  font-size: 22px;
  font-weight: 700;
  color: var(--violet);
  line-height: 1;
}
.ab-val.online { color: var(--emerald); }
.ab-lbl {
  font-size: 10px;
  color: var(--text-dim);
  text-transform: uppercase;
  letter-spacing: 0.05em;
  margin-top: 4px;
  white-space: nowrap;
}
.ab-chart-wrap {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 4px;
}
.ab-chart-lbl {
  font-size: 10px;
  color: var(--text-dim);
  text-transform: uppercase;
  letter-spacing: 0.04em;
}

/* ????????? TRAIN STATS ????????? */
.train-stats {
  display: flex;
  gap: 16px;
  margin-bottom: 16px;
}
.ts-card {
  background: var(--surface2);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 10px 18px;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4px;
}
.ts-val {
  font-family: var(--font-mono);
  font-size: 20px;
  font-weight: 700;
  color: var(--cyan);
}
.ts-lbl {
  font-size: 10px;
  color: var(--text-dim);
  text-transform: uppercase;
  letter-spacing: 0.05em;
}
```

## REGRAS R??GIDAS
- N??O remova classes, IDs ou funcionalidade existente
- N??O adicione depend??ncias externas
- N??O use ferramentas MCP
- Edi????es incrementais e cir??rgicas

Ao terminar, liste em 3 linhas o que foi alterado em cada arquivo.


codex
Vou ler os três arquivos pedidos com comandos nativos do PowerShell e localizar os pontos exatos de inserção antes de 
editar.
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw -LiteralPath 
'_apps/javis-local-interface/frontend/index.html'" in C:\Users\noteacer\Desktop\javis
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw -LiteralPath 
'_apps/javis-local-interface/frontend/style.css'" in C:\Users\noteacer\Desktop\javis
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw -LiteralPath 
'_apps/javis-local-interface/frontend/app.js'" in C:\Users\noteacer\Desktop\javis
 succeeded in 466ms:
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>JAVIS</title>
  <link rel="stylesheet" href="style.css" />
</head>
<body>

<div class="app-shell">

  <!-- â•â•â• SIDEBAR â•â•â• -->
  <nav class="sidebar">
    <div class="sb-logo">
      <span class="sb-logo-bolt">âš¡</span>
      <div class="sb-logo-text">
        <span class="sb-logo-name">JAVIS</span>
        <span class="sb-logo-ver">AIOS</span>
      </div>
    </div>

    <div class="sb-nav">
      <button class="sb-item active" data-view="chat">
        <span class="sb-item-icon">ðŸ’¬</span>
        <span class="sb-item-label">Chat</span>
      </button>
      <button class="sb-item" data-view="agents">
        <span class="sb-item-icon">ðŸ¤–</span>
        <span class="sb-item-label">Agentes</span>
      </button>
      <button class="sb-item" data-view="workflows">
        <span class="sb-item-icon">âš¡</span>
        <span class="sb-item-label">Workflows</span>
      </button>
      <button class="sb-item" data-view="room">
        <span class="sb-item-icon">ðŸ¢</span>
        <span class="sb-item-label">Sala</span>
      </button>
      <button class="sb-item" data-view="projects">
        <span class="sb-item-icon">ðŸ—‚ï¸</span>
        <span class="sb-item-label">Projetos</span>
      </button>
      <button class="sb-item" data-view="train">
        <span class="sb-item-icon">ðŸŽ“</span>
        <span class="sb-item-label">Treino</span>
      </button>
      <button class="sb-item" data-view="integrations">
        <span class="sb-item-icon">ðŸ”—</span>
        <span class="sb-item-label">IntegraÃ§Ãµes</span>
      </button>
    </div>

    <div class="sb-sep"></div>

    <div class="sb-agents-section">
      <div class="sb-section-label">AGENTES</div>
      <div class="sb-agents-list" id="sb-agents-list">
        <div class="sb-agent"><span class="sb-agent-dot online"></span><span class="sb-agent-name">Orion</span><span 
class="sb-agent-role">@master</span></div>
        <div class="sb-agent"><span class="sb-agent-dot online"></span><span class="sb-agent-name">Khan</span><span 
class="sb-agent-role">@bdr</span></div>
        <div class="sb-agent"><span class="sb-agent-dot online"></span><span class="sb-agent-name">Phantom</span><span 
class="sb-agent-role">@closer</span></div>
        <div class="sb-agent"><span class="sb-agent-dot idle"></span><span class="sb-agent-name">Vera</span><span 
class="sb-agent-role">@cmo</span></div>
        <div class="sb-agent"><span class="sb-agent-dot idle"></span><span class="sb-agent-name">Dara</span><span 
class="sb-agent-role">@data-eng</span></div>
        <div class="sb-agent"><span class="sb-agent-dot offline"></span><span class="sb-agent-name">Intel</span><span 
class="sb-agent-role">@analyst</span></div>
        <div class="sb-agent"><span class="sb-agent-dot online"></span><span class="sb-agent-name">Titan</span><span 
class="sb-agent-role">@cro</span></div>
        <div class="sb-agent"><span class="sb-agent-dot online"></span><span class="sb-agent-name">Blade</span><span 
class="sb-agent-role">@bdr-en</span></div>
      </div>
    </div>

    <div class="sb-footer">
      <div class="sb-svc-row">
        <div class="svc" id="svc-ollama"><div class="svc-dot"></div><span>OLLAMA</span></div>
        <div class="svc offline" id="svc-webui"><div class="svc-dot"></div><span>WEBUI</span></div>
      </div>
      <button class="sb-item sb-vp-btn" onclick="window.location.href='/vempassear'">
        <span class="sb-item-icon">ðŸŒŠ</span>
        <span class="sb-item-label">Vem Passear</span>
      </button>
    </div>
  </nav>

  <!-- â•â•â• MAIN AREA â•â•â• -->
  <div class="main-area">

    <header class="topbar">
      <div class="topbar-left">
        <span class="topbar-view-title" id="topbar-view-title">Orquestrador</span>
      </div>
      <div class="topbar-center">
        <span class="tc-val" id="stat-count">0</span><span class="tc-lbl">msgs</span>
        <span class="tc-sep">Â·</span>
        <span class="tc-val">8</span><span class="tc-lbl">agentes</span>
        <span class="tc-sep">Â·</span>
        <span class="tc-val" id="stat-brain-lbl">MAIN</span>
        <span class="tc-sep">Â·</span>
        <span class="tc-val" id="stat-session">00:00</span>
      </div>
      <div class="topbar-right">
        <div class="agents-online-badge">
          <span class="aob-dot"></span>
          <span>5 online</span>
        </div>
      </div>
    </header>

    <div class="views-container">

      <!-- VIEW: CHAT -->
      <div class="view active" id="view-chat">
        <div class="layout">

          <aside class="panel-left">
            <div class="brain-card active" data-brain="main" id="bc-main">
              <div class="bc-bar"></div>
              <div class="bc-top"><span class="bc-icon">ðŸ¤“</span><div><div class="bc-label">CÃ‰REBRO 
PRINCIPAL</div><div class="bc-name" id="bc-main-name">llama3.2:3b</div></div></div>
              <div class="bc-desc">RaciocÃ­nio direto e rÃ¡pido. Modelo principal de IA.</div>
            </div>
            <div class="brain-card" data-brain="conclave" id="bc-conclave">
              <div class="bc-bar"></div>
              <div class="bc-top"><span class="bc-icon">ðŸ§</span><div><div class="bc-label">MEGA-BRAIN 
CONCLAVE</div><div class="bc-name">CÃ©rebro Coletivo</div></div></div>
              <div class="bc-desc">3 especialistas debatem cada decisÃ£o.</div>
            </div>
            <div class="brain-card" data-brain="memory" id="bc-memory">
              <div class="bc-bar"></div>
              <div class="bc-top"><span class="bc-icon">ðŸ’¾</span><div><div class="bc-label">MEMÃ“RIA 
ETERNA</div><div class="bc-name">Obsidian Memory</div></div></div>
              <div class="bc-desc">MemÃ³ria permanente. Cresce com cada conversa.</div>
            </div>

            <div class="left-sep"></div>

            <div class="left-section">
              <div class="left-title">MODELO DE IA</div>
              <select class="model-sel" id="model-sel">
                <optgroup label="â˜ï¸ Cloud">
                  <option value="claude-sonnet-4-6">Claude Sonnet 4.6</option>
                  <option value="claude-opus-4-8">Claude Opus 4.8</option>
                  <option value="gpt-4o-mini">GPT-4o mini</option>
                  <option value="gpt-4o">GPT-4o</option>
                </optgroup>
                <optgroup label="ðŸ  Local (Ollama)">
                  <option value="llama3.2:3b">llama3.2:3b</option>
                  <option value="phi3:mini">phi3:mini</option>
                  <option value="llama3.1">llama3.1</option>
                  <option value="gemma3">gemma3</option>
                  <option value="mistral">mistral</option>
                </optgroup>
              </select>
            </div>

            <div class="left-section">
              <div class="left-title">STATUS DO SISTEMA</div>
              <div class="sys-rows">
                <div class="sys-row"><span>RESPOSTAS</span><span id="sys-count">0</span></div>
                <div class="sys-row"><span>ÃšLTIMO MS</span><span id="sys-ms">â€”</span></div>
                <div class="sys-row"><span>CÃ‰REBRO</span><span id="sys-brain">MAIN</span></div>
                <div class="sys-row"><span>SESSÃƒO</span><span id="sys-uptime">00:00</span></div>
                <div class="sys-row"><span>INTENT</span><span id="sys-intent">â€”</span></div>
              </div>
            </div>

            <div class="left-section">
              <div class="left-title">RESERVATÃ“RIO Â· ATIVIDADE</div>
              <canvas id="reservoir-canvas" width="216" height="86"></canvas>
              <div class="res-stats"><span id="res-avg">avg â€”ms</span><span id="res-peak">pico â€”ms</span></div>
            </div>

            <div class="left-section">
              <div class="left-title">ÃšLTIMO PLANO</div>
              <div class="plan-box" id="plan-box">â€”</div>
            </div>

            <div class="left-section">
              <div class="left-title">ESPECIALISTAS</div>
              <div class="agents-grid" id="agents-grid">
                <div class="ag-card" data-id="architect"><div class="ag-icon">ðŸ—ï¸</div><div 
class="ag-name">Architect</div></div>
                <div class="ag-card" data-id="developer"><div class="ag-icon">ðŸ’»</div><div 
class="ag-name">Developer</div></div>
                <div class="ag-card" data-id="ux_designer"><div class="ag-icon">ðŸŽ¨</div><div class="ag-name">UX 
Design</div></div>
                <div class="ag-card" data-id="qa"><div class="ag-icon">ðŸ”</div><div class="ag-name">QA</div></div>
                <div class="ag-card" data-id="pm"><div class="ag-icon">ðŸ“‹</div><div class="ag-name">PM</div></div>
                <div class="ag-card" data-id="po"><div class="ag-icon">ðŸŽ¯</div><div class="ag-name">PO</div></div>
                <div class="ag-card" data-id="scrum"><div class="ag-icon">âš™ï¸</div><div 
class="ag-name">Scrum</div></div>
                <div class="ag-card" data-id="analyst"><div class="ag-icon">ðŸ“Š</div><div 
class="ag-name">Analyst</div></div>
                <div class="ag-card" data-id="devops"><div class="ag-icon">ðŸš€</div><div 
class="ag-name">DevOps</div></div>
                <div class="ag-card" data-id="data_engineer"><div class="ag-icon">ðŸ—„ï¸</div><div 
class="ag-name">Data Eng</div></div>
                <div class="ag-card meta-card" data-id="aios_master"><div class="ag-icon">ðŸ§¬</div><div 
class="ag-name">AIOS Master</div></div>
                <div class="ag-card meta-card" data-id="jarvis_soul"><div class="ag-icon">âœ¨</div><div 
class="ag-name">Jamba Soul</div></div>
              </div>
            </div>
          </aside>

          <main class="stage">
            <div class="stage-name">JAVIS</div>
            <div class="stage-sub">orquestrador Â· voz Â· IA multiagente</div>
            <div class="orb-stage" id="orb-stage"><canvas id="orb-canvas"></canvas></div>
            <div class="orb-state-lbl" id="neural-state">ONLINE</div>
            <div class="orb-pulse-lbl" id="neural-pulse-lbl">aguardando comando</div>
            <div class="voice-status" id="voice-status">
              <div class="voice-status-dot"></div>
              <span>OUVINDO</span>
              <span class="voice-interim" id="voice-interim"></span>
            </div>
            <div class="orb-controls">
              <button type="button" class="mic-btn" id="mic-btn" title="Comando de voz (pt-BR)">ðŸŽ¤</button>
              <button type="button" class="voice-auto-btn" id="wake-btn" title='Wake word'>ðŸŽ™ï¸</button>
              <button type="button" class="voice-auto-btn" id="voice-auto-btn" title="Voz sempre ativa">ðŸ‘‚</button>
              <label class="tts-chk" title="Falar resposta (TTS)"><input type="checkbox" id="use-tts" checked 
/><span>ðŸ”Š</span></label>
              <label class="conclave-chk" title="Ativar Conclave"><input type="checkbox" id="use-conclave" 
/><span>âš”ï¸</span></label>
            </div>
            <div class="quick-row">
              <button type="button" class="qbtn" data-cmd="status do sistema">âš¡ status</button>
              <button type="button" class="qbtn" data-cmd="que tempo faz hoje">ðŸŒ¡ï¸ clima</button>
              <button type="button" class="qbtn" data-cmd="abre o youtube">â–¶ youtube</button>
              <button type="button" class="qbtn" data-cmd="toca uma mÃºsica">ðŸŽµ mÃºsica</button>
              <button type="button" class="qbtn" data-cmd="abre o vscode">ðŸ’» vscode</button>
              <button type="button" class="qbtn" data-cmd="abre o terminal">$ terminal</button>
              <button type="button" class="qbtn" data-cmd="abre o projeto">ðŸ“‚ projeto</button>
              <button type="button" class="qbtn" data-cmd="analisa o site ">ðŸŒ site</button>
              <button type="button" class="qbtn qbtn-debate" id="btn-debate">ðŸ¤ debate</button>
            </div>
          </main>

          <aside class="panel-chat">
            <div class="chat-hdr">
              <span>ðŸ’¬ CONVERSA</span>
              <span class="chat-hdr-sub" id="chat-hdr-sub">Javis online</span>
            </div>
            <div class="chat-log" id="chat-log">
              <div class="welcome-block">
                <div class="wb-title">JAVIS online.</div>
                <div class="wb-sub">Fala ou digita. Eu te respondo.</div>
                <div class="wb-chips">
                  <span class="wb-chip">ðŸ¤“ Principal</span>
                  <span class="wb-chip">ðŸ§  Conclave</span>
                  <span class="wb-chip">ðŸ’¾ MemÃ³ria</span>
                  <span class="wb-chip">ðŸŽ¤ Voz pt-BR</span>
                </div>
              </div>
            </div>
            <div class="conclave-wrap hidden" id="conclave-wrap">
              <button class="panel-hdr" id="conclave-toggle"><span>ðŸ§ CONCLAVE â€” debate interno</span><span 
id="conclave-chevron">â–¼</span></button>
              <div class="conclave-agents" id="conclave-body">
                <div class="ca"><div class="ca-hdr red"><span>ðŸ”´</span> CRÃTICO</div><div class="ca-txt" 
id="ct-critico">aguardando debate...</div></div>
                <div class="ca"><div class="ca-hdr orange"><span>âš”ï¸</span> ADVOGADO</div><div class="ca-txt" 
id="ct-advogado">aguardando debate...</div></div>
                <div class="ca"><div class="ca-hdr green"><span>âœ…</span> SINTETIZADOR</div><div class="ca-txt" 
id="ct-sintetizador">aguardando debate...</div></div>
              </div>
            </div>
            <div class="squad-wrap hidden" id="squad-wrap">
              <button class="panel-hdr" id="squad-toggle"><span>ðŸ¤ SQUAD â€” colaboraÃ§Ã£o autÃ´noma</span><span 
id="squad-chevron">â–¼</span></button>
              <div class="squad-rounds" id="squad-rounds"></div>
            </div>
            <form class="chat-input" id="form">
              <label class="upload-btn" title="Enviar arquivo para anÃ¡lise">ðŸ“Ž<input type="file" id="file-input" 
accept=".pdf,.xlsx,.xls,.docx,.pptx,.csv,.txt,.html,.png,.jpg,.jpeg" 
style="position:absolute;opacity:0;width:1px;height:1px;pointer-events:none;" /></label>
              <input id="input" type="text" placeholder="Fala com a Javis..." autocomplete="off" autofocus />
              <button type="submit" class="send-btn">â–¶</button>
            </form>
          </aside>

        </div>
      </div>

      <!-- VIEW: AGENTES -->
      <div class="view" id="view-agents">
        <div class="agents-view-layout">
          <div class="agents-view-main">
            <div class="view-header">
              <h2 class="view-title">Escolha um Agente</h2>
              <div class="view-filters">
                <button class="filter-btn active" data-filter="all">Todos</button>
                <button class="filter-btn" data-filter="vendas">Vendas</button>
                <button class="filter-btn" data-filter="criativo">Criativo</button>
                <button class="filter-btn" data-filter="dados">Dados</button>
                <button class="filter-btn" data-filter="ops">Ops</button>
              </div>
            </div>
            <div class="gallery-grid" id="gallery-grid"></div>
          </div>
          <aside class="activity-panel">
            <div class="activity-title">Atividade</div>
            <div class="activity-tabs">
              <button class="act-tab active">Recente</button>
              <button class="act-tab">MissÃµes</button>
            </div>
            <div class="activity-feed" id="activity-feed"></div>
          </aside>
        </div>
      </div>

      <!-- VIEW: WORKFLOWS -->
      <div class="view" id="view-workflows">
        <div class="workflows-layout">
          <aside class="missions-panel">
            <div class="missions-title">MISSÃ•ES ATIVAS</div>
            <div class="missions-list" id="missions-list"></div>
            <button class="new-mission-btn">+ Nova MissÃ£o</button>
          </aside>
          <main class="workflow-canvas-area">
            <div class="wf-canvas-header">
              <span class="wf-canvas-title" id="wf-canvas-title">Campanha de LanÃ§amento Q1</span>
              <div class="wf-canvas-status">Em execuÃ§Ã£o</div>
            </div>
            <div class="workflow-canvas" id="workflow-canvas">
              <svg class="wf-connections" id="wf-connections"></svg>
            </div>
          </main>
          <aside class="task-detail-panel">
            <div class="tdp-title">DETALHES DA TASK</div>
            <div class="tdp-content" id="tdp-content"><div class="tdp-empty">Selecione uma task no canvas</div></div>
          </aside>
        </div>
      </div>

      <!-- VIEW: TREINAMENTO -->
      <div class="view" id="view-train">
        <div class="train-layout">
          <h2 class="view-title">SDR Academy</h2>
          <p class="train-subtitle">Adicione vÃdeos para treinar os agentes. O SDR vai absorver o conteÃºdo e aplicar 
nas vendas.</p>
          <div class="train-agent-select">
            <label>Agente a treinar:</label>
            <select id="train-agent-sel">
              <option value="khan">Khan (@bdr)</option>
              <option value="phantom">Phantom (@closer)</option>
              <option value="vera">Vera (@cmo)</option>
              <option value="orion">Orion (@master)</option>
            </select>
          </div>
          <div class="train-url-input">
            <input type="text" id="train-url" placeholder="Cole o link do YouTube aqui..." />
            <button id="train-add-btn">Treinar</button>
          </div>
          <div class="train-queue" id="train-queue">
            <div class="train-empty">Nenhum vÃ­deo em treinamento ainda.</div>
          </div>
        </div>
      </div>

      <!-- VIEW: INTEGRAÃ‡Ã•ES -->
      <div class="view" id="view-integrations">
        <div class="integrations-layout">
          <div class="view-header">
            <div>
              <h2 class="view-title">IntegraÃ§Ãµes</h2>
              <p class="view-subtitle">Conecte os agentes Ã s suas ferramentas</p>
            </div>
          </div>
          <div class="integrations-grid" id="integrations-grid"></div>
        </div>
      </div>

      <!-- VIEW: SALA DE AGENTES -->
      <div class="view" id="view-room">
        <div class="room-layout">
          <div class="room-header">
            <div>
              <h2 class="view-title">Sala dos Agentes</h2>
              <p class="view-subtitle">O squad trabalhando em tempo real. Cada boneco Ã© um agente.</p>
            </div>
            <div class="room-stats" id="room-stats">
              <span class="rstat"><b id="rstat-working">0</b> trabalhando</span>
              <span class="rstat"><b id="rstat-idle">0</b> ociosos</span>
              <span class="rstat"><b id="rstat-tasks">0</b> tarefas</span>
            </div>
          </div>
          <div class="room-floor" id="room-floor">
            <div class="room-grid-bg"></div>
            <!-- estaÃ§Ãµes e bonecos injetados via JS -->
          </div>
          <div class="room-log" id="room-log"></div>
        </div>
      </div>

      <!-- VIEW: PROJETOS -->
      <div class="view" id="view-projects">
        <div class="projects-layout">
          <div class="view-header">
            <div>
              <h2 class="view-title">Projetos Orquestrados</h2>
              <p class="view-subtitle">Javis Ã© o cÃ©rebro mestre. Cada projeto tem seu prÃ³prio squad.</p>
            </div>
          </div>
          <div class="projects-grid" id="projects-grid"></div>
        </div>
      </div>

    </div>
  </div>
</div>

<div id="toast-container" class="toast-container"></div>
<script src="app.js"></script>
</body>
</html>


 succeeded in 466ms:
/* â”€â”€â”€ RESET & VARIABLES 
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&family=JetBrains+Mono:wght@400
;600;700&display=swap');

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

:root {
  --bg:          #0a0a1a;
  --surface:     #0d0d1a;
  --surface2:    #131325;
  --border:      #18182e;
  --border2:     #22223a;
  --violet:      #7c3aed;
  --violet-dim:  rgba(124,58,237,0.15);
  --violet-glow: rgba(124,58,237,0.45);
  --cyan:        #00e5ff;
  --cyan-dim:    rgba(0,229,255,0.15);
  --emerald:     #10b981;
  --matrix:      #00ff9c;
  --progress:    #3b82f6;
  --warn:        #f59e0b;
  --danger:      #ef4444;
  --purple:      var(--violet);
  --green:       var(--emerald);
  --orange:      var(--warn);
  --red:         var(--danger);
  --amber:       var(--warn);
  --text:        #e2e8f0;
  --text-mid:    #8892a4;
  --text-dim:    #5a6478;
  --radius:      8px;
  --font:        'Inter', 'Segoe UI', system-ui, sans-serif;
  --font-mono:   'JetBrains Mono', 'SF Mono', Consolas, monospace;
  --sb-width:    220px;
  --glass:       rgba(19,19,37,0.62);
  --glass-deep:  rgba(13,13,26,0.78);
  --shadow:      0 8px 24px rgba(0,0,0,0.3);
  --shadow-hover: 0 12px 28px rgba(0,0,0,0.34);
}

html, body { height: 100%; overflow: hidden; }
body {
  background: var(--bg);
  color: var(--text);
  font-family: var(--font);
  font-size: 14px;
  display: flex;
  flex-direction: column;
}

/* â”€â”€â”€ TOPBAR â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.topbar {
  height: 46px;
  background: var(--surface);
  border-bottom: 1px solid var(--border);
  display: flex;
  align-items: center;
  padding: 0 16px;
  gap: 16px;
  flex-shrink: 0;
  z-index: 10;
}
.topbar-left { display: flex; align-items: center; gap: 6px; flex-shrink: 0; }
.logo-bolt { font-size: 18px; filter: drop-shadow(0 0 8px var(--cyan)); }
.logo-name { font-size: 16px; font-weight: 800; letter-spacing: 0.14em; color: var(--cyan); text-shadow: 0 0 20px 
rgba(0,229,255,0.4); }
.logo-ver  { font-size: 10px; color: var(--text-dim); background: var(--border2); padding: 1px 6px; border-radius: 
4px; font-weight: 600; }

.topbar-center {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  font-size: 11px;
}
.tc-val  { color: var(--cyan); font-weight: 700; font-family: var(--font-mono); }
.tc-lbl  { color: var(--text-dim); font-weight: 500; }
.tc-sep  { color: var(--border2); font-size: 10px; }

.topbar-right { display: flex; align-items: center; gap: 10px; flex-shrink: 0; }
.svc { display: flex; align-items: center; gap: 5px; font-size: 10px; font-weight: 700; color: var(--text-dim); 
letter-spacing: 0.06em; }
.svc-dot { width: 7px; height: 7px; border-radius: 50%; background: var(--green); box-shadow: 0 0 6px var(--green); 
transition: all 0.3s; }
.svc.offline .svc-dot  { background: var(--text-dim); box-shadow: none; }
.svc.checking .svc-dot { background: var(--amber); box-shadow: 0 0 6px var(--amber); animation: pulse 1s infinite; }
@keyframes pulse { 0%,100%{opacity:1} 50%{opacity:.4} }

/* â”€â”€â”€ LAYOUT â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.layout { flex: 1; display: flex; overflow: hidden; }

/* â”€â”€â”€ LEFT PANEL â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”
€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.panel-left {
  width: 244px;
  flex-shrink: 0;
  background: var(--surface);
  border-right: 1px solid var(--border);
  overflow-y: auto;
  display: flex;
  flex-direction: column;
  padding: 12px;
  gap: 8px;
}
.panel-left::-webkit-scrollbar { width: 3px; }
.panel-left::-webkit-scrollbar-thumb { background: var(--border2); border-radius: 2px; }

/* Brain Cards */
.brain-card {
  background: var(--surface);
  border: 1px solid var(--border2);
  border-radius: 12px;
  padding: 12px 14px 12px 20px;
  cursor: pointer;
  transition: all 0.25s;
  position: relative;
  overflow: hidden;
  user-select: none;
}
.brain-card:hover { border-color: rgba(0,229,255,0.25); }
.brain-card.active {
  border-color: var(--cyan);
  background: rgba(0,229,255,0.04);
  box-shadow: 0 0 24px rgba(0,229,255,0.07), inset 0 0 24px rgba(0,229,255,0.02);
}
.brain-card[data-brain="conclave"].active { border-color: var(--purple); background: rgba(168,85,247,0.04); 
box-shadow: 0 0 24px rgba(168,85,247,0.07); }
.brain-card[data-brain="memory"].active   { border-color: var(--amber);  background: rgba(245,158,11,0.04);  
box-shadow: 0 0 24px rgba(245,158,11,0.07); }

.bc-bar {
  position: absolute;
  left: 0; top: 14px; bottom: 14px;
  width: 3px;
  background: var(--border2);
  border-radius: 0 2px 2px 0;
  transition: all 0.25s;
}
.brain-card.active .bc-bar                          { background: var(--cyan);   box-shadow: 0 0 8px var(--cyan); }
.brain-card[data-brain="conclave"].active .bc-bar   { background: var(--purple); box-shadow: 0 0 8px var(--purple); }
.brain-card[data-brain="memory"].active .bc-bar     { background: var(--amber);  box-shadow: 0 0 8px var(--amber); }

.bc-top { display: flex; align-items: center; gap: 10px; margin-bottom: 5px; }
.bc-icon { font-size: 22px; flex-shrink: 0; line-height: 1; }
.bc-label {
  font-size: 8.5px; font-weight: 800; letter-spacing: 0.1em;
  color: var(--text-dim); margin-bottom: 2px; transition: color 0.25s;
}
.brain-card.active                        .bc-label { color: var(--cyan); }
.brain-card[data-brain="conclave"].active .bc-label { color: var(--purple); }
.brain-card[data-brain="memory"].active   .bc-label { color: var(--amber); }
.bc-name { font-size: 13px; font-weight: 700; color: var(--text); }
.bc-desc { font-size: 10.5px; color: var(--text-dim); line-height: 1.4; }

.left-sep { height: 1px; background: var(--border); margin: 0 -12px; }

/* Left sections */
.left-section {
  background: var(--surface2);
  border: 1px solid var(--border2);
  border-radius: var(--radius);
  padding: 10px 12px;
}
.left-title {
  font-size: 8.5px; font-weight: 800; letter-spacing: 0.12em;
  color: var(--text-dim); margin-bottom: 8px; text-transform: uppercase;
}
.model-sel {
  width: 100%;
  background: var(--bg);
  border: 1px solid var(--border2);
  color: var(--text);
  border-radius: var(--radius);
  padding: 7px 10px;
  font-size: 12px; font-weight: 600;
  cursor: pointer; outline: none; transition: border-color 0.2s;
}
.model-sel:focus { border-color: var(--cyan); }
.model-sel option { background: var(--surface); }

.sys-rows { display: flex; flex-direction: column; gap: 5px; }
.sys-row { display: flex; justify-content: space-between; align-items: center; font-size: 11px; }
.sys-row span:first-child { color: var(--text-dim); letter-spacing: 0.02em; }
.sys-row span:last-child  { color: var(--cyan); font-weight: 700; font-family: var(--font-mono); font-size: 11px; }

.plan-box {
  font-size: 11px; color: var(--text-dim); line-height: 1.5;
  min-height: 36px; font-family: var(--font-mono);
  white-space: pre-wrap; word-break: break-word;
}

.dlv-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 4px; }
.dlv {
  font-size: 10px; color: var(--text-dim);
  padding: 5px 7px; background: var(--bg);
  border: 1px solid var(--border2); border-radius: 6px;
  cursor: pointer; transition: all 0.15s;
}
.dlv:hover { color: var(--cyan); border-color: rgba(0,229,255,0.3); }

/* â”€â”€â”€ CENTER CHAT â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â
”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
/* â”€â”€â”€ CENTER STAGE â€” Orbe JAMBA â”€â”€â”€ */
.stage {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 10px;
  padding: 20px;
  background:
    radial-gradient(ellipse at 50% 45%, rgba(0,229,255,.06), transparent 60%),
    var(--bg);
  position: relative;
  overflow: hidden;
}
.stage::before {
  content: ""; position: absolute; inset: 0;
  background-image:
    linear-gradient(rgba(0,229,255,.025) 1px, transparent 1px),
    linear-gradient(90deg, rgba(0,229,255,.025) 1px, transparent 1px);
  background-size: 40px 40px;
  pointer-events: none;
}
.stage-name {
  font-size: 30px; font-weight: 900; letter-spacing: .42em;
  color: var(--cyan); text-shadow: 0 0 30px rgba(0,229,255,.5);
  z-index: 1; padding-left: .42em;
}
.stage-sub {
  font-size: 11px; letter-spacing: .18em; color: var(--text-dim);
  text-transform: uppercase; z-index: 1; margin-top: -4px;
}
.orb-stage {
  position: relative; z-index: 1;
  width: min(46vh, 420px); height: min(46vh, 420px);
  display: flex; align-items: center; justify-content: center;
  margin: 6px 0;
}
.orb-stage canvas { width: 100%; height: 100%; display: block; }

.orb-state-lbl {
  z-index: 1; font-family: var(--font-mono); font-size: 13px; font-weight: 700;
  letter-spacing: .28em; color: var(--cyan); text-shadow: 0 0 10px rgba(0,229,255,.5);
}
.orb-pulse-lbl {
  z-index: 1; font-family: var(--font-mono); font-size: 10px;
  letter-spacing: .12em; color: var(--text-dim); margin-top: -4px;
}
.stage.thinking .orb-state-lbl { animation: neuralBlink 1s infinite; }
.stage.conclave .orb-state-lbl { color: var(--purple); text-shadow: 0 0 10px rgba(168,85,247,.5); }
@keyframes neuralBlink { 0%,100% { opacity: 1; } 50% { opacity: .45; } }

.orb-controls {
  z-index: 1; display: flex; align-items: center; gap: 9px; margin-top: 6px;
}

.chat-log {
  flex: 1;
  overflow-y: auto;
  padding: 20px;
  display: flex;
  flex-direction: column;
  gap: 12px;
  scroll-behavior: smooth;
}
.chat-log::-webkit-scrollbar { width: 4px; }
.chat-log::-webkit-scrollbar-thumb { background: var(--border2); border-radius: 4px; }

/* Welcome Block + Brain Orb */
.welcome-block {
  margin: auto;
  text-align: center;
  padding: 24px 20px 30px;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16px;
}

.orb-wrap {
  position: relative;
  width: 164px; height: 164px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}
.orb-core {
  position: absolute; z-index: 3;
  width: 60px; height: 60px;
  border-radius: 50%;
  background: radial-gradient(circle, rgba(0,229,255,0.22) 0%, rgba(0,229,255,0.05) 50%, transparent 70%);
  box-shadow:
    0 0 30px rgba(0,229,255,0.65),
    0 0 70px rgba(0,229,255,0.25),
    0 0 120px rgba(0,229,255,0.1),
    inset 0 0 20px rgba(0,229,255,0.12);
  display: flex; align-items: center; justify-content: center;
  font-size: 24px;
  animation: orb-pulse 3.5s ease-in-out infinite;
}
.orb-ring {
  position: absolute;
  border-radius: 50%;
  border-style: solid;
  border-color: transparent;
}
.orb-ring.r1 {
  width: 92px; height: 92px; border-width: 1.5px;
  border-top-color: rgba(0,229,255,0.65);
  border-right-color: rgba(0,229,255,0.2);
  animation: spin 5s linear infinite;
}
.orb-ring.r2 {
  width: 124px; height: 124px; border-width: 1px;
  border-top-color: rgba(168,85,247,0.45);
  border-left-color: rgba(168,85,247,0.15);
  animation: spin 9s linear infinite reverse;
}
.orb-ring.r3 {
  width: 156px; height: 156px; border-width: 1px;
  border-bottom-color: rgba(0,229,255,0.22);
  border-right-color: rgba(0,229,255,0.08);
  animation: spin 15s linear infinite;
}
@keyframes spin { from{transform:rotate(0deg)} to{transform:rotate(360deg)} }
@keyframes orb-pulse {
  0%,100% { box-shadow: 0 0 30px rgba(0,229,255,0.65), 0 0 70px rgba(0,229,255,0.25); }
  50%      { box-shadow: 0 0 50px rgba(0,229,255,0.9), 0 0 110px rgba(0,229,255,0.38); }
}

.wb-title { font-size: 22px; font-weight: 800; letter-spacing: 0.1em; color: var(--cyan); text-shadow: 0 0 24px 
rgba(0,229,255,0.4); }
.wb-sub   { color: var(--text-dim); font-size: 12px; }
.wb-chips { display: flex; gap: 6px; flex-wrap: wrap; justify-content: center; }
.wb-chip  {
  font-size: 10px; font-weight: 600; color: var(--text-dim);
  background: var(--surface2); border: 1px solid var(--border2);
  padding: 3px 10px; border-radius: 20px; letter-spacing: 0.03em;
}

/* Messages */
.msg {
  display: flex; flex-direction: column; gap: 4px;
  max-width: 84%; animation: fadeIn 0.2s ease;
}
@keyframes fadeIn { from{opacity:0;transform:translateY(6px)} to{opacity:1;transform:none} }
.msg.user      { align-self: flex-end; }
.msg.assistant { align-self: flex-start; }

.msg-bubble {
  padding: 10px 14px; border-radius: var(--radius);
  line-height: 1.6; font-size: 13.5px; word-break: break-word;
}
.msg.user .msg-bubble {
  background: rgba(0,229,255,.08);
  border: 1px solid rgba(0,229,255,.2);
}
.msg.assistant .msg-bubble {
  background: var(--surface2);
  border: 1px solid var(--border2);
}
.msg.blocked .msg-bubble { background: rgba(239,68,68,.07); border: 1px solid rgba(239,68,68,.3); color: var(--red); }
.msg.error   .msg-bubble { background: rgba(239,68,68,.04); border: 1px solid rgba(239,68,68,.18); color: 
var(--text-dim); }

.msg-meta { font-size: 11px; color: var(--text-dim); display: flex; gap: 6px; flex-wrap: wrap; padding: 0 2px; }
.msg.user .msg-meta { justify-content: flex-end; }
.tag {
  background: var(--surface2); border: 1px solid var(--border2);
  padding: 1px 6px; border-radius: 10px;
  font-size: 10px; font-weight: 600; letter-spacing: 0.03em;
}

/* Typing */
.typing-indicator {
  display: flex; align-items: center; gap: 8px;
  color: var(--text-dim); font-size: 12px;
  padding: 8px 12px;
  background: var(--surface2); border: 1px solid var(--border2);
  border-radius: var(--radius); align-self: flex-start;
}
.typing-dots { display: flex; gap: 4px; }
.typing-dots span { width: 5px; height: 5px; background: var(--cyan); border-radius: 50%; animation: bounce 1.2s 
infinite; }
.typing-dots span:nth-child(2) { animation-delay: 0.2s; }
.typing-dots span:nth-child(3) { animation-delay: 0.4s; }
@keyframes bounce { 0%,60%,100%{transform:none} 30%{transform:translateY(-5px)} }

/* Conclave / Squad panels */
.conclave-wrap, .squad-wrap {
  border-top: 1px solid var(--border);
  background: var(--surface);
  flex-shrink: 0;
}
.conclave-wrap.hidden, .squad-wrap.hidden { display: none; }

.panel-hdr {
  width: 100%; display: flex; align-items: center; justify-content: space-between;
  padding: 10px 16px; background: none; border: none;
  color: var(--text-mid); font-size: 11px; font-weight: 700;
  letter-spacing: 0.08em; cursor: pointer; transition: color 0.2s;
}
.panel-hdr:hover { color: var(--cyan); }
#conclave-chevron, #squad-chevron { font-size: 10px; transition: transform 0.2s; }
.conclave-wrap.open #conclave-chevron,
.squad-wrap.open    #squad-chevron { transform: rotate(180deg); }

.conclave-agents {
  display: none; grid-template-columns: 1fr 1fr 1fr; gap: 1px;
  background: var(--border); border-top: 1px solid var(--border); max-height: 180px;
}
.conclave-wrap.open .conclave-agents { display: grid; }
.ca { background: var(--surface); padding: 10px 12px; overflow-y: auto; max-height: 180px; }
.ca::-webkit-scrollbar { width: 3px; }
.ca::-webkit-scrollbar-thumb { background: var(--border2); }
.ca-hdr { display: flex; align-items: center; gap: 5px; font-size: 9.5px; font-weight: 800; letter-spacing: 0.08em; 
margin-bottom: 6px; }
.ca-hdr.red    { color: var(--red); }
.ca-hdr.orange { color: var(--orange); }
.ca-hdr.green  { color: var(--green); }
.ca-txt { font-size: 11px; color: var(--text-mid); line-height: 1.5; }

.squad-rounds { display: none; flex-direction: column; border-top: 1px solid var(--border); max-height: 240px; 
overflow-y: auto; }
.squad-wrap.open .squad-rounds { display: flex; }
.squad-rounds::-webkit-scrollbar { width: 3px; }
.squad-rounds::-webkit-scrollbar-thumb { background: var(--border2); }
.squad-round { padding: 10px 14px; border-bottom: 1px solid var(--border); }
.squad-round-hdr { font-size: 9.5px; font-weight: 800; letter-spacing: 0.1em; color: var(--text-dim); margin-bottom: 
8px; }
.squad-agent-outputs { display: flex; flex-direction: column; gap: 6px; }
.squad-agent { background: var(--surface2); border: 1px solid var(--border2); border-radius: var(--radius); padding: 
7px 10px; }
.squad-agent-name { font-size: 9.5px; font-weight: 700; color: var(--cyan); margin-bottom: 3px; }
.squad-agent-txt  { font-size: 11px; color: var(--text-mid); line-height: 1.5; }

/* â”€â”€â”€ RIGHT: CHAT PANEL 
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.panel-chat {
  width: 380px;
  flex-shrink: 0;
  background: var(--surface);
  border-left: 1px solid var(--border);
  display: flex;
  flex-direction: column;
  min-height: 0;
}
.chat-hdr {
  display: flex; align-items: center; justify-content: space-between;
  padding: 12px 16px; border-bottom: 1px solid var(--border);
  font-size: 11px; font-weight: 800; letter-spacing: .1em; color: var(--text-mid);
  flex-shrink: 0;
}
.chat-hdr-sub { font-size: 9px; font-weight: 600; letter-spacing: .06em; color: var(--green); }

.chat-input {
  border-top: 1px solid var(--border);
  padding: 12px 14px;
  display: flex; align-items: center; gap: 8px;
  background: var(--surface);
  flex-shrink: 0;
}

#input {
  flex: 1;
  background: var(--surface2);
  border: 1px solid var(--border2);
  border-radius: var(--radius);
  color: var(--text);
  font-size: 14px; font-family: var(--font);
  padding: 9px 14px; outline: none;
  transition: border-color 0.2s, box-shadow 0.2s;
}
#input:focus { border-color: var(--cyan); box-shadow: 0 0 0 2px rgba(0,229,255,.08); }
#input::placeholder { color: var(--text-dim); }

.send-btn {
  background: var(--cyan); color: var(--bg);
  border: none; border-radius: var(--radius);
  padding: 9px 16px; font-size: 15px; font-weight: 900;
  cursor: pointer; transition: all 0.2s; flex-shrink: 0;
}
.send-btn:hover { background: #33ecff; box-shadow: 0 0 14px rgba(0,229,255,.4); }
.send-btn:active { transform: scale(0.96); }
.send-btn:disabled { opacity: 0.35; cursor: not-allowed; transform: none; }
.upload-btn {
  display: flex; align-items: center; justify-content: center;
  width: 36px; height: 36px; border-radius: 8px; flex-shrink: 0;
  cursor: pointer; font-size: 16px; opacity: 0.7;
  transition: opacity 0.2s, background 0.2s;
  background: rgba(255,255,255,.06);
}
.upload-btn:hover { opacity: 1; background: rgba(20,224,196,.15); }

/* Quick buttons */
.quick-row { display: flex; gap: 5px; flex-wrap: wrap; }
.qbtn {
  background: var(--surface2); border: 1px solid var(--border2);
  color: var(--text-dim); border-radius: 20px;
  padding: 4px 11px; font-size: 10.5px; font-weight: 600;
  cursor: pointer; transition: all 0.15s; letter-spacing: 0.02em;
}
.qbtn:hover { border-color: var(--cyan); color: var(--cyan); background: rgba(0,229,255,.05); }
.qbtn-debate { border-color: rgba(168,85,247,.4); color: var(--purple); }
.qbtn-debate:hover { border-color: var(--purple); background: rgba(168,85,247,.08); color: var(--purple); }

/* Toggles */
.conclave-chk, .tts-chk {
  display: flex; align-items: center; justify-content: center;
  width: 36px; height: 36px;
  background: var(--surface2); border: 1px solid var(--border2);
  border-radius: var(--radius); cursor: pointer; font-size: 16px;
  transition: all 0.2s; flex-shrink: 0;
}
.conclave-chk:has(input:checked) { border-color: var(--orange); background: rgba(249,115,22,.1); box-shadow: 0 0 8px 
rgba(249,115,22,.2); }
.tts-chk:has(input:checked)      { border-color: var(--green);  background: rgba(34,197,94,.1);  box-shadow: 0 0 8px 
rgba(34,197,94,.18); }
.conclave-chk input, .tts-chk input { display: none; }

/* â”€â”€â”€ VOICE CONTROLS 
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ 
*/
.mic-btn {
  width: 36px; height: 36px;
  background: var(--surface2); border: 1px solid var(--border2);
  border-radius: var(--radius); color: var(--text-mid);
  font-size: 16px; cursor: pointer; transition: all 0.2s; flex-shrink: 0;
  display: flex; align-items: center; justify-content: center;
}
.mic-btn:hover { border-color: var(--cyan); color: var(--cyan); }
/* BotÃµes de voz maiores no palco central */
.orb-controls .mic-btn,
.orb-controls .voice-auto-btn { width: 48px; height: 48px; font-size: 20px; border-radius: 12px; }
.orb-controls .tts-chk,
.orb-controls .conclave-chk { width: 44px; height: 44px; font-size: 18px; border-radius: 12px; }
.mic-btn.recording {
  border-color: var(--red); background: rgba(239,68,68,.12); color: var(--red);
  animation: mic-pulse 1s infinite;
}
@keyframes mic-pulse {
  0%   { box-shadow: 0 0 0 0 rgba(239,68,68,.55); }
  70%  { box-shadow: 0 0 0 9px rgba(239,68,68,0); }
  100% { box-shadow: 0 0 0 0 rgba(239,68,68,0); }
}
.voice-status {
  display: none; align-items: center; justify-content: center; gap: 7px;
  font-size: 10.5px; color: var(--red); font-weight: 700; letter-spacing: 0.07em;
  z-index: 1; min-height: 16px;
}
.voice-status.active { display: flex; }
.voice-status-dot { width: 7px; height: 7px; border-radius: 50%; background: var(--red); animation: pulse 1s infinite; 
flex-shrink: 0; }
.voice-interim { color: var(--text-dim); font-style: italic; font-weight: 400; letter-spacing: 0; overflow: hidden; 
text-overflow: ellipsis; white-space: nowrap; max-width: 340px; }

/* â”€â”€â”€ RIGHT PANEL â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â
”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.panel-right {
  width: 240px;
  flex-shrink: 0;
  background: var(--surface);
  border-left: 1px solid var(--border);
  overflow-y: auto;
  display: flex;
  flex-direction: column;
  padding: 10px;
  gap: 8px;
}
.panel-right::-webkit-scrollbar { width: 3px; }
.panel-right::-webkit-scrollbar-thumb { background: var(--border2); border-radius: 2px; }

.right-section {
  background: var(--surface2);
  border: 1px solid var(--border2);
  border-radius: var(--radius);
  padding: 10px 10px 8px;
}
.right-title {
  font-size: 8.5px; font-weight: 800; letter-spacing: 0.12em;
  color: var(--text-dim); margin-bottom: 8px; text-transform: uppercase;
}

/* Reservoir Chart */
#reservoir-canvas {
  width: 100%; border-radius: 4px;
  display: block; background: var(--bg);
}
.res-stats {
  display: flex; justify-content: space-between;
  font-size: 10px; color: var(--text-dim);
  font-family: var(--font-mono); margin-top: 5px;
}

/* Agent cards */
.agents-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 5px; }
.ag-card {
  background: var(--bg); border: 1px solid var(--border2);
  border-radius: var(--radius); padding: 7px 6px;
  cursor: pointer; transition: all 0.2s; text-align: center;
}
.ag-card:hover { border-color: var(--cyan); background: rgba(0,229,255,.04); }
.ag-card.active { border-color: var(--cyan); box-shadow: 0 0 8px rgba(0,229,255,.2); }

.ag-card.meta-card { border-color: rgba(0,229,255,.15); background: rgba(0,229,255,.02); }
.ag-card.meta-card:hover, .ag-card.meta-card.active { border-color: var(--cyan); box-shadow: 0 0 10px 
rgba(0,229,255,.25); }
.ag-card.red-card:hover,    .ag-card.red-card.active    { border-color: var(--red);    box-shadow: 0 0 8px 
rgba(239,68,68,.2); }
.ag-card.orange-card:hover, .ag-card.orange-card.active { border-color: var(--orange); box-shadow: 0 0 8px 
rgba(249,115,22,.2); }
.ag-card.green-card:hover,  .ag-card.green-card.active  { border-color: var(--green);  box-shadow: 0 0 8px 
rgba(34,197,94,.2); }

.ag-icon { font-size: 16px; margin-bottom: 3px; }
.ag-name { font-size: 10px; font-weight: 700; color: var(--text-mid); }
.ag-role { font-size: 9px; color: var(--text-dim); margin-top: 1px; line-height: 1.3; }

/* Message bubble code */
.msg-bubble code { background: rgba(0,229,255,.07); border: 1px solid rgba(0,229,255,.14); padding: 1px 5px; 
border-radius: 4px; font-family: var(--font-mono); font-size: 12px; color: var(--cyan); }
.msg-bubble pre { background: var(--surface); border: 1px solid var(--border2); border-radius: var(--radius); padding: 
10px 12px; margin-top: 8px; overflow-x: auto; font-family: var(--font-mono); font-size: 12px; line-height: 1.6; }
.msg-bubble pre code { background: none; border: none; padding: 0; color: var(--text); }
.msg-bubble strong { color: var(--cyan); font-weight: 700; }
.msg-bubble ul, .msg-bubble ol { padding-left: 18px; margin-top: 6px; }
.msg-bubble li { margin-bottom: 3px; }

/* â”€â”€â”€ STREAMING â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.stream-cursor {
  display: inline-block;
  color: var(--cyan);
  animation: blink-cur 0.65s step-end infinite;
  text-shadow: 0 0 8px var(--cyan);
  margin-left: 1px;
}
@keyframes blink-cur { 0%,100%{opacity:1} 50%{opacity:0} }
.msg.streaming .msg-bubble { min-height: 1.5em; }

/* â”€â”€â”€ SCI-FI ENHANCEMENTS 
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */

/* Scanlines overlay */
body::after {
  content: "";
  position: fixed;
  inset: 0;
  background: repeating-linear-gradient(
    0deg,
    transparent,
    transparent 2px,
    rgba(0,0,0,0.06) 2px,
    rgba(0,0,0,0.06) 4px
  );
  pointer-events: none;
  z-index: 9999;
}

/* HUD grid background no chat */
.chat-log {
  background-image:
    linear-gradient(rgba(0,229,255,0.018) 1px, transparent 1px),
    linear-gradient(90deg, rgba(0,229,255,0.018) 1px, transparent 1px);
  background-size: 44px 44px;
}

/* Radial glow atrÃ¡s do orb */
.welcome-block::before {
  content: "";
  position: absolute;
  width: 340px; height: 340px;
  background: radial-gradient(circle, rgba(0,229,255,0.06) 0%, transparent 65%);
  pointer-events: none;
  z-index: 0;
}
.welcome-block { position: relative; }
.orb-wrap, .wb-title, .wb-sub, .wb-chips { position: relative; z-index: 1; }

/* Topbar border glow */
.topbar {
  border-bottom: 1px solid rgba(0,229,255,0.15);
  box-shadow: 0 1px 24px rgba(0,229,255,0.04);
}

/* Brain card active â€” radial highlight */
.brain-card.active::before {
  content: "";
  position: absolute;
  inset: 0;
  border-radius: 12px;
  background: radial-gradient(ellipse at 20% 20%, rgba(0,229,255,0.06) 0%, transparent 65%);
  pointer-events: none;
}
.brain-card[data-brain="conclave"].active::before {
  background: radial-gradient(ellipse at 20% 20%, rgba(168,85,247,0.06) 0%, transparent 65%);
}
.brain-card[data-brain="memory"].active::before {
  background: radial-gradient(ellipse at 20% 20%, rgba(245,158,11,0.06) 0%, transparent 65%);
}

/* Voz sempre ativa â€” botÃ£o */
.voice-auto-btn {
  width: 36px; height: 36px;
  background: var(--surface2); border: 1px solid var(--border2);
  border-radius: var(--radius); color: var(--text-dim);
  font-size: 16px; cursor: pointer; transition: all 0.2s; flex-shrink: 0;
  display: flex; align-items: center; justify-content: center;
}
.voice-auto-btn:hover { border-color: var(--cyan); color: var(--cyan); }
.voice-auto-btn.active {
  border-color: var(--cyan);
  background: rgba(0,229,255,0.1);
  color: var(--cyan);
  animation: voice-auto-glow 2s ease-in-out infinite;
}
@keyframes voice-auto-glow {
  0%,100% { box-shadow: 0 0 6px rgba(0,229,255,0.3); }
  50%      { box-shadow: 0 0 18px rgba(0,229,255,0.65); }
}

/* Mic recording â€” efeito de onda */
.mic-btn.recording::after {
  content: "";
  position: absolute;
  inset: -4px;
  border-radius: calc(var(--radius) + 4px);
  border: 1px solid rgba(239,68,68,0.4);
  animation: mic-ring 1.5s ease-out infinite;
}
.mic-btn { position: relative; }
@keyframes mic-ring {
  0%   { transform: scale(1);    opacity: 0.8; }
  100% { transform: scale(1.5);  opacity: 0; }
}

/* Status panel â€” neon value glow */
.sys-row span:last-child {
  text-shadow: 0 0 8px rgba(0,229,255,0.35);
}

/* Input focus â€” stronger neon */
#input:focus {
  box-shadow: 0 0 0 2px rgba(0,229,255,0.08), 0 0 14px rgba(0,229,255,0.12) !important;
}

/* Message bubbles â€” slight glow on user */
.msg.user .msg-bubble {
  box-shadow: 0 0 12px rgba(0,229,255,0.06);
}

/* Agent card active â€” pulse ring */
.ag-card.active::after {
  content: "";
  position: absolute;
  inset: -2px;
  border-radius: calc(var(--radius) + 2px);
  border: 1px solid rgba(0,229,255,0.5);
  animation: ag-ring 2s ease-in-out infinite;
  pointer-events: none;
}
.ag-card { position: relative; }
@keyframes ag-ring {
  0%,100% { opacity: 0.5; }
  50%     { opacity: 1; }
}

/* Reservoir canvas border */
#reservoir-canvas {
  border: 1px solid var(--border2);
}

/* optgroup no select */
.model-sel optgroup { font-size: 10px; color: var(--text-dim); }

/* â”€â”€â”€ RESPONSIVE â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”
€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
@media (max-width: 900px) {
  .panel-right { display: none; }
}
@media (max-width: 640px) {
  .panel-left { display: none; }
  .topbar-center { display: none; }
}

/* â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
   JAVIS AIOS â€” Shell, Sidebar & Multi-View
â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â• */

/* Richer background */
body {
  background:
    radial-gradient(ellipse at 15% 85%, rgba(124,58,237,0.07) 0%, transparent 50%),
    radial-gradient(ellipse at 85% 15%, rgba(0,229,255,0.04) 0%, transparent 50%),
    linear-gradient(rgba(124,58,237,0.035) 1px, transparent 1px),
    linear-gradient(90deg, rgba(124,58,237,0.035) 1px, transparent 1px),
    #0a0a1a;
  background-size: auto, auto, 52px 52px, 52px 52px, auto;
  background-attachment: fixed;
  display: block !important;
  height: 100%;
}
html { height: 100%; }

body::before {
  content: "";
  position: fixed;
  inset: 0;
  background:
    radial-gradient(ellipse at 50% 30%, rgba(124,58,237,0.10), transparent 55%),
    radial-gradient(ellipse at 70% 80%, rgba(0,229,255,0.05), transparent 48%);
  mask-image: radial-gradient(ellipse at 50% 50%, #000 35%, transparent 82%);
  -webkit-mask-image: radial-gradient(ellipse at 50% 50%, #000 35%, transparent 82%);
  pointer-events: none;
  z-index: 0;
}

/* App shell */
.app-shell {
  display: flex;
  height: 100vh;
  overflow: hidden;
  position: relative;
  z-index: 1;
}

/* â”€â”€â”€ SIDEBAR â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”
€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.sidebar {
  width: var(--sb-width);
  flex-shrink: 0;
  background: rgba(8, 8, 22, 0.97);
  border-right: 1px solid rgba(124,58,237,0.2);
  display: flex;
  flex-direction: column;
  z-index: 100;
  box-shadow: 2px 0 24px rgba(0,0,0,0.4);
}

.sb-logo {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 16px 14px 14px;
  border-bottom: 1px solid rgba(255,255,255,0.06);
  flex-shrink: 0;
}
.sb-logo-bolt { font-size: 22px; filter: drop-shadow(0 0 8px var(--cyan)); }
.sb-logo-name { font-size: 16px; font-weight: 900; letter-spacing: 0.14em; color: var(--cyan); text-shadow: 0 0 20px 
rgba(0,229,255,0.4); display: block; }
.sb-logo-ver  { font-size: 8px; font-weight: 700; color: var(--violet); letter-spacing: 0.1em; background: 
rgba(124,58,237,0.15); padding: 1px 5px; border-radius: 3px; border: 1px solid rgba(124,58,237,0.3); display: block; 
margin-top: 2px; }

.sb-nav {
  padding: 10px 8px;
  display: flex;
  flex-direction: column;
  gap: 2px;
  flex-shrink: 0;
}
.sb-item {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 9px 12px;
  border-radius: 8px;
  border: 1px solid transparent;
  background: none;
  color: var(--text-mid);
  font-size: 13px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.18s;
  text-align: left;
  width: 100%;
  font-family: var(--font);
}
.sb-item:hover { background: rgba(255,255,255,0.04); color: var(--text); }
.sb-item.active { background: var(--violet-dim); color: var(--violet); border-color: rgba(124,58,237,0.3); }
.sb-item-icon { font-size: 15px; flex-shrink: 0; }
.sb-item-label { font-size: 13px; }

.sb-sep { height: 1px; background: rgba(255,255,255,0.06); margin: 4px 12px; flex-shrink: 0; }

.sb-agents-section { flex: 1; overflow-y: auto; padding: 6px 8px; }
.sb-agents-section::-webkit-scrollbar { width: 3px; }
.sb-agents-section::-webkit-scrollbar-thumb { background: var(--border2); }

.sb-section-label {
  font-size: 9px; font-weight: 800; letter-spacing: 0.14em;
  color: var(--text-dim); padding: 4px 8px 8px; text-transform: uppercase;
}
.sb-agent {
  display: flex; align-items: center; gap: 8px;
  padding: 6px 10px; border-radius: 7px; cursor: pointer; transition: all 0.15s;
}
.sb-agent:hover { background: rgba(255,255,255,0.04); }
.sb-agent-dot { width: 7px; height: 7px; border-radius: 50%; flex-shrink: 0; }
.sb-agent-dot.online  { background: var(--emerald); box-shadow: 0 0 5px var(--emerald); }
.sb-agent-dot.idle    { background: var(--amber); }
.sb-agent-dot.offline { background: var(--text-dim); }
.sb-agent-name { font-size: 12px; font-weight: 600; color: var(--text); flex: 1; }
.sb-agent-role { font-size: 10px; color: var(--text-dim); font-family: var(--font-mono); }

.sb-footer { padding: 8px; border-top: 1px solid rgba(255,255,255,0.06); flex-shrink: 0; }
.sb-svc-row { display: flex; gap: 10px; padding: 4px 8px 8px; }
.sb-vp-btn { color: #14e0c4 !important; }
.sb-vp-btn:hover { border-color: rgba(20,224,196,0.3) !important; }

/* â”€â”€â”€ MAIN AREA â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.main-area { flex: 1; display: flex; flex-direction: column; overflow: hidden; min-width: 0; }

.topbar-view-title {
  font-size: 14px; font-weight: 800; letter-spacing: 0.06em; color: var(--text);
}

/* â”€â”€â”€ VIEWS â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â
”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.views-container { flex: 1; overflow: hidden; position: relative; }
.view { display: none; height: 100%; overflow: hidden; }
.view.active { display: flex; flex-direction: column; }

/* view-chat keeps original 3-panel layout */
#view-chat .layout { flex: 1; display: flex; overflow: hidden; }

/* â”€â”€â”€ AGENTS ONLINE BADGE 
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.agents-online-badge {
  display: flex; align-items: center; gap: 6px;
  font-size: 11px; font-weight: 700; color: var(--emerald);
  background: rgba(16,185,129,0.08); border: 1px solid rgba(16,185,129,0.25);
  padding: 3px 10px; border-radius: 20px;
}
.aob-dot {
  width: 6px; height: 6px; border-radius: 50%;
  background: var(--emerald); box-shadow: 0 0 6px var(--emerald);
  animation: pulse 2s infinite;
}

/* â”€â”€â”€ VIEW: AGENTS â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.agents-view-layout { display: flex; height: 100%; overflow: hidden; }
.agents-view-main { flex: 1; overflow-y: auto; padding: 24px; }
.agents-view-main::-webkit-scrollbar { width: 4px; }
.agents-view-main::-webkit-scrollbar-thumb { background: var(--border2); }

.view-header { display: flex; align-items: flex-start; justify-content: space-between; margin-bottom: 22px; gap: 12px; 
flex-wrap: wrap; }
.view-title { font-size: 20px; font-weight: 800; color: var(--text); letter-spacing: 0.02em; }
.view-subtitle { color: var(--text-dim); font-size: 13px; margin-top: 4px; margin-bottom: 22px; line-height: 1.5; }

.view-filters { display: flex; gap: 6px; flex-wrap: wrap; }
.filter-btn {
  padding: 5px 14px; border-radius: 20px; border: 1px solid var(--border2);
  background: var(--surface2); color: var(--text-dim); font-size: 11px;
  font-weight: 600; cursor: pointer; transition: all 0.15s; font-family: var(--font);
}
.filter-btn:hover { border-color: var(--violet); color: var(--violet); }
.filter-btn.active { background: var(--violet-dim); border-color: var(--violet); color: var(--violet); }

.gallery-grid {
  display: grid; grid-template-columns: repeat(auto-fill, minmax(230px, 1fr)); gap: 14px;
}
.gallery-card {
  background: var(--surface2); border: 1px solid var(--border2);
  border-radius: 12px; padding: 16px; cursor: pointer; transition: all 0.2s;
  position: relative; overflow: hidden;
}
.gallery-card:hover { border-color: rgba(124,58,237,0.4); box-shadow: 0 4px 20px rgba(124,58,237,0.08); transform: 
translateY(-1px); }
.gallery-card::before { content:''; position:absolute; top:0; left:0; right:0; height:2px; background:var(--violet); 
opacity:0; transition:opacity 0.2s; }
.gallery-card:hover::before { opacity:1; }

.gc-header { display: flex; align-items: center; gap: 10px; margin-bottom: 10px; }
.gc-avatar {
  width: 40px; height: 40px; border-radius: 10px; flex-shrink: 0;
  background: linear-gradient(135deg, rgba(124,58,237,0.2), rgba(0,229,255,0.1));
  border: 1px solid rgba(124,58,237,0.3);
  display: flex; align-items: center; justify-content: center; font-size: 20px;
}
.gc-info { flex: 1; min-width: 0; }
.gc-name { font-size: 14px; font-weight: 700; color: var(--text); }
.gc-role { font-size: 10px; color: var(--text-dim); font-family: var(--font-mono); }
.gc-status-dot { width: 8px; height: 8px; border-radius: 50%; flex-shrink: 0; }
.gc-status-dot.online  { background: var(--emerald); box-shadow: 0 0 6px var(--emerald); }
.gc-status-dot.idle    { background: var(--amber); }
.gc-status-dot.offline { background: var(--text-dim); }

.gc-desc { font-size: 12px; color: var(--text-dim); line-height: 1.5; margin-bottom: 10px; }
.gc-skills { display: flex; gap: 4px; flex-wrap: wrap; margin-bottom: 10px; }
.gc-skill { font-size: 10px; padding: 2px 7px; background: var(--surface); border: 1px solid var(--border2); 
border-radius: 10px; color: var(--text-dim); }
.gc-integrations { display: flex; gap: 4px; }
.gc-int-icon { width: 22px; height: 22px; border-radius: 5px; background: var(--surface); border: 1px solid 
var(--border2); display: flex; align-items: center; justify-content: center; font-size: 12px; }

.activity-panel { width: 270px; flex-shrink: 0; background: var(--surface); border-left: 1px solid var(--border); 
display: flex; flex-direction: column; }
.activity-title { font-size: 13px; font-weight: 800; letter-spacing: 0.06em; color: var(--text-mid); padding: 16px 
16px 10px; border-bottom: 1px solid var(--border); }
.activity-tabs { display: flex; border-bottom: 1px solid var(--border); }
.act-tab { flex: 1; padding: 8px; background: none; border: none; border-bottom: 2px solid transparent; color: 
var(--text-dim); font-size: 11px; font-weight: 600; cursor: pointer; transition: all 0.15s; font-family: var(--font); }
.act-tab.active { color: var(--violet); border-bottom-color: var(--violet); }
.activity-feed { flex: 1; overflow-y: auto; padding: 10px; display: flex; flex-direction: column; gap: 7px; }
.activity-feed::-webkit-scrollbar { width: 3px; }
.activity-feed::-webkit-scrollbar-thumb { background: var(--border2); }
.activity-item { background: var(--surface2); border: 1px solid var(--border2); border-radius: 8px; padding: 8px 10px; 
}
.ai-agent { font-size: 10px; font-weight: 700; color: var(--violet); margin-bottom: 3px; }
.ai-text { font-size: 11px; color: var(--text-dim); line-height: 1.4; }
.ai-time { font-size: 10px; color: var(--text-dim); margin-top: 4px; opacity: 0.6; }

/* â”€â”€â”€ VIEW: WORKFLOWS 
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.workflows-layout { display: flex; height: 100%; overflow: hidden; }

.missions-panel { width: 230px; flex-shrink: 0; background: var(--surface); border-right: 1px solid var(--border); 
display: flex; flex-direction: column; }
.missions-title { font-size: 9px; font-weight: 800; letter-spacing: 0.14em; color: var(--text-dim); padding: 16px 14px 
8px; text-transform: uppercase; }
.missions-list { flex: 1; overflow-y: auto; padding: 8px; }
.missions-list::-webkit-scrollbar { width: 3px; }
.missions-list::-webkit-scrollbar-thumb { background: var(--border2); }
.mission-item { padding: 11px 12px; border-radius: 8px; border: 1px solid var(--border2); background: var(--surface2); 
cursor: pointer; transition: all 0.15s; margin-bottom: 6px; }
.mission-item:hover { border-color: rgba(124,58,237,0.3); }
.mission-item.active { border-color: var(--violet); background: var(--violet-dim); }
.mi-name { font-size: 12px; font-weight: 700; color: var(--text); margin-bottom: 6px; }
.mi-progress-row { display: flex; align-items: center; gap: 8px; }
.mi-bar { flex: 1; height: 3px; background: var(--border2); border-radius: 2px; overflow: hidden; }
.mi-bar-fill { height: 100%; background: linear-gradient(90deg, var(--violet), var(--cyan)); border-radius: 2px; }
.mi-pct { font-size: 10px; font-weight: 700; color: var(--violet); font-family: var(--font-mono); }
.new-mission-btn { margin: 8px; padding: 9px; border-radius: 8px; border: 1px dashed rgba(124,58,237,0.4); background: 
none; color: var(--violet); font-size: 12px; font-weight: 600; cursor: pointer; transition: all 0.15s; font-family: 
var(--font); }
.new-mission-btn:hover { background: var(--violet-dim); }

.workflow-canvas-area {
  flex: 1; display: flex; flex-direction: column; overflow: hidden; position: relative;
  background:
    linear-gradient(rgba(124,58,237,0.025) 1px, transparent 1px),
    linear-gradient(90deg, rgba(124,58,237,0.025) 1px, transparent 1px),
    #0a0a1a;
  background-size: 40px 40px, 40px 40px;
}
.wf-canvas-header { display: flex; align-items: center; justify-content: space-between; padding: 14px 20px; 
border-bottom: 1px solid var(--border); background: rgba(10,10,26,0.85); backdrop-filter: blur(8px); flex-shrink: 0; 
z-index: 5; }
.wf-canvas-title { font-size: 14px; font-weight: 700; color: var(--text); }
.wf-canvas-status { font-size: 10px; font-weight: 700; color: var(--emerald); background: rgba(16,185,129,0.1); 
border: 1px solid rgba(16,185,129,0.3); padding: 3px 10px; border-radius: 20px; letter-spacing: 0.06em; }
.workflow-canvas { flex: 1; position: relative; overflow: auto; }
.wf-connections { position: absolute; top: 0; left: 0; width: 100%; height: 100%; pointer-events: none; z-index: 1; }
.wf-node {
  position: absolute; background: var(--surface2); border: 1px solid var(--border2);
  border-radius: 10px; padding: 12px 16px; min-width: 130px; cursor: pointer;
  transition: all 0.2s; z-index: 2; box-shadow: 0 4px 16px rgba(0,0,0,0.3); user-select: none;
}
.wf-node:hover { transform: scale(1.04); box-shadow: 0 8px 24px rgba(0,0,0,0.4); }
.wf-node.type-orchestrator { border-color: var(--violet); background: rgba(124,58,237,0.1); box-shadow: 0 0 20px 
rgba(124,58,237,0.2); }
.wf-node.type-copy    { border-color: rgba(249,115,22,0.5); }
.wf-node.type-review  { border-color: rgba(168,85,247,0.5); }
.wf-node.type-publish { border-color: rgba(16,185,129,0.5); }
.wf-node.type-research{ border-color: rgba(59,130,246,0.5); }
.wf-node.status-running { animation: node-pulse 2s ease-in-out infinite; }
@keyframes node-pulse { 0%,100%{box-shadow:0 4px 16px rgba(0,0,0,0.3)} 50%{box-shadow:0 0 20px rgba(59,130,246,0.3)} }
.wf-node-label { font-size: 12px; font-weight: 700; color: var(--text); margin-bottom: 4px; }
.wf-node-type { font-size: 9px; color: var(--text-dim); letter-spacing: 0.08em; text-transform: uppercase; 
margin-bottom: 6px; }
.wf-node-bar { height: 2px; background: var(--border2); border-radius: 1px; overflow: hidden; }
.wf-node-bar-fill { height: 100%; border-radius: 1px; background: var(--progress); }
.wf-node.type-orchestrator .wf-node-bar-fill { background: var(--violet); }
.wf-node.type-copy    .wf-node-bar-fill { background: var(--orange); }
.wf-node.type-review  .wf-node-bar-fill { background: var(--purple); }
.wf-node.type-publish .wf-node-bar-fill { background: var(--emerald); }

.task-detail-panel { width: 250px; flex-shrink: 0; background: var(--surface); border-left: 1px solid var(--border); 
display: flex; flex-direction: column; }
.tdp-title { font-size: 9px; font-weight: 800; letter-spacing: 0.12em; color: var(--text-dim); padding: 16px 14px 8px; 
border-bottom: 1px solid var(--border); text-transform: uppercase; }
.tdp-content { padding: 14px; flex: 1; overflow-y: auto; }
.tdp-empty { color: var(--text-dim); font-size: 12px; text-align: center; margin-top: 40px; line-height: 1.6; }
.tdp-task-name { font-size: 14px; font-weight: 700; color: var(--text); margin-bottom: 6px; }
.tdp-progress-big { font-size: 28px; font-weight: 900; color: var(--violet); font-family: var(--font-mono); 
margin-bottom: 14px; }
.tdp-section-label { font-size: 9px; font-weight: 800; letter-spacing: 0.12em; color: var(--text-dim); margin-bottom: 
6px; text-transform: uppercase; }
.tdp-subtasks { display: flex; flex-direction: column; gap: 6px; margin-bottom: 16px; }
.tdp-subtask { display: flex; align-items: center; gap: 8px; font-size: 11px; color: var(--text-dim); }
.tdp-subtask.done { color: var(--emerald); }
.tdp-context { font-size: 11px; color: var(--text-mid); line-height: 1.5; background: var(--surface2); border: 1px 
solid var(--border2); border-radius: 6px; padding: 8px 10px; }

/* â”€â”€â”€ VIEW: TREINO â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.train-layout { max-width: 680px; margin: 0 auto; padding: 28px 24px; overflow-y: auto; height: 100%; box-sizing: 
border-box; }
.train-subtitle { color: var(--text-dim); font-size: 13px; line-height: 1.6; margin: 8px 0 24px; }
.train-agent-select { display: flex; align-items: center; gap: 12px; margin-bottom: 10px; }
.train-agent-select label { font-size: 12px; color: var(--text-dim); font-weight: 600; flex-shrink: 0; }
#train-agent-sel { background: var(--surface2); border: 1px solid var(--border2); color: var(--text); border-radius: 
8px; padding: 7px 10px; font-size: 12px; outline: none; font-family: var(--font); }
.train-url-input { display: flex; gap: 8px; margin-bottom: 24px; }
#train-url { flex: 1; background: var(--surface2); border: 1px solid var(--border2); color: var(--text); 
border-radius: 8px; padding: 10px 14px; font-size: 13px; outline: none; transition: border-color 0.2s; font-family: 
var(--font); }
#train-url:focus { border-color: var(--violet); }
#train-url::placeholder { color: var(--text-dim); }
#train-add-btn { padding: 10px 20px; background: var(--violet); color: #fff; border: none; border-radius: 8px; 
font-size: 13px; font-weight: 700; cursor: pointer; transition: all 0.2s; font-family: var(--font); flex-shrink: 0; }
#train-add-btn:hover { background: #6d28d9; box-shadow: 0 0 14px rgba(124,58,237,0.4); }
.train-empty { color: var(--text-dim); font-size: 12px; text-align: center; padding: 32px; }
.train-queue { display: flex; flex-direction: column; gap: 10px; }
.train-item { background: var(--surface2); border: 1px solid var(--border2); border-radius: 10px; padding: 14px 16px; 
display: flex; align-items: center; gap: 14px; }
.ti-icon { font-size: 22px; flex-shrink: 0; }
.ti-info { flex: 1; min-width: 0; }
.ti-title { font-size: 13px; font-weight: 600; color: var(--text); margin-bottom: 4px; white-space: nowrap; overflow: 
hidden; text-overflow: ellipsis; }
.ti-agent { font-size: 10px; color: var(--text-dim); margin-bottom: 6px; }
.ti-progress { height: 3px; background: var(--border2); border-radius: 2px; overflow: hidden; }
.ti-progress-fill { height: 100%; background: linear-gradient(90deg, var(--violet), var(--cyan)); border-radius: 2px; 
transition: width 0.5s; }
.ti-status { font-size: 10px; color: var(--text-dim); font-weight: 600; flex-shrink: 0; }
.ti-status.studying { color: var(--violet); }
.ti-status.done { color: var(--emerald); }

/* â”€â”€â”€ VIEW: INTEGRAÃ‡Ã•ES 
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.integrations-layout { padding: 24px; overflow-y: auto; height: 100%; box-sizing: border-box; }
.integrations-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(190px, 1fr)); gap: 14px; }
.integration-card { background: var(--surface2); border: 1px solid var(--border2); border-radius: 12px; padding: 20px; 
cursor: pointer; transition: all 0.2s; text-align: center; }
.integration-card:hover { border-color: rgba(124,58,237,0.3); box-shadow: 0 4px 14px rgba(0,0,0,0.2); }
.integration-card.connected { border-color: rgba(16,185,129,0.4); background: rgba(16,185,129,0.04); }
.ic-icon { font-size: 30px; margin-bottom: 10px; }
.ic-name { font-size: 13px; font-weight: 700; color: var(--text); margin-bottom: 4px; }
.ic-desc { font-size: 11px; color: var(--text-dim); line-height: 1.4; margin-bottom: 12px; }
.ic-status { display: inline-block; font-size: 10px; font-weight: 700; padding: 3px 10px; border-radius: 20px; 
letter-spacing: 0.06em; }
.ic-status.connected    { background: rgba(16,185,129,0.12); color: var(--emerald); border: 1px solid 
rgba(16,185,129,0.3); }
.ic-status.disconnected { background: var(--surface); color: var(--text-dim); border: 1px solid var(--border2); }

/* â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
   SALA DOS AGENTES â€” animated agent room (bonequinhos)
   â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â• */
.room-layout { display: flex; flex-direction: column; height: 100%; padding: 20px 24px; box-sizing: border-box; gap: 
14px; }
.room-header { display: flex; justify-content: space-between; align-items: flex-end; }
.room-stats { display: flex; gap: 16px; }
.rstat { font-size: 12px; color: var(--text-dim); }
.rstat b { color: var(--violet); font-size: 15px; margin-right: 3px; }

.room-floor {
  position: relative; flex: 1; min-height: 380px; border-radius: 16px; overflow: hidden;
  background: radial-gradient(ellipse at 50% 30%, rgba(124,58,237,0.10) 0%, transparent 60%),
              linear-gradient(160deg, #11112a 0%, #0a0a1a 100%);
  border: 1px solid var(--border2);
  box-shadow: inset 0 0 80px rgba(0,0,0,0.5);
}
.room-grid-bg {
  position: absolute; inset: 0;
  background-image: linear-gradient(rgba(124,58,237,0.06) 1px, transparent 1px),
                    linear-gradient(90deg, rgba(124,58,237,0.06) 1px, transparent 1px);
  background-size: 44px 44px;
  mask-image: radial-gradient(ellipse at 50% 60%, #000 40%, transparent 90%);
}

/* desk / workstation */
.room-desk {
  position: absolute; width: 64px; height: 30px; border-radius: 6px;
  background: linear-gradient(180deg, #1c1c3a, #14142c);
  border: 1px solid rgba(124,58,237,0.25);
  transform: translate(-50%, 8px);
  box-shadow: 0 6px 14px rgba(0,0,0,0.4);
}
.room-desk::after {
  content: 'ðŸ–¥ï¸'; position: absolute; top: -16px; left: 50%; transform: translateX(-50%);
  font-size: 15px; filter: grayscale(0.3);
}

/* agent character */
.room-agent {
  position: absolute; width: 56px; text-align: center;
  transform: translate(-50%, -50%);
  transition: left 1.6s cubic-bezier(.45,.05,.3,1), top 1.6s cubic-bezier(.45,.05,.3,1);
  cursor: pointer; z-index: 5;
}
.room-agent-body {
  font-size: 30px; line-height: 1; position: relative; display: inline-block;
  animation: agentBob 2.4s ease-in-out infinite;
  filter: drop-shadow(0 4px 6px rgba(0,0,0,0.5));
}
@keyframes agentBob { 0%,100% { transform: translateY(0); } 50% { transform: translateY(-3px); } }
.room-agent.working .room-agent-body { animation: agentWork 0.5s ease-in-out infinite; }
@keyframes agentWork { 0%,100% { transform: translateY(0) rotate(-2deg); } 50% { transform: translateY(-1px) 
rotate(2deg); } }
.room-agent.offline { opacity: 0.35; filter: grayscale(1); }
.room-agent.offline .room-agent-body { animation: none; }

.room-agent-name {
  font-size: 10px; font-weight: 700; color: var(--text); margin-top: 2px;
  text-shadow: 0 1px 3px #000; white-space: nowrap;
}
.room-agent-status-dot {
  position: absolute; top: 2px; right: 8px; width: 8px; height: 8px; border-radius: 50%;
  border: 2px solid #0a0a1a;
}
.room-agent-status-dot.working { background: var(--violet); box-shadow: 0 0 8px var(--violet); animation: pulseDot 1s 
infinite; }
.room-agent-status-dot.idle    { background: #eab308; }
.room-agent-status-dot.offline { background: #6b7280; }
@keyframes pulseDot { 0%,100% { transform: scale(1); } 50% { transform: scale(1.4); } }

/* glow ring under working agent */
.room-agent.working::before {
  content: ''; position: absolute; bottom: -6px; left: 50%; transform: translateX(-50%);
  width: 40px; height: 12px; border-radius: 50%;
  background: radial-gradient(ellipse, rgba(124,58,237,0.5), transparent 70%);
  animation: ringPulse 1.5s ease-in-out infinite;
}
@keyframes ringPulse { 0%,100% { opacity: 0.4; } 50% { opacity: 0.9; } }

/* speech / task bubble */
.room-bubble {
  position: absolute; bottom: calc(100% + 4px); left: 50%; transform: translateX(-50%);
  background: rgba(124,58,237,0.95); color: #fff; font-size: 10px; font-weight: 600;
  padding: 4px 8px; border-radius: 10px; white-space: nowrap; max-width: 160px;
  overflow: hidden; text-overflow: ellipsis; pointer-events: none;
  box-shadow: 0 4px 10px rgba(0,0,0,0.4); opacity: 0; transition: opacity 0.3s;
}
.room-bubble.show { opacity: 1; }
.room-bubble::after {
  content: ''; position: absolute; top: 100%; left: 50%; transform: translateX(-50%);
  border: 5px solid transparent; border-top-color: rgba(124,58,237,0.95);
}

.room-log {
  height: 92px; overflow-y: auto; background: var(--surface2); border: 1px solid var(--border2);
  border-radius: 10px; padding: 8px 12px; font-size: 11px; line-height: 1.7; color: var(--text-dim);
  font-family: 'SF Mono', Menlo, monospace;
}
.room-log .rl-line { opacity: 0; animation: fadeInLine 0.4s forwards; }
.room-log .rl-time { color: var(--violet); margin-right: 6px; }
.room-log .rl-agent { color: var(--text); font-weight: 700; }
@keyframes fadeInLine { to { opacity: 1; } }

/* â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
   PROJETOS â€” master orchestration
   â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â• */
.projects-layout { padding: 24px; overflow-y: auto; height: 100%; box-sizing: border-box; }
.projects-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 18px; }
.project-card {
  background: var(--surface2); border: 1px solid var(--border2); border-radius: 14px;
  padding: 20px; transition: all 0.2s; position: relative; overflow: hidden;
}
.project-card::before {
  content: ''; position: absolute; top: 0; left: 0; right: 0; height: 3px; background: var(--accent-grad, 
var(--violet));
}
.project-card:hover { border-color: rgba(124,58,237,0.4); transform: translateY(-2px); box-shadow: 0 8px 24px 
rgba(0,0,0,0.3); }
.pc-head { display: flex; align-items: center; gap: 12px; margin-bottom: 12px; }
.pc-icon { font-size: 30px; }
.pc-name { font-size: 16px; font-weight: 700; color: var(--text); }
.pc-tag  { font-size: 11px; color: var(--text-dim); }
.pc-desc { font-size: 12px; color: var(--text-dim); line-height: 1.5; margin-bottom: 14px; }
.pc-meta { display: flex; gap: 14px; margin-bottom: 14px; }
.pc-stat { font-size: 11px; color: var(--text-dim); }
.pc-stat b { display: block; font-size: 18px; color: var(--violet); }
.pc-status { display: inline-flex; align-items: center; gap: 6px; font-size: 11px; font-weight: 700; padding: 4px 
10px; border-radius: 20px; }
.pc-status.online  { background: rgba(16,185,129,0.12); color: var(--emerald); }
.pc-status.planned { background: var(--surface); color: var(--text-dim); border: 1px solid var(--border2); }
.pc-actions { display: flex; gap: 8px; margin-top: 14px; }
.pc-btn {
  flex: 1; padding: 9px; border-radius: 8px; border: 1px solid var(--border2); background: var(--surface);
  color: var(--text); font-size: 12px; font-weight: 600; cursor: pointer; transition: all 0.15s;
}
.pc-btn:hover { background: var(--violet); border-color: var(--violet); }
.pc-btn:disabled { opacity: 0.4; cursor: not-allowed; }

/* AIOS design polish */
.topbar,
.sidebar,
.panel-left,
.panel-chat,
.panel-right,
.activity-panel,
.missions-panel,
.task-detail-panel,
.conclave-wrap,
.squad-wrap {
  background: var(--glass-deep);
  border-color: rgba(124,58,237,0.22);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
}

.sidebar,
.panel-left,
.missions-panel {
  box-shadow: 8px 0 28px rgba(0,0,0,0.28);
}

.panel-chat,
.panel-right,
.activity-panel,
.task-detail-panel {
  box-shadow: -8px 0 28px rgba(0,0,0,0.28);
}

.brain-card,
.left-section,
.right-section,
.gallery-card,
.project-card,
.integration-card,
.activity-item,
.mission-item,
.wf-node,
.tdp-context,
.train-item,
.room-log,
.msg.assistant .msg-bubble,
.typing-indicator {
  background: var(--glass);
  border-color: rgba(124,58,237,0.18);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  box-shadow: 0 8px 24px rgba(0,0,0,0.18), inset 0 1px 0 rgba(255,255,255,0.035);
}

.chat-input,
.wf-canvas-header {
  background: rgba(13,13,26,0.82);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
}

.stage::before {
  background-image:
    linear-gradient(rgba(124,58,237,0.04) 1px, transparent 1px),
    linear-gradient(90deg, rgba(124,58,237,0.04) 1px, transparent 1px);
  mask-image: radial-gradient(ellipse at 50% 50%, #000 38%, transparent 86%);
  -webkit-mask-image: radial-gradient(ellipse at 50% 50%, #000 38%, transparent 86%);
}

.chat-log {
  background-image:
    linear-gradient(rgba(124,58,237,0.04) 1px, transparent 1px),
    linear-gradient(90deg, rgba(124,58,237,0.04) 1px, transparent 1px);
}

.workflow-canvas-area {
  background:
    radial-gradient(ellipse at 50% 20%, rgba(124,58,237,0.10), transparent 55%),
    linear-gradient(rgba(124,58,237,0.04) 1px, transparent 1px),
    linear-gradient(90deg, rgba(124,58,237,0.04) 1px, transparent 1px),
    var(--bg);
  background-size: auto, 40px 40px, 40px 40px, auto;
}

.room-floor {
  box-shadow: inset 0 0 90px rgba(0,0,0,0.58), 0 14px 34px rgba(0,0,0,0.26), 0 0 32px rgba(124,58,237,0.10);
}

.tc-val,
.tc-lbl,
.svc,
.agents-online-badge,
.sb-logo-ver,
.sb-section-label,
.sb-agent-role,
.left-title,
.right-title,
.sys-row span:first-child,
.sys-row span:last-child,
.plan-box,
.res-stats,
.tag,
.voice-status,
.gc-role,
.gc-skill,
.wf-canvas-status,
.wf-node-type,
.mi-pct,
.tdp-title,
.tdp-progress-big,
.tdp-section-label,
.ti-agent,
.ti-status,
.ic-status,
.rstat b,
.room-log,
.room-bubble,
.pc-tag,
.pc-stat b,
.pc-status,
.ai-agent,
.ai-time {
  font-family: var(--font-mono);
}

.tc-val,
.sys-row span:last-child,
.res-stats span {
  color: var(--matrix);
  text-shadow: 0 0 10px rgba(0,255,156,0.28);
}

.gallery-card,
.project-card,
.integration-card {
  transition: border-color 0.2s ease, box-shadow 0.2s ease, transform 0.2s ease, background 0.2s ease;
}

.gallery-card:hover,
.project-card:hover,
.integration-card:hover {
  border-color: var(--violet);
  transform: translateY(-2px);
  box-shadow: var(--shadow-hover), 0 0 22px rgba(124,58,237,0.16);
}

.mission-item:hover,
.wf-node:hover,
.pc-btn:hover,
.filter-btn:hover,
.qbtn:hover {
  box-shadow: 0 0 18px rgba(124,58,237,0.12);
}

.sb-item.active,
.filter-btn.active,
.mission-item.active {
  box-shadow: inset 3px 0 0 var(--violet), 0 0 20px rgba(124,58,237,0.10);
}

/* â”€â”€â”€ TOAST NOTIFICATIONS â”€â”€â”€ */
.toast-container {
  position: fixed;
  bottom: 24px;
  right: 24px;
  z-index: 9999;
  display: flex;
  flex-direction: column;
  gap: 8px;
  pointer-events: none;
}
.toast {
  background: var(--surface2);
  border: 1px solid var(--border);
  border-left: 3px solid var(--violet);
  border-radius: var(--radius);
  padding: 10px 16px;
  font-family: var(--font);
  font-size: 13px;
  color: var(--text);
  max-width: 300px;
  box-shadow: 0 4px 24px rgba(0,0,0,0.4);
  opacity: 0;
  transform: translateX(20px);
  transition: opacity 0.25s, transform 0.25s;
  pointer-events: auto;
}
.toast.show { opacity: 1; transform: translateX(0); }
.toast.success { border-left-color: var(--emerald); }
.toast.warning { border-left-color: var(--amber); }
.toast.error   { border-left-color: var(--red); }


 succeeded in 466ms:
// Javis v2 â€” frontend app
// Connects to FastAPI server at localhost:8000

const API = "http://localhost:8000";

// â”€â”€â”€ DOM refs â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â
”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
const chatLog      = document.getElementById("chat-log");
const form         = document.getElementById("form");
const inputEl      = document.getElementById("input");
const modelSel     = document.getElementById("model-sel");
const useConclave  = document.getElementById("use-conclave");
const sendBtn      = document.querySelector(".send-btn");
const planBox      = document.getElementById("plan-box");
const conclaveWrap = document.getElementById("conclave-wrap");
const squadWrap    = document.getElementById("squad-wrap");
const squadRounds  = document.getElementById("squad-rounds");
const svcOllama    = document.getElementById("svc-ollama");
const svcWebui     = document.getElementById("svc-webui");
const micBtn       = document.getElementById("mic-btn");
const useTts       = document.getElementById("use-tts");
const voiceStatus  = document.getElementById("voice-status");
const voiceInterim = document.getElementById("voice-interim");

// Brain cards (new layout)
const brainCards = {
  main:     document.getElementById("bc-main"),
  conclave: document.getElementById("bc-conclave"),
  memory:   document.getElementById("bc-memory"),
};

// Stat elements
const statCount    = document.getElementById("stat-count");
const statBrainLbl = document.getElementById("stat-brain-lbl");
const statSession  = document.getElementById("stat-session");
const sysMsEl      = document.getElementById("sys-ms");
const sysCountEl   = document.getElementById("sys-count");
const sysBrainEl   = document.getElementById("sys-brain");
const sysUptimeEl  = document.getElementById("sys-uptime");
const sysIntentEl  = document.getElementById("sys-intent");

let _msgCount = 0;
const _sessionStart = Date.now();

// Conclave text areas
const ct = {
  critico:      document.getElementById("ct-critico"),
  advogado:     document.getElementById("ct-advogado"),
  sintetizador: document.getElementById("ct-sintetizador"),
};

// Agent cards
const agentCards = {};
document.querySelectorAll(".ag-card[data-id]").forEach(card => {
  agentCards[card.dataset.id] = card;
});

// â”€â”€â”€ Init â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”
€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
checkStatus();
setInterval(checkStatus, 30000);
setInterval(_tickSession, 1000);
setInterval(pollReminders, 15000);
loadChatHistory();

async function loadChatHistory() {
  try {
    const r = await fetch(`${API}/history`);
    if (!r.ok) return;
    const hist = await r.json();
    const recent = hist.slice(-30);
    for (const h of recent) {
      if (h.role === "user") appendMsg("user", esc(h.content));
      else if (h.role === "assistant") appendMsg("assistant", renderMarkdown(h.content));
    }
  } catch (e) { console.warn("HistÃ³rico:", e); }
}

// Lembretes vencidos â†’ Jamba avisa (mostra no chat + fala)
async function pollReminders() {
  try {
    const res = await fetch(`${API}/reminders/poll`);
    if (!res.ok) return;
    const data = await res.json();
    for (const r of (data.due || [])) {
      const msg = `â° Lembrete, senhor: ${r.text}`;
      removeWelcome();
      appendMsg("assistant", esc(msg), { brain: "memory", intent: "lembrete" });
      neuralBrain?.setState("thinking");
      neuralBrain?.fire?.();
      if (useTts?.checked) speak(msg);
      setTimeout(() => { if (!sendBtn.disabled) neuralBrain?.setState("idle"); }, 2500);
    }
  } catch (_) {}
}

// Brain card clicks
Object.entries(brainCards).forEach(([key, el]) => {
  el?.addEventListener("click", () => {
    setBrain(key);
    if (key === "conclave") useConclave.checked = true;
    else                    useConclave.checked = false;
  });
});

document.getElementById("conclave-toggle").addEventListener("click", () => {
  conclaveWrap.classList.toggle("open");
});
document.getElementById("squad-toggle")?.addEventListener("click", () => {
  squadWrap.classList.toggle("open");
});

// Debate autÃ´nomo
document.getElementById("btn-debate")?.addEventListener("click", () => {
  const task = inputEl.value.trim();
  if (!task) {
    inputEl.focus();
    inputEl.placeholder = "Digite a tarefa para o debate autÃ´nomo...";
    return;
  }
  inputEl.value = "";
  runAutonomousDebate(task);
});

// â”€â”€â”€ Form submit â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â
”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
form.addEventListener("submit", e => {
  e.preventDefault();
  const text = inputEl.value.trim();
  if (!text || sendBtn.disabled) return;
  inputEl.value = "";
  sendMessage(text);
});

// File upload handler
const fileInput = document.getElementById("file-input");
if (fileInput) {
  fileInput.addEventListener("change", async () => {
    const file = fileInput.files[0];
    if (!file) return;
    fileInput.value = "";
    removeWelcome();
    appendMsg("user", `ðŸ“Ž <strong>${file.name}</strong> enviado para anÃ¡lise`);
    const typingEl = appendTyping("analisando arquivo...");
    try {
      const fd = new FormData();
      fd.append("file", file);
      const res = await fetch(`${API}/upload`, { method: "POST", body: fd });
      const data = await res.json();
      typingEl.remove();
      if (data.status === "ok") {
        appendMsg("assistant", renderMarkdown(data.message), { brain: "file-analyzer" });
      } else {
        appendMsg("assistant", `Erro ao analisar arquivo: ${data.message}`);
      }
    } catch (err) {
      typingEl.remove();
      appendMsg("assistant", `Erro ao enviar arquivo: ${err.message}`);
    }
  });
}

// Quick action buttons
document.querySelectorAll(".qbtn:not(#btn-debate)").forEach(btn => {
  btn.addEventListener("click", () => {
    const cmd = btn.dataset.cmd;
    if (!cmd || sendBtn.disabled) return;
    // Comando incompleto (termina com espaÃ§o) â†’ preenche o campo e aguarda a URL
    if (cmd.endsWith(" ")) {
      inputEl.value = cmd;
      inputEl.focus();
      inputEl.placeholder = "cole a URL do site aqui...";
    } else {
      sendMessage(cmd);
    }
  });
});

// â”€â”€â”€ Streaming message 
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
async function sendMessage(text) {
  removeWelcome();
  appendMsg("user", esc(text));
  setLoading(true);
  clearAgentActivity();

  const streamDiv = _appendStreamMsg();
  let fullText = "";
  let meta = {};

  try {
    const res = await fetch(`${API}/chat/stream`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        message:      text,
        use_conclave: useConclave.checked,
        model:        modelSel.value,
      }),
    });

    if (!res.ok) {
      streamDiv.remove();
      const err = await res.json().catch(() => ({}));
      appendMsg("error", `âš ï¸ Erro ${res.status}: ${esc(err.error || res.statusText)}`);
      return;
    }

    const reader  = res.body.getReader();
    const decoder = new TextDecoder();
    let buf = "";

    while (true) {
      const { done, value } = await reader.read();
      if (done) break;
      buf += decoder.decode(value, { stream: true });
      const lines = buf.split("\n");
      buf = lines.pop();

      for (const line of lines) {
        if (!line.startsWith("data: ")) continue;
        const raw = line.slice(6).trim();
        if (raw === "[DONE]") break;
        try {
          const ev = JSON.parse(raw);
          if (ev.type === "token") {
            fullText += ev.text;
            neuralBrain?.fire(1);   // cada token dispara um neurÃ´nio
            streamDiv.querySelector(".msg-bubble").innerHTML =
              renderMarkdown(fullText) + '<span class="stream-cursor">â–‹</span>';
            chatLog.scrollTop = chatLog.scrollHeight;
          } else if (ev.type === "meta") {
            meta = ev;
            if (ev.brain)  setBrain(ev.brain);
            if (ev.intent && sysIntentEl) sysIntentEl.textContent = ev.intent;
          } else if (ev.type === "done") {
            if (ev.text) fullText = ev.text;
            if (ev.brain)  { setBrain(ev.brain); meta.brain = ev.brain; }
            if (ev.intent) meta.intent = ev.intent;
            if (ev.ms)     { meta.ms = ev.ms; if (sysMsEl) sysMsEl.textContent = ev.ms + "ms"; reservoir?.push(ev.ms); 
}
          }
        } catch (_) {}
      }
    }

    // Finaliza a bolha
    streamDiv.querySelector(".msg-bubble").innerHTML = renderMarkdown(fullText || "Sem resposta.");
    streamDiv.classList.remove("streaming");
    if (meta.intent || meta.brain) {
      const md = document.createElement("div");
      md.className = "msg-meta";
      if (meta.intent) md.innerHTML += `<span class="tag">${esc(meta.intent)}</span>`;
      if (meta.brain)  md.innerHTML += `<span class="tag">ðŸ§  ${esc(meta.brain)}</span>`;
      if (meta.ms)     md.innerHTML += `<span>${meta.ms}ms</span>`;
      streamDiv.appendChild(md);
    }
    _msgCount++;
    if (statCount) statCount.textContent  = _msgCount;
    if (sysCountEl) sysCountEl.textContent = _msgCount;

    if (useTts?.checked && fullText) speak(fullText);

  } catch (err) {
    streamDiv.remove();
    if (err.name === "TypeError") {
      appendMsg("error", "âš ï¸ Servidor offline. Inicie com: <code>python backend/server.py</code>");
    } else {
      appendMsg("error", `âš ï¸ ${esc(err.message)}`);
    }
  } finally {
    setLoading(false);
  }
}

function _appendStreamMsg() {
  const div    = document.createElement("div");
  div.className = "msg assistant streaming";
  const bubble = document.createElement("div");
  bubble.className = "msg-bubble";
  bubble.innerHTML = '<span class="stream-cursor">â–‹</span>';
  div.appendChild(bubble);
  chatLog.appendChild(div);
  chatLog.scrollTop = chatLog.scrollHeight;
  return div;
}

// â”€â”€â”€ Debate autÃ´nomo (Squad) 
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
async function runAutonomousDebate(task) {
  removeWelcome();
  appendMsg("user", `ðŸ¤ Debate autÃ´nomo: ${esc(task)}`);
  setLoading(true);
  clearAgentActivity();

  const typing = appendTyping("squad debatendo...");

  try {
    const res = await fetch(`${API}/debate`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        task,
        agents: ["architect", "developer", "analyst", "qa"],
        rounds: 2,
        model:  modelSel.value,
      }),
    });

    typing.remove();

    if (!res.ok) {
      appendMsg("error", `âš ï¸ Erro no debate: ${res.statusText}`);
      return;
    }

    const data = await res.json();

    appendMsg("assistant", renderMarkdown(data.synthesis || "Sem sÃ­ntese."), {
      intent: "debate autÃ´nomo",
      brain: "squad",
    });

    if (data.rounds?.length) {
      renderSquadRounds(data.rounds);
      squadWrap.classList.remove("hidden");
      squadWrap.classList.add("open");
    }

    if (data.saved_to) {
      appendMsg("assistant",
        `ðŸ’¾ DecisÃ£o salva em <code>_memoria/${esc(data.saved_to)}</code>`,
        {}
      );
    }

    setBrain("conclave");
    highlightAgents(data.agents || []);

  } catch (err) {
    typing.remove();
    appendMsg("error", `âš ï¸ ${esc(err.message)}`);
  } finally {
    setLoading(false);
  }
}

// â”€â”€â”€ Handle API response 
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
function handleResponse(data) {
  const response  = data.response || data.message || "Sem resposta.";
  const brain     = data.brain || "main";
  const agents    = data.agents || [];
  const plan      = data.plan || "";
  const conclave  = data.conclave || {};
  const squad     = data.squad || {};
  const isBlocked = data.status === "blocked";

  appendMsg(
    isBlocked ? "blocked" : "assistant",
    renderMarkdown(response),
    { intent: data.intent, brain, ms: data.ms }
  );

  setBrain(brain);

  // Update stats
  _msgCount++;
  if (statCount)  statCount.textContent  = _msgCount;
  if (sysCountEl) sysCountEl.textContent = _msgCount;
  if (data.ms != null) {
    if (sysMsEl) sysMsEl.textContent = data.ms + "ms";
    reservoir?.push(data.ms);
  }
  if (data.intent && sysIntentEl) {
    sysIntentEl.textContent = data.intent;
  }

  if (agents.length) highlightAgents(agents);
  if (plan)          planBox.textContent = plan;

  if (conclave.used) {
    updateConclave(conclave);
    conclaveWrap.classList.remove("hidden");
    conclaveWrap.classList.add("open");
  }

  if (squad.used) {
    renderSquadRounds(squad.rounds || []);
    squadWrap.classList.remove("hidden");
    squadWrap.classList.add("open");
  }
}

// â”€â”€â”€ Squad rounds renderer 
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
function renderSquadRounds(rounds) {
  squadRounds.innerHTML = "";

  const agentIcons = {
    architect: "ðŸ—ï¸", developer: "ðŸ’»", analyst: "ðŸ“Š",
    qa: "ðŸ”", jarvis_soul: "âœ¨",
  };

  rounds.forEach(r => {
    const div = document.createElement("div");
    div.className = "squad-round";

    const label = r.type === "analise" ? "ANÃLISE INDIVIDUAL" : `DEBATE â€” RODADA ${r.round}`;
    div.innerHTML = `<div class="squad-round-hdr">RODADA ${r.round} Â· ${label}</div>`;

    const outputs = document.createElement("div");
    outputs.className = "squad-agent-outputs";

    for (const [agentId, txt] of Object.entries(r.outputs || {})) {
      const agDiv = document.createElement("div");
      agDiv.className = "squad-agent";
      const icon = agentIcons[agentId] || "ðŸ¤–";
      agDiv.innerHTML = `
        <div class="squad-agent-name">${icon} ${agentId.toUpperCase()}</div>
        <div class="squad-agent-txt">${esc(txt).replace(/\n/g, "<br>")}</div>
      `;
      outputs.appendChild(agDiv);
    }

    div.appendChild(outputs);
    squadRounds.appendChild(div);
  });
}

// â”€â”€â”€ UI helpers â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”
€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
function appendMsg(role, html, meta = {}) {
  const div = document.createElement("div");
  div.className = `msg ${role}`;

  const bubble = document.createElement("div");
  bubble.className = "msg-bubble";
  bubble.innerHTML = html;
  div.appendChild(bubble);

  if (meta && (meta.intent || meta.brain || meta.ms != null)) {
    const metaDiv = document.createElement("div");
    metaDiv.className = "msg-meta";
    if (meta.intent) metaDiv.innerHTML += `<span class="tag">${esc(meta.intent)}</span>`;
    if (meta.brain)  metaDiv.innerHTML += `<span class="tag">ðŸ§  ${esc(meta.brain)}</span>`;
    if (meta.ms != null) metaDiv.innerHTML += `<span>${meta.ms}ms</span>`;
    div.appendChild(metaDiv);
  }

  chatLog.appendChild(div);
  chatLog.scrollTop = chatLog.scrollHeight;
  return div;
}

function appendTyping(label = "pensando...") {
  const div = document.createElement("div");
  div.className = "typing-indicator";
  div.innerHTML = `<div class="typing-dots"><span></span><span></span><span></span></div><span>${esc(label)}</span>`;
  chatLog.appendChild(div);
  chatLog.scrollTop = chatLog.scrollHeight;
  return div;
}

function removeWelcome() {
  const wb = chatLog.querySelector(".welcome-block");
  if (wb) wb.remove();
}

function setLoading(on) {
  sendBtn.disabled = on;
  inputEl.disabled = on;
  // Orbe reage: pensa enquanto carrega; ao terminar volta a ouvir (se voz ativa) ou idle
  if (on) {
    neuralBrain?.setState(useConclave?.checked ? "conclave" : "thinking");
  } else if (typeof autoWhi !== "undefined" && autoWhi?.active) {
    neuralBrain?.setState("listening");
  } else {
    neuralBrain?.setState("idle");
  }
}

function setBrain(name) {
  const map = { squad: "conclave" };
  const key = map[name] || name;
  Object.entries(brainCards).forEach(([k, el]) => {
    el?.classList.toggle("active", k === key);
  });
  const label = { main: "MAIN", conclave: "CONCLAVE", memory: "MEMORY" }[key] || key.toUpperCase();
  if (statBrainLbl) statBrainLbl.textContent = label;
  if (sysBrainEl)   sysBrainEl.textContent   = label;
  // Reflete o cÃ©rebro ativo na cor dos neurÃ´nios enquanto processa
  if (sendBtn.disabled) {
    neuralBrain?.setState(key === "conclave" ? "conclave" : "thinking");
  }
}

function highlightAgents(ids) {
  clearAgentActivity();
  ids.forEach(id => {
    if (agentCards[id]) agentCards[id].classList.add("active");
  });
  setTimeout(clearAgentActivity, 5000);
}

function clearAgentActivity() {
  Object.values(agentCards).forEach(c => c.classList.remove("active"));
}

function updateConclave(c) {
  if (c.critico)   ct.critico.textContent   = c.critico;
  if (c.advogado)  ct.advogado.textContent  = c.advogado;
  if (c.synthesis) ct.sintetizador.textContent = c.synthesis;
}

// â”€â”€â”€ Status check â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
async function checkStatus() {
  setSvc(svcOllama, "checking");
  setSvc(svcWebui,  "checking");
  try {
    const res  = await fetch(`${API}/status`, { signal: AbortSignal.timeout(4000) });
    const data = await res.json();
    const svcs = data.services || {};

    setSvc(svcOllama, svcs["Ollama"]?.status     === "online" ? "online" : "offline");
    setSvc(svcWebui,  svcs["Open WebUI"]?.status === "online" ? "online" : "offline");
  } catch {
    setSvc(svcOllama, "offline");
    setSvc(svcWebui,  "offline");
  }
}

function setSvc(el, state) {
  el.className = "svc";
  if (state === "offline")  el.classList.add("offline");
  if (state === "checking") el.classList.add("checking");
}

// â”€â”€â”€ Markdown renderer 
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
function renderMarkdown(raw) {
  const blocks = [];
  const withPH = raw.replace(/```[\w]*\n?([\s\S]*?)```/g, (_, code) => {
    blocks.push(code.trim());
    return `\x00BLOCK${blocks.length - 1}\x00`;
  });

  let out = esc(withPH)
    .replace(/`([^`]+)`/g, "<code>$1</code>")
    .replace(/\*\*(.+?)\*\*/g, "<strong>$1</strong>")
    .replace(/\*(.+?)\*/g, "<em>$1</em>")
    .replace(/^#{1,3}\s+(.+)$/gm, "<strong>$1</strong>")
    .replace(/^\s*[-*]\s+(.+)$/gm, "â€¢ $1")
    .replace(/\n/g, "<br>");

  blocks.forEach((code, i) => {
    out = out.replace(`\x00BLOCK${i}\x00`, `<pre><code>${esc(code)}</code></pre>`);
  });
  return out;
}

function esc(s) {
  return String(s)
    .replace(/&/g, "&amp;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;")
    .replace(/"/g, "&quot;");
}

// Update brain card name when model changes
modelSel?.addEventListener("change", () => {
  const nameEl = document.getElementById("bc-main-name");
  if (nameEl) nameEl.textContent = modelSel.value;
});

// â”€â”€â”€ Session Timer 
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
function _tickSession() {
  const s = Math.floor((Date.now() - _sessionStart) / 1000);
  const mm = String(Math.floor(s / 60)).padStart(2, "0");
  const ss = String(s % 60).padStart(2, "0");
  const t = `${mm}:${ss}`;
  if (statSession)  statSession.textContent  = t;
  if (sysUptimeEl)  sysUptimeEl.textContent  = t;
}

// â”€â”€â”€ Reservoir Chart 
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
class ReservoirChart {
  constructor(canvas) {
    this.ctx  = canvas.getContext("2d");
    this.w    = canvas.width;
    this.h    = canvas.height;
    this.data = new Array(32).fill(0);
    this.peak = 0;
    this._draw();
  }

  push(ms) {
    this.data.push(ms);
    if (this.data.length > 32) this.data.shift();
    if (ms > this.peak) this.peak = ms;
    const avg = Math.round(this.data.reduce((a, b) => a + b, 0) / this.data.filter(v => v > 0).length || 1);
    const resAvg  = document.getElementById("res-avg");
    const resPeak = document.getElementById("res-peak");
    if (resAvg)  resAvg.textContent  = `avg ${avg}ms`;
    if (resPeak) resPeak.textContent = `pico ${this.peak}ms`;
    this._draw();
  }

  _draw() {
    const { ctx, w, h, data } = this;
    const max = Math.max(500, ...data) * 1.15;
    ctx.clearRect(0, 0, w, h);

    // Grid lines
    ctx.strokeStyle = "rgba(34,34,58,0.9)";
    ctx.lineWidth = 1;
    for (let i = 1; i < 4; i++) {
      const y = (h / 4) * i;
      ctx.beginPath(); ctx.moveTo(0, y); ctx.lineTo(w, y); ctx.stroke();
    }

    // Bars
    const bw = w / data.length;
    data.forEach((v, i) => {
      if (!v) return;
      const bh  = (v / max) * (h - 2);
      const x   = i * bw;
      const y   = h - bh;
      const alpha = 0.3 + (i / data.length) * 0.7;
      const grad = ctx.createLinearGradient(0, y, 0, h);
      grad.addColorStop(0, `rgba(0,229,255,${alpha})`);
      grad.addColorStop(1, `rgba(0,229,255,${alpha * 0.15})`);
      ctx.fillStyle = grad;
      ctx.fillRect(x + 1, y, Math.max(bw - 2, 1), bh);
    });

    // Trend line
    const pts = data.map((v, i) => [i * bw + bw / 2, h - (v / max) * (h - 2)]);
    ctx.beginPath();
    ctx.strokeStyle = "rgba(0,229,255,0.55)";
    ctx.lineWidth = 1.5;
    ctx.setLineDash([]);
    pts.forEach(([x, y], i) => i === 0 ? ctx.moveTo(x, y) : ctx.lineTo(x, y));
    ctx.stroke();
  }
}

const _resCanvas = document.getElementById("reservoir-canvas");
const reservoir  = _resCanvas ? new ReservoirChart(_resCanvas) : null;

// â”€â”€â”€ Voice Orb â€” orbe circular tecnolÃ³gico (cÃ©rebro de voz JAMBA) â”€â”€â”€
class VoiceOrb {
  constructor(canvas) {
    this.canvas = canvas;
    this.ctx    = canvas.getContext("2d");
    this.state  = "idle";              // idle | thinking | conclave | listening
    this.color  = { r: 0, g: 229, b: 255 };
    this.level  = 0;                   // 0..1 amplitude do microfone
    this._level = 0;                   // suavizado
    this.flash  = 0;                   // brilho extra a cada token
    this.wave   = new Array(64).fill(0);
    this._resize();
    window.addEventListener("resize", () => this._resize());
    this._loop();
  }

  _resize() {
    const r = this.canvas.getBoundingClientRect();
    const dpr = window.devicePixelRatio || 1;
    this.w = r.width || 360;  this.h = r.height || 360;
    this.canvas.width  = this.w * dpr;
    this.canvas.height = this.h * dpr;
    this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
  }

  setState(state) {
    this.state = state;
    const stage = document.getElementById("orb-stage")?.closest(".stage");
    const lblS  = document.getElementById("neural-state");
    const lblP  = document.getElementById("neural-pulse-lbl");
    stage?.classList.remove("thinking", "conclave");
    if (state === "thinking") {
      this.color = { r: 0, g: 229, b: 255 };
      stage?.classList.add("thinking");
      if (lblS) lblS.textContent = "PROCESSANDO";
      if (lblP) lblP.textContent = "raciocinando...";
    } else if (state === "conclave") {
      this.color = { r: 168, g: 85, b: 247 };
      stage?.classList.add("conclave");
      if (lblS) lblS.textContent = "CONCLAVE";
      if (lblP) lblP.textContent = "debate interno";
    } else if (state === "listening") {
      this.color = { r: 0, g: 229, b: 255 };
      if (lblS) lblS.textContent = "OUVINDO";
      if (lblP) lblP.textContent = "fala que eu te escuto";
    } else {
      this.color = { r: 0, g: 229, b: 255 };
      if (lblS) lblS.textContent = "ONLINE";
      if (lblP) lblP.textContent = "aguardando comando";
    }
  }

  // nÃ­vel de Ã¡udio do microfone (0..1) â€” deixa o orbe reativo Ã  voz
  setLevel(v) { this.level = Math.min(1, Math.max(0, v)); }

  // brilho pontual a cada token gerado
  fire() { this.flash = Math.min(1, this.flash + 0.5); }

  _loop() {
    const { ctx, w, h, color } = this;
    const cx = w / 2, cy = h / 2;
    const R  = Math.min(w, h) * 0.34;     // raio base
    const t  = Date.now() / 1000;
    ctx.clearRect(0, 0, w, h);

    // suaviza nÃ­vel e flash
    this._level += (this.level - this._level) * 0.2;
    this.flash  *= 0.9;
    const active = this.state !== "idle";
    const energy = (active ? 0.5 : 0.18) + this._level * 0.9 + this.flash * 0.6;
    const C = (a) => `rgba(${color.r},${color.g},${color.b},${a})`;

    // halo externo
    const halo = ctx.createRadialGradient(cx, cy, R * 0.4, cx, cy, R * 2.0);
    halo.addColorStop(0, C(0.10 + energy * 0.10));
    halo.addColorStop(1, C(0));
    ctx.fillStyle = halo;
    ctx.fillRect(0, 0, w, h);

    // anÃ©is rotativos (arcos)
    const rings = [
      { rad: R * 1.55, w: 1,   sp: 0.25, span: 1.1, a: 0.18 },
      { rad: R * 1.34, w: 1.5, sp: -0.4, span: 2.4, a: 0.30 },
      { rad: R * 1.16, w: 1,   sp: 0.6,  span: 0.8, a: 0.22 },
    ];
    rings.forEach((rg, i) => {
      ctx.strokeStyle = C(rg.a + energy * 0.15);
      ctx.lineWidth = rg.w;
      ctx.beginPath();
      ctx.arc(cx, cy, rg.rad, t * rg.sp, t * rg.sp + rg.span);
      ctx.stroke();
      // ponto na ponta do arco
      const ex = cx + Math.cos(t * rg.sp + rg.span) * rg.rad;
      const ey = cy + Math.sin(t * rg.sp + rg.span) * rg.rad;
      ctx.fillStyle = C(0.6);
      ctx.beginPath(); ctx.arc(ex, ey, 2, 0, Math.PI * 2); ctx.fill();
    });

    // anel pontilhado (tracejado) girando
    ctx.save();
    ctx.translate(cx, cy);
    ctx.rotate(-t * 0.3);
    ctx.strokeStyle = C(0.12);
    ctx.setLineDash([2, 8]);
    ctx.lineWidth = 1;
    ctx.beginPath(); ctx.arc(0, 0, R * 1.72, 0, Math.PI * 2); ctx.stroke();
    ctx.setLineDash([]);
    ctx.restore();

    // onda de Ã¡udio circular (waveform reativa)
    const N = this.wave.length;
    for (let i = 0; i < N; i++) {
      const target = active || this._level > 0.02
        ? (Math.sin(t * 6 + i * 0.5) * 0.5 + 0.5) * (0.3 + this._level * 2 + this.flash)
        : 0;
      this.wave[i] += (target - this.wave[i]) * 0.25;
    }
    ctx.beginPath();
    for (let i = 0; i <= N; i++) {
      const idx = i % N;
      const ang = (i / N) * Math.PI * 2 - Math.PI / 2;
      const amp = R * 0.9 + this.wave[idx] * R * 0.45;
      const x = cx + Math.cos(ang) * amp;
      const y = cy + Math.sin(ang) * amp;
      i === 0 ? ctx.moveTo(x, y) : ctx.lineTo(x, y);
    }
    ctx.closePath();
    ctx.strokeStyle = C(0.5 + energy * 0.3);
    ctx.lineWidth = 1.5;
    ctx.stroke();

    // nÃºcleo pulsante
    const corePulse = 1 + Math.sin(t * 2) * 0.06 + this._level * 0.5 + this.flash * 0.3;
    const coreR = R * 0.5 * corePulse;
    const core = ctx.createRadialGradient(cx, cy, 0, cx, cy, coreR);
    core.addColorStop(0,   C(0.55 + energy * 0.3));
    core.addColorStop(0.5, C(0.18));
    core.addColorStop(1,   C(0));
    ctx.fillStyle = core;
    ctx.beginPath(); ctx.arc(cx, cy, coreR, 0, Math.PI * 2); ctx.fill();

    // brilho central sÃ³lido
    ctx.fillStyle = C(0.85);
    ctx.beginPath(); ctx.arc(cx, cy, R * 0.06 * corePulse, 0, Math.PI * 2); ctx.fill();

    // partÃ­culas orbitando
    const pc = active ? 5 : 3;
    for (let i = 0; i < pc; i++) {
      const a = t * (0.5 + i * 0.2) + i * 2.4;
      const rr = R * (1.0 + 0.25 * Math.sin(t + i));
      const px = cx + Math.cos(a) * rr;
      const py = cy + Math.sin(a) * rr;
      ctx.fillStyle = C(0.7);
      ctx.beginPath(); ctx.arc(px, py, 1.6, 0, Math.PI * 2); ctx.fill();
    }

    requestAnimationFrame(() => this._loop());
  }
}

const _orbCanvas = document.getElementById("orb-canvas");
const neuralBrain = _orbCanvas ? new VoiceOrb(_orbCanvas) : null;

// â”€â”€â”€ Voice Engine â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€

class VoiceEngine {
  constructor() {
    const SR = window.SpeechRecognition || window.webkitSpeechRecognition;
    this.supported   = !!SR;
    this.recording   = false;
    this.autoMode    = false;
    this._manualStop = false;

    if (!this.supported) {
      if (micBtn) { micBtn.title = "Voz nÃ£o suportada neste navegador"; micBtn.style.opacity = "0.35"; }
      const autoBtn = document.getElementById("voice-auto-btn");
      if (autoBtn) { autoBtn.title = "Voz nÃ£o suportada"; autoBtn.style.opacity = "0.35"; }
      return;
    }

    this._buildRec(SR);
  }

  _buildRec(SR) {
    this.rec = new (SR || window.SpeechRecognition || window.webkitSpeechRecognition)();
    this.rec.lang = "pt-BR";
    this.rec.continuous = true;
    this.rec.interimResults = true;

    this.rec.onstart = () => {
      this.recording = true;
      micBtn?.classList.add("recording");
      voiceStatus?.classList.add("active");
    };

    this.rec.onresult = (e) => {
      let interim = "";
      let final   = "";
      for (const result of e.results) {
        if (result.isFinal) final   += result[0].transcript;
        else                 interim += result[0].transcript;
      }
      const display = this._stripWake(final || interim);
      inputEl.value = display;
      if (voiceInterim) voiceInterim.textContent = display;

      // Auto-envia assim que hÃ¡ resultado final â€” sem precisar clicar
      if (final.trim() && !sendBtn.disabled) {
        const toSend = this._stripWake(final.trim());
        if (toSend) {
          inputEl.value = "";
          if (voiceInterim) voiceInterim.textContent = "";
          sendVoiceMessage(toSend);
        }
      }
    };

    this.rec.onend = () => {
      this.recording = false;
      micBtn?.classList.remove("recording");
      if (!this.autoMode) voiceStatus?.classList.remove("active");
      if (voiceInterim) voiceInterim.textContent = "";

      const text = inputEl.value.trim();
      if (text && !sendBtn.disabled) {
        inputEl.value = "";
        sendVoiceMessage(text);
      }

      // Reinicia automaticamente se modo sempre-ativa estiver ligado
      if (this.autoMode && !this._manualStop) {
        setTimeout(() => {
          if (this.autoMode) {
            this._buildRec();
            try { this.rec.start(); } catch (e) {}
          }
        }, 500);
      }
    };

    this.rec.onerror = (e) => {
      this.recording = false;
      micBtn?.classList.remove("recording");
      voiceStatus?.classList.remove("active");
      if (e.error !== "no-speech" && e.error !== "aborted") {
        console.warn("Voice error:", e.error);
      }
    };
  }

  _stripWake(text) {
    return text.replace(/^(jamba|jambo|jambÃ¡|javis|jarvis|j[aÃ¡]vis)[,.]?\s*/i, "").trim() || text;
  }

  toggle() {
    if (!this.supported) return;
    if (this.recording) {
      this._manualStop = true;
      this.rec.stop();
    } else {
      this._manualStop = false;
      this._buildRec();
      try { this.rec.start(); } catch (e) { console.warn("Mic start failed:", e); }
    }
  }

  toggleAutoMode() {
    if (!this.supported) return;
    this.autoMode = !this.autoMode;
    const btn = document.getElementById("voice-auto-btn");
    if (this.autoMode) {
      this._manualStop = false;
      btn?.classList.add("active");
      voiceStatus?.classList.add("active");
      if (!this.recording) {
        this._buildRec();
        try { this.rec.start(); } catch (e) { console.warn("Auto voice start failed:", e); }
      }
    } else {
      this._manualStop = true;
      btn?.classList.remove("active");
      voiceStatus?.classList.remove("active");
      if (this.recording) this.rec.stop();
    }
  }
}

// â”€â”€â”€ Whisper Recorder (push-to-talk via Whisper API) â”€â”€â”€â”€â”€
class WhisperRecorder {
  constructor() {
    this.recording   = false;
    this.mediaRec    = null;
    this.chunks      = [];
    this.stream      = null;
  }

  async toggle() {
    if (this.recording) {
      this._stop();
    } else {
      await this._start();
    }
  }

  async _start() {
    try {
      this.stream  = await navigator.mediaDevices.getUserMedia({ audio: true });
      this.chunks  = [];
      this.mediaRec = new MediaRecorder(this.stream);
      this.mediaRec.ondataavailable = (e) => { if (e.data.size > 0) this.chunks.push(e.data); };
      this.mediaRec.start(150);
      this.recording = true;
      micBtn?.classList.add("recording");
      voiceStatus?.classList.add("active");
      if (voiceInterim) voiceInterim.textContent = "gravando...";
    } catch (e) {
      console.warn("Mic error:", e);
    }
  }

  _stop() {
    if (!this.recording || !this.mediaRec) return;
    this.recording = false;
    micBtn?.classList.remove("recording");
    if (voiceInterim) voiceInterim.textContent = "transcrevendo...";

    this.mediaRec.onstop = async () => {
      const blob = new Blob(this.chunks, { type: this.mediaRec.mimeType || "audio/webm" });
      this.stream?.getTracks().forEach(t => t.stop());
      voiceStatus?.classList.remove("active");
      if (blob.size < 500) { if (voiceInterim) voiceInterim.textContent = ""; return; }
      try {
        const fd  = new FormData();
        fd.append("file", blob, "audio.webm");
        const res = await fetch(`${API}/transcribe`, { method: "POST", body: fd });
        if (res.ok) {
          const data = await res.json();
          const text = data.text?.trim();
          if (text && !sendBtn.disabled) sendVoiceMessage(text);
        }
      } catch (err) { console.warn("Whisper error:", err); }
      if (voiceInterim) voiceInterim.textContent = "";
    };
    this.mediaRec.stop();
  }
}

// â”€â”€â”€ Auto Whisper Engine (always-on com VAD calibrado) â”€â”€â”€
class AutoWhisperEngine {
  constructor() {
    this.active      = false;
    this.recording   = false;
    this.stream      = null;
    this.mediaRec    = null;
    this.chunks      = [];
    this.analyser    = null;
    this.silTimer    = null;
    this.recStart    = 0;
    this.THRESHOLD   = 0.035;   // calibrado dinamicamente no enable()
    this.SILENCE_MS  = 2200;
    this.MIN_REC_MS  = 800;     // ignora gravaÃ§Ãµes muito curtas (ruÃ­do pontual)
    this.MIN_BLOB    = 4000;    // bytes mÃ­nimos para enviar ao Whisper
  }

  async enable() {
    this.active = true;
    this.stream = await navigator.mediaDevices.getUserMedia({ audio: true });
    const ctx   = new AudioContext();
    const src   = ctx.createMediaStreamSource(this.stream);
    this.analyser = ctx.createAnalyser();
    this.analyser.fftSize = 1024;
    src.connect(this.analyser);

    // CalibraÃ§Ã£o: mede ruÃ­do ambiente por 1.2s e define threshold dinÃ¢mico
    if (voiceInterim) voiceInterim.textContent = "calibrando microfone...";
    const noiseLevel = await this._measureNoise(1200);
    this.THRESHOLD = Math.max(0.030, noiseLevel * 3.5);
    if (voiceInterim) voiceInterim.textContent = `pronto (noise=${noiseLevel.toFixed(4)})`;
    setTimeout(() => {
      if (this.active && voiceInterim) voiceInterim.textContent = "aguardando voz...";
    }, 1200);

    this._loop();
    voiceStatus?.classList.add("active");
    neuralBrain?.setState("listening");
  }

  disable() {
    this.active = false;
    clearTimeout(this.silTimer);
    if (this.recording && this.mediaRec?.state === "recording") this.mediaRec.stop();
    this.stream?.getTracks().forEach(t => t.stop());
    voiceStatus?.classList.remove("active");
    if (voiceInterim) voiceInterim.textContent = "";
    neuralBrain?.setLevel(0);
    if (!sendBtn.disabled) neuralBrain?.setState("idle");
  }

  async _measureNoise(durationMs) {
    return new Promise(resolve => {
      const samples = [];
      const interval = setInterval(() => {
        const buf = new Float32Array(this.analyser.fftSize);
        this.analyser.getFloatTimeDomainData(buf);
        samples.push(Math.sqrt(buf.reduce((s, v) => s + v * v, 0) / buf.length));
      }, 50);
      setTimeout(() => {
        clearInterval(interval);
        const avg = samples.reduce((a, b) => a + b, 0) / (samples.length || 1);
        resolve(avg);
      }, durationMs);
    });
  }

  _loop() {
    if (!this.active) return;
    const buf = new Float32Array(this.analyser.fftSize);
    this.analyser.getFloatTimeDomainData(buf);
    const vol = Math.sqrt(buf.reduce((s, v) => s + v * v, 0) / buf.length);

    // Alimenta o orbe com o nÃ­vel de voz â€” reage em tempo real
    neuralBrain?.setLevel(Math.min(1, vol * 12));

    if (vol > this.THRESHOLD && !this.recording) {
      this._startSeg();
    } else if (vol <= this.THRESHOLD && this.recording && !this.silTimer) {
      this.silTimer = setTimeout(() => this._stopSeg(), this.SILENCE_MS);
    } else if (vol > this.THRESHOLD && this.silTimer) {
      clearTimeout(this.silTimer); this.silTimer = null;
    }
    requestAnimationFrame(() => this._loop());
  }

  _startSeg() {
    this.recording = true;
    this.chunks    = [];
    this.recStart  = Date.now();
    this.mediaRec  = new MediaRecorder(this.stream);
    this.mediaRec.ondataavailable = (e) => { if (e.data.size > 0) this.chunks.push(e.data); };
    this.mediaRec.start(100);
    if (voiceInterim) voiceInterim.textContent = "ouvindo...";
  }

  _stopSeg() {
    this.recording = false;
    this.silTimer  = null;
    if (!this.mediaRec || this.mediaRec.state !== "recording") return;

    const elapsed = Date.now() - this.recStart;

    this.mediaRec.onstop = async () => {
      const blob = new Blob(this.chunks, { type: this.mediaRec.mimeType || "audio/webm" });

      // Descarta ruÃ­do curto ou blob muito pequeno
      if (elapsed < this.MIN_REC_MS || blob.size < this.MIN_BLOB) {
        if (this.active && voiceInterim) voiceInterim.textContent = "aguardando voz...";
        return;
      }

      if (voiceInterim) voiceInterim.textContent = "transcrevendo...";
      try {
        const fd  = new FormData();
        fd.append("file", blob, "audio.webm");
        const res = await fetch(`${API}/transcribe`, { method: "POST", body: fd });
        if (res.ok) {
          const data = await res.json();
          const text = data.text?.trim();
          // Filtra respostas de ruÃ­do (Whisper Ã s vezes retorna "Obrigado." ou "..." para silÃªncio)
          const WHISPER_NOISE = /^(obrigado\.?|thanks\.?|thank 
you\.?|\.{2,}|mÃºsica\.?|music\.?|legendas?\.?|subtitles?\.?|silence\.?|silÃªncio\.?|som de 
fundo\.?|background\.?|applause\.?|aplauso\.?|ruÃdo\.?|noise\.?|beep\.?|bip\.?)$/i;
          const isNoise = !text
            || text.length < 4
            || WHISPER_NOISE.test(text.trim())
            || (text.length < 8 && /^[.,!?â€¦\s]+$/.test(text));
          if (!isNoise && !sendBtn.disabled) sendVoiceMessage(text);
        }
      } catch (err) { console.warn("Auto whisper error:", err); }
      if (this.active && voiceInterim) voiceInterim.textContent = "aguardando voz...";
    };
    this.mediaRec.stop();
  }
}

// â”€â”€â”€ Wake Word Engine â€” "Jamba" ativa por voz (hands-free) â”€â”€â”€
class WakeWordEngine {
  constructor() {
    const SR = window.SpeechRecognition || window.webkitSpeechRecognition;
    this.supported = !!SR;
    this.SR = SR;
    this.active = false;
    this.awoken = false;
    this.awokenTimer = null;
    this.rec = null;
    this.wakeRe = /\b(jamba|jamb[aÃ¡]|jambo|jambar|jarvis|j[aÃ¡]vis)\b/i;
  }

  toggle() {
    if (!this.supported) {
      if (voiceInterim) voiceInterim.textContent = "wake word precisa do Chrome/Edge";
      return;
    }
    this.active ? this.disable() : this.enable();
  }

  enable() {
    // mutuamente exclusivo com o modo always-on (ðŸ‘‚) â€” ambos usam o microfone
    if (typeof autoWhi !== "undefined" && autoWhi?.active) {
      autoWhi.disable();
      document.getElementById("voice-auto-btn")?.classList.remove("active");
    }
    this.active = true;
    document.getElementById("wake-btn")?.classList.add("active");
    voiceStatus?.classList.add("active");
    if (voiceInterim) voiceInterim.textContent = 'diga "Jamba" para ativar...';
    neuralBrain?.setState("listening");
    this._build();
    this._start();
  }

  disable() {
    this.active = false;
    clearTimeout(this.awokenTimer);
    this.awoken = false;
    document.getElementById("wake-btn")?.classList.remove("active");
    voiceStatus?.classList.remove("active");
    if (voiceInterim) voiceInterim.textContent = "";
    if (!sendBtn.disabled) neuralBrain?.setState("idle");
    try { this.rec?.stop(); } catch (e) {}
  }

  _build() {
    this.rec = new this.SR();
    this.rec.lang = "pt-BR";
    this.rec.continuous = true;
    this.rec.interimResults = true;
    this.rec.onresult = (e) => this._onResult(e);
    this.rec.onend = () => { if (this.active) { try { this.rec.start(); } catch (e) {} } };
    this.rec.onerror = (e) => { if (e.error === "not-allowed") this.disable(); };
  }

  _start() { try { this.rec.start(); } catch (e) {} }

  _onResult(e) {
    // sÃ³ o que foi finalizado agora (resultIndex evita reprocessar tudo)
    let finalText = "";
    for (let i = e.resultIndex; i < e.results.length; i++) {
      if (e.results[i].isFinal) finalText += e.results[i][0].transcript;
    }
    finalText = finalText.trim();
    if (!finalText) return;

    const lower = finalText.toLowerCase();
    const m = lower.match(this.wakeRe);

    if (m) {
      neuralBrain?.setState("listening");
      neuralBrain?.fire?.();
      const idx = lower.indexOf(m[0]) + m[0].length;
      const after = finalText.slice(idx).replace(/^[,.\s]+/, "").trim();
      if (after) {
        this._send(after);                 // "Jamba, toca jazz" â†’ envia "toca jazz"
      } else {
        this.awoken = true;                // sÃ³ "Jamba" â†’ janela de 6s pro comando
        if (voiceInterim) voiceInterim.textContent = "pode falar, senhor...";
        clearTimeout(this.awokenTimer);
        this.awokenTimer = setTimeout(() => {
          this.awoken = false;
          if (this.active && voiceInterim) voiceInterim.textContent = 'diga "Jamba" para ativar...';
        }, 6000);
      }
    } else if (this.awoken) {
      this.awoken = false;
      clearTimeout(this.awokenTimer);
      this._send(finalText);               // comando logo apÃ³s o "Jamba"
    }
  }

  _send(cmd) {
    if (!cmd || sendBtn.disabled) return;
    if (voiceInterim) voiceInterim.textContent = "";
    sendVoiceMessage(cmd);
    if (this.active && voiceInterim) {
      setTimeout(() => {
        if (this.active && !sendBtn.disabled && voiceInterim)
          voiceInterim.textContent = 'diga "Jamba" para ativar...';
      }, 1500);
    }
  }
}

const whisper   = new WhisperRecorder();
const autoWhi   = new AutoWhisperEngine();
const voice     = new VoiceEngine();
const wakeWord  = new WakeWordEngine();

micBtn?.addEventListener("click", () => whisper.toggle());
document.getElementById("wake-btn")?.addEventListener("click", () => wakeWord.toggle());

document.getElementById("voice-auto-btn")?.addEventListener("click", async () => {
  const btn = document.getElementById("voice-auto-btn");
  if (autoWhi.active) {
    autoWhi.disable();
    btn?.classList.remove("active");
  } else {
    // mutuamente exclusivo com o wake word (ðŸŽ™ï¸) â€” ambos usam o microfone
    if (typeof wakeWord !== "undefined" && wakeWord?.active) wakeWord.disable();
    try {
      await autoWhi.enable();
      btn?.classList.add("active");
    } catch (e) {
      console.warn("Auto whisper enable failed:", e);
      voice.toggleAutoMode();
    }
  }
});

// â”€â”€â”€ Send via voice 
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€

async function sendVoiceMessage(text) {
  removeWelcome();
  appendMsg("user", `ðŸŽ¤ ${esc(text)}`);
  setLoading(true);
  clearAgentActivity();

  // Para Ã¡udio anterior
  if (_currentAudio) { _currentAudio.pause(); _currentAudio = null; }
  _audioQueue.length = 0;
  _audioPlaying = false;

  const typing = appendTyping("processando voz...");
  let fullText = "";
  let msgDiv = null;

  try {
    const res = await fetch(`${API}/voice/stream`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        transcript:   text,
        model:        modelSel?.value || "",
        use_conclave: useConclave?.checked || false,
        tts:          useTts?.checked ?? true,
      }),
    });

    typing.remove();

    if (!res.ok) {
      const err = await res.json().catch(() => ({}));
      appendMsg("error", `âš ï¸ Erro ${res.status}: ${esc(err.error || res.statusText)}`);
      return;
    }

    const reader = res.body.getReader();
    const dec = new TextDecoder();
    let buf = "";

    while (true) {
      const { done, value } = await reader.read();
      if (done) break;
      buf += dec.decode(value, { stream: true });
      const lines = buf.split("\n");
      buf = lines.pop();

      for (const line of lines) {
        if (!line.startsWith("data: ")) continue;
        const raw = line.slice(6).trim();
        if (raw === "[DONE]") break;
        let evt;
        try { evt = JSON.parse(raw); } catch (_) { continue; }

        if (evt.type === "audio") {
          enqueueAudio(evt.b64);
          if (evt.sentence) fullText += (fullText ? " " : "") + evt.sentence;
        } else if (evt.type === "tts_text") {
          speak(evt.text);
          fullText = evt.text;
        } else if (evt.type === "token") {
          fullText += evt.text;
        } else if (evt.type === "meta" && evt.text) {
          fullText = evt.text;
        } else if (evt.type === "done") {
          if (evt.text) fullText = evt.text;
          if (!msgDiv) {
            msgDiv = appendMsg("assistant", renderMarkdown(fullText),
                               { intent: evt.intent, brain: evt.brain, ms: evt.ms });
          }
          setBrain(evt.brain || "main");
          _msgCount++;
          if (statCount)  statCount.textContent  = _msgCount;
          if (sysCountEl) sysCountEl.textContent = _msgCount;
          if (evt.ms != null && sysMsEl) sysMsEl.textContent = evt.ms + "ms";
          if (evt.intent && sysIntentEl) sysIntentEl.textContent = evt.intent;
        }
      }
    }

    if (!msgDiv && fullText) {
      appendMsg("assistant", renderMarkdown(fullText));
    }

  } catch (err) {
    typing?.remove();
    if (err.name === "TypeError") {
      appendMsg("error", "âš ï¸ Servidor offline.");
    } else {
      appendMsg("error", `âš ï¸ ${esc(err.message)}`);
    }
  } finally {
    setLoading(false);
  }
}

let _currentAudio = null;

// Fila de Ã¡udio â€” reproduz chunks em ordem sem gaps
const _audioQueue = [];
let _audioPlaying = false;

function enqueueAudio(b64) {
  const bytes = atob(b64);
  const arr = new Uint8Array(bytes.length);
  for (let i = 0; i < bytes.length; i++) arr[i] = bytes.charCodeAt(i);
  const blob = new Blob([arr], { type: "audio/mpeg" });
  _audioQueue.push(URL.createObjectURL(blob));
  _drainAudioQueue();
}

function _drainAudioQueue() {
  if (_audioPlaying || _audioQueue.length === 0) return;
  _audioPlaying = true;
  const url = _audioQueue.shift();
  _currentAudio = new Audio(url);
  neuralBrain?.setState("speaking");
  _currentAudio.onended = () => {
    URL.revokeObjectURL(url);
    _currentAudio = null;
    _audioPlaying = false;
    if (_audioQueue.length > 0) {
      _drainAudioQueue();
    } else {
      if (!sendBtn?.disabled) neuralBrain?.setState("idle");
    }
  };
  _currentAudio.play().catch(() => { _audioPlaying = false; });
}

function cleanForSpeech(text) {
  return String(text || "")
    .replace(/```[\s\S]*?```/g, " ")              // blocos de cÃ³digo
    .replace(/`[^`]*`/g, " ")                      // cÃ³digo inline
    .replace(/\[[^\]]*\]\([^)]*\)/g, "")           // links markdown [txt](url)
    .replace(/https?:\/\/\S+/g, "")                // URLs cruas
    .replace(/\[[^\]]*\.(md|txt|py|js|json)\]/gi, "") // marcadores de arquivo do RAG
    .replace(/<[^>]+>/g, "")                        // HTML
    .replace(/[*_#>`~|]/g, "")                      // sÃ­mbolos markdown
    .replace(/^\s*\d+[\.\)]\s*/gm, "")             // "1." "2)" de listas
    .replace(/^\s*[-â€¢]\s*/gm, "")                   // bullets
    .replace(/[\u{1F000}-\u{1FAFF}\u{2600}-\u{27BF}\u{2190}-\u{21FF}\u{2B00}-\u{2BFF}]/gu, "") // emojis/setas
    .replace(/\s+/g, " ")                           // espaÃ§os/quebras mÃºltiplas
    .trim()
    .substring(0, 600);
}

async function speak(text) {
  if (!text) return;
  const clean = cleanForSpeech(text);
  if (!clean) return;

  // Para Ã¡udio anterior se ainda estiver tocando
  if (_currentAudio) { _currentAudio.pause(); _currentAudio = null; }
  if (window.speechSynthesis) window.speechSynthesis.cancel();

  try {
    const res = await fetch(`${API}/tts`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ text: clean }),
    });
    if (res.ok) {
      const blob = await res.blob();
      const url  = URL.createObjectURL(blob);
      _currentAudio = new Audio(url);
      _currentAudio.onended = () => { URL.revokeObjectURL(url); _currentAudio = null; };
      _currentAudio.play();
      return;
    }
  } catch (_) {}

  // Fallback â€” Web Speech API (voz do navegador)
  if (!window.speechSynthesis) return;
  const utt   = new SpeechSynthesisUtterance(clean);
  utt.lang    = "pt-BR";
  utt.rate    = 1.0;
  utt.pitch   = 1.0;

  neuralBrain?.setState("speaking");
  utt.onboundary = (e) => {
    const intensity = Math.min(1, (e.charLength || 4) / 12);
    neuralBrain?.setLevel(0.3 + intensity * 0.7);
  };
  utt.onend = () => {
    neuralBrain?.setLevel(0);
    if (!sendBtn?.disabled) neuralBrain?.setState("idle");
  };

  window.speechSynthesis.speak(utt);
}

/* â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
   JAVIS AIOS â€” View Router & Multi-View Logic
â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â• */

const VIEW_TITLES = { chat:'Orquestrador', agents:'Agentes', workflows:'Workflows', room:'Sala dos Agentes', 
projects:'Projetos', train:'SDR Academy', integrations:'IntegraÃ§Ãµes' };

function switchView(viewId) {
  document.querySelectorAll('.view').forEach(v => v.classList.remove('active'));
  document.querySelectorAll('.sb-item[data-view]').forEach(b => b.classList.remove('active'));
  const view = document.getElementById('view-' + viewId);
  if (view) view.classList.add('active');
  const btn = document.querySelector(`.sb-item[data-view="${viewId}"]`);
  if (btn) btn.classList.add('active');
  const titleEl = document.getElementById('topbar-view-title');
  if (titleEl) titleEl.textContent = VIEW_TITLES[viewId] || viewId;
  if (viewId === 'agents' && !document.getElementById('gallery-grid').children.length) renderAgentGallery();
  if (viewId === 'workflows') initWorkflowCanvas();
  if (viewId === 'integrations' && !document.getElementById('integrations-grid').children.length) renderIntegrations();
  if (viewId === 'room') initAgentRoom(); else stopAgentRoom();
  if (viewId === 'projects' && !document.getElementById('projects-grid').children.length) renderProjects();
}

document.querySelectorAll('.sb-item[data-view]').forEach(btn => {
  btn.addEventListener('click', () => switchView(btn.dataset.view));
});

/* â”€â”€â”€ AGENT DATA â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”
€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
const GALLERY_AGENTS = [
  { id:'orion',   name:'Orion',   role:'@master',     emoji:'ðŸ§¬', squad:'ops',      status:'online',  
desc:'Orquestra todos os agentes e decisÃµes estratÃ©gicas do sistema.', 
skills:['OrquestraÃ§Ã£o','DecisÃ£o','PriorizaÃ§Ã£o'], ints:['slack','gmail','notion'] },
  { id:'titan',   name:'Titan',   role:'@cro',        emoji:'ðŸ‘‘', squad:'ops',      status:'online',  
desc:'Orquestra todo o time de vendas, garantindo alinhamento e resultados.', skills:['LideranÃ§a','Vendas','KPIs'], 
ints:['slack','gmail','notion'] },
  { id:'khan',    name:'Khan',    role:'@bdr-global', emoji:'ðŸ“ž', squad:'vendas',   status:'online',  desc:'Gerencia 
outbound global, prospecÃ§Ã£o e qualificaÃ§Ã£o de leads.', skills:['ProspecÃ§Ã£o','Cold Call','QualificaÃ§Ã£o'], 
ints:['whatsapp','gmail','sheets'] },
  { id:'phantom', name:'Phantom', role:'@closer',     emoji:'ðŸŽ¯', squad:'vendas',   status:'online',  desc:'Conduz 
negociaÃ§Ãµes e fecha contratos de alto valor.', skills:['Fechamento','NegociaÃ§Ã£o','Follow-up'], 
ints:['whatsapp','gmail'] },
  { id:'blade',   name:'Blade',   role:'@bdr-en',     emoji:'âš”ï¸', squad:'vendas',   status:'online',  
desc:'ProspecÃ§Ã£o em inglÃªs para mercados internacionais.', skills:['Outbound EN','LinkedIn','Email'], 
ints:['gmail','linkedin'] },
  { id:'vera',    name:'Vera',    role:'@cmo',        emoji:'ðŸ“¢', squad:'criativo', status:'idle',    
desc:'Impulsiona presenÃ§a da marca, estratÃ©gias e campanhas criativas.', 
skills:['Marketing','ConteÃºdo','Branding'], ints:['instagram','gmail'] },
  { id:'dara',    name:'Dara',    role:'@data-eng',   emoji:'ðŸ“Š', squad:'dados',    status:'idle',    desc:'Monitora 
mÃ©tricas, analisa dados de CRM e alimenta dashboards.', skills:['Analytics','CRM','RelatÃ³rios'], 
ints:['sheets','notion','slack'] },
  { id:'intel',   name:'Intel',   role:'@call-intel', emoji:'ðŸ”', squad:'dados',    status:'offline', desc:'Pesquisa 
mercado, concorrentes e oportunidades estratÃ©gicas.', skills:['Pesquisa','InteligÃªncia','Insights'], 
ints:['notion','gmail'] },
  { id:'prism',   name:'Prism',   role:'@insight',    emoji:'ðŸ’Ž', squad:'dados',    status:'idle',    
desc:'Transforma dados brutos em insights acionÃ¡veis e relatÃ³rios.', 
skills:['Insights','VisualizaÃ§Ã£o','Reportes'], ints:['sheets','notion'] },
  { id:'recap',   name:'Recap',   role:'@followup',   emoji:'ðŸ”„', squad:'ops',      status:'online',  desc:'Registra 
e acompanha follow-ups de todas as interaÃ§Ãµes de vendas.', skills:['Follow-up','CRM','Lembretes'], 
ints:['whatsapp','gmail','notion'] },
  { id:'brief',   name:'Brief',   role:'@discovery',  emoji:'ðŸ“‹', squad:'vendas',   status:'idle',    desc:'Conduz 
descoberta e prepara briefings de prospects qualificados.', skills:['Discovery','Briefing','QualificaÃ§Ã£o'], 
ints:['notion','gmail'] },
  { id:'ana',     name:'Ana',     role:'@analyst',    emoji:'ðŸ“ˆ', squad:'dados',    status:'idle',    desc:'Analisa 
execuÃ§Ã£o e gera relatÃ³rios de performance de campanhas.', skills:['AnÃ¡lise','Performance','Reportes'], 
ints:['sheets','notion'] },
];

const INT_ICONS = { gmail:'ðŸ“§', whatsapp:'ðŸ’¬', slack:'ðŸ’¼', notion:'ðŸ““', sheets:'ðŸ“Š', instagram:'ðŸ“¸', 
linkedin:'ðŸ”—' };

/* â”€â”€â”€ AGENT GALLERY â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”
€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
let _activeFilter = 'all';

function renderAgentGallery(filter) {
  filter = filter || _activeFilter;
  _activeFilter = filter;
  const grid = document.getElementById('gallery-grid');
  if (!grid) return;
  const list = filter === 'all' ? GALLERY_AGENTS : GALLERY_AGENTS.filter(a => a.squad === filter);
  grid.innerHTML = list.map(a => `
    <div class="gallery-card" onclick="openAgentChat('${a.id}')">
      <div class="gc-header">
        <div class="gc-avatar">${a.emoji}</div>
        <div class="gc-info"><div class="gc-name">${a.name}</div><div class="gc-role">${a.role}</div></div>
        <div class="gc-status-dot ${a.status}"></div>
      </div>
      <div class="gc-desc">${a.desc}</div>
      <div class="gc-skills">${a.skills.map(s=>`<span class="gc-skill">${s}</span>`).join('')}</div>
      <div class="gc-integrations">${a.ints.map(i=>`<div class="gc-int-icon" 
title="${i}">${INT_ICONS[i]||'ðŸ”Œ'}</div>`).join('')}</div>
    </div>
  `).join('');
  renderActivityFeed();
}

function openAgentChat(agentId) {
  const a = GALLERY_AGENTS.find(x => x.id === agentId);
  if (!a) return;
  switchView('chat');
  const inp = document.getElementById('input');
  if (inp) { inp.value = `Falar com ${a.name} (${a.role}): `; inp.focus(); }
}

document.addEventListener('click', e => {
  const btn = e.target.closest('.filter-btn');
  if (!btn) return;
  document.querySelectorAll('.filter-btn').forEach(b => b.classList.remove('active'));
  btn.classList.add('active');
  renderAgentGallery(btn.dataset.filter);
});

function renderActivityFeed() {
  const feed = document.getElementById('activity-feed');
  if (!feed) return;
  const items = [
    { agent:'Khan @bdr',      text:'Prospectou 3 novos leads via LinkedIn',        time:'2m atrÃ¡s' },
    { agent:'Orion @master',  text:'Delegou campanha Q2 para Vera e Dara',         time:'8m atrÃ¡s' },
    { agent:'Phantom @closer',text:'Follow-up enviado para MedTech S.A.',           time:'15m atrÃ¡s' },
    { agent:'Dara @data',     text:'Dashboard de CRM atualizado com 47 entradas',  time:'22m atrÃ¡s' },
    { agent:'Vera @cmo',      text:'Reel gerado: "5 razÃµes para escolher..."',     time:'1h atrÃ¡s' },
  ];
  feed.innerHTML = items.map(i => `
    <div class="activity-item">
      <div class="ai-agent">${i.agent}</div>
      <div class="ai-text">${i.text}</div>
      <div class="ai-time">${i.time}</div>
    </div>
  `).join('');
}

/* â”€â”€â”€ WORKFLOW CANVAS 
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
const MISSIONS = [
  { id:'m1', name:'Campanha LanÃ§amento Q1', pct:89, active:true },
  { id:'m2', name:'SDR Outbound EN',        pct:62, active:false },
  { id:'m3', name:'ConteÃºdo Julho',         pct:34, active:false },
];

const WF_NODES = [
  { id:'n0', label:'Orion',     type:'orchestrator', x:60,  y:150, status:'online',  pct:100 },
  { id:'n1', label:'Headlines', type:'copy',         x:240, y:70,  status:'done',    pct:100 },
  { id:'n2', label:'Body Copy', type:'copy',         x:240, y:230, status:'running', pct:67  },
  { id:'n3', label:'House G',   type:'review',       x:430, y:150, status:'waiting', pct:0   },
  { id:'n4', label:'Publish',   type:'publish',      x:610, y:150, status:'waiting', pct:0   },
];
const WF_EDGES = [['n0','n1'],['n0','n2'],['n1','n3'],['n2','n3'],['n3','n4']];

async function fetchMissions() {
  try {
    const r = await fetch(`${API}/missions`);
    if (!r.ok) return;
    const data = await r.json();
    if (data.missions) {
      data.missions.forEach(m => {
        const existing = MISSIONS.find(x => x.id === m.id);
        if (existing) { existing.pct = m.pct; existing.active = m.active; }
        else MISSIONS.push(m);
      });
    }
  } catch { /* backend offline â€” use hardcoded */ }
}

async function initWorkflowCanvas() {
  await fetchMissions();
  const list = document.getElementById('missions-list');
  if (list) list.innerHTML = MISSIONS.map(m => `
    <div class="mission-item ${m.active?'active':''}" data-mid="${m.id}" onclick="selectMission('${m.id}')">
      <div class="mi-name">${m.name}</div>
      <div class="mi-progress-row">
        <div class="mi-bar"><div class="mi-bar-fill" style="width:${m.pct}%"></div></div>
        <span class="mi-pct">${m.pct}%</span>
      </div>
    </div>
  `).join('');

  const canvas = document.getElementById('workflow-canvas');
  if (!canvas) return;
  canvas.querySelectorAll('.wf-node').forEach(n => n.remove());
  WF_NODES.forEach(node => {
    const div = document.createElement('div');
    div.className = `wf-node type-${node.type} status-${node.status}`;
    div.id = 'wfn-' + node.id;
    div.style.cssText = `left:${node.x}px;top:${node.y}px`;
    div.innerHTML = `<div class="wf-node-label">${node.label}</div><div class="wf-node-type">${node.type}</div><div 
class="wf-node-bar"><div class="wf-node-bar-fill" style="width:${node.pct}%"></div></div>`;
    div.addEventListener('click', () => showTaskDetail(node));
    canvas.appendChild(div);
  });
  requestAnimationFrame(() => drawWfEdges());
}

function selectMission(mid) {
  MISSIONS.forEach(m => m.active = (m.id === mid));
  initWorkflowCanvas();
}

function drawWfEdges() {
  const svg = document.getElementById('wf-connections');
  const canvas = document.getElementById('workflow-canvas');
  if (!svg || !canvas) return;
  svg.innerHTML = '';
  const cr = canvas.getBoundingClientRect();
  WF_EDGES.forEach(([fId, tId]) => {
    const fEl = document.getElementById('wfn-' + fId);
    const tEl = document.getElementById('wfn-' + tId);
    if (!fEl || !tEl) return;
    const fr = fEl.getBoundingClientRect();
    const tr = tEl.getBoundingClientRect();
    const x1 = fr.right - cr.left, y1 = (fr.top+fr.bottom)/2 - cr.top;
    const x2 = tr.left  - cr.left, y2 = (tr.top+tr.bottom)/2  - cr.top;
    const mx = (x1+x2)/2;
    const path = document.createElementNS('http://www.w3.org/2000/svg','path');
    path.setAttribute('d', `M${x1},${y1} C${mx},${y1} ${mx},${y2} ${x2},${y2}`);
    path.setAttribute('stroke', 'rgba(124,58,237,0.4)');
    path.setAttribute('stroke-width', '1.5');
    path.setAttribute('fill', 'none');
    path.setAttribute('stroke-dasharray', '5 3');
    svg.appendChild(path);
  });
}

function showTaskDetail(node) {
  const panel = document.getElementById('tdp-content');
  if (!panel) return;
  const subtaskMap = {
    orchestrator: ['AnÃ¡lise de contexto','DelegaÃ§Ã£o de tasks','Monitoramento'],
    copy:         ['Pesquisa de referÃªncias','Rascunho inicial','RevisÃ£o de tom'],
    review:       ['Checklist de qualidade','AprovaÃ§Ã£o humana','Feedback loop'],
    publish:      ['Agendamento','PublicaÃ§Ã£o final','Report de alcance'],
    research:     ['Coleta de dados','AnÃ¡lise','SÃ­ntese'],
  };
  const tasks = subtaskMap[node.type] || [];
  const done = Math.floor((node.pct/100)*tasks.length);
  panel.innerHTML = `
    <div class="tdp-task-name">${node.label}</div>
    <div class="tdp-progress-big">${node.pct}%</div>
    <div class="tdp-section-label">Subtarefas</div>
    <div class="tdp-subtasks">${tasks.map((t,i)=>`<div class="tdp-subtask 
${i<done?'done':''}"><span>${i<done?'âœ…':'â—‹'}</span><span>${t}</span></div>`).join('')}</div>
    <div class="tdp-section-label" style="margin-top:12px">Contexto</div>
    <div class="tdp-context">Agente: <strong>${node.label}</strong><br>Tipo: ${node.type}<br>Status: 
${node.status}</div>
  `;
}

/* â”€â”€â”€ TRAINING VIEW 
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ 
*/
const _trainingQueue = [];

document.addEventListener('click', async e => {
  if (e.target.id !== 'train-add-btn') return;
  const urlInp = document.getElementById('train-url');
  const agSel  = document.getElementById('train-agent-sel');
  const url    = (urlInp?.value || '').trim();
  if (!url) return;
  const agent = agSel?.value || 'khan';

  const idx = _trainingQueue.length;
  _trainingQueue.push({ url, title: 'Extraindo transcriÃ§Ã£o...', agent, progress: 5, status: 'studying' });
  if (urlInp) urlInp.value = '';
  renderTrainingQueue();

  const fakeId = setInterval(() => {
    const item = _trainingQueue[idx];
    if (!item || item.progress >= 85) { clearInterval(fakeId); return; }
    item.progress = Math.min(85, item.progress + Math.random() * 5 + 2);
    const bar = document.getElementById('ti-bar-' + idx);
    if (bar) bar.style.width = item.progress + '%';
  }, 400);

  try {
    const res  = await fetch('/train/youtube', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ url, agent }),
    });
    clearInterval(fakeId);
    const data = await res.json();
    const item = _trainingQueue[idx];
    if (data.error) {
      item.title = 'âŒ ' + data.error;
      item.status = 'error';
      item.progress = 0;
    } else {
      item.title    = data.title || item.title;
      item.status   = 'done';
      item.progress = 100;
      item.chars    = data.chars;
      showToast(`âœ… ${item.agent} absorveu: ${item.title}`, 'success');
    }
    renderTrainingQueue();
  } catch (err) {
    clearInterval(fakeId);
    const item = _trainingQueue[idx];
    if (item) { item.title = 'âŒ Erro de conexÃ£o'; item.status = 'error'; item.progress = 0; }
    renderTrainingQueue();
  }
});

function renderTrainingQueue() {
  const el = document.getElementById('train-queue');
  if (!el) return;
  if (!_trainingQueue.length) { el.innerHTML='<div class="train-empty">Nenhum vÃdeo em treinamento ainda.</div>'; 
return; }
  el.innerHTML = _trainingQueue.map((item,i) => `
    <div class="train-item">
      <div class="ti-icon">ðŸŽ“</div>
      <div class="ti-info">
        <div class="ti-title">${item.title}${item.chars ? ' <small style="color:var(--text-dim);font-weight:400">Â· 
'+Math.round(item.chars/1000)+'k chars</small>' : ''}</div>
        <div class="ti-agent">Agente: ${item.agent}</div>
        <div class="ti-progress"><div class="ti-progress-fill" id="ti-bar-${i}" 
style="width:${item.progress}%"></div></div>
      </div>
      <div class="ti-status ${item.status}" id="ti-st-${i}">${item.status==='done'?'âœ… 
Absorvido':item.status==='error'?'Erro':'ðŸ“š Estudando'}</div>
    </div>
  `).join('');
}

/* â”€â”€â”€ INTEGRATIONS â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
const INTEGRATIONS_DATA = [
  { id:'gmail',     name:'Gmail',         icon:'ðŸ“§', status:'disconnected', desc:'Envio e recebimento de emails' },
  { id:'whatsapp',  name:'WhatsApp',      icon:'ðŸ’¬', status:'disconnected', desc:'Mensagens via WhatsApp Business' },
  { id:'slack',     name:'Slack',         icon:'ðŸ’¼', status:'disconnected', desc:'NotificaÃ§Ãµes e alertas de 
equipe' },
  { id:'notion',    name:'Notion',        icon:'ðŸ““', status:'disconnected', desc:'DocumentaÃ§Ã£o e conhecimento' },
  { id:'sheets',    name:'Google Sheets', icon:'ðŸ“Š', status:'disconnected', desc:'Planilhas e relatÃ³rios de dados' 
},
  { id:'instagram', name:'Instagram',     icon:'ðŸ“¸', status:'disconnected', desc:'GestÃ£o de conteÃºdo e DMs' },
  { id:'linkedin',  name:'LinkedIn',      icon:'ðŸ”—', status:'disconnected', desc:'ProspecÃ§Ã£o B2B e conexÃµes' },
  { id:'evolution', name:'Evolution API', icon:'âš¡', status:'disconnected', desc:'WhatsApp automatizado via API' },
];

/* â”€â”€â”€ TOAST SYSTEM â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
function showToast(msg, type = 'info', duration = 3500) {
  const container = document.getElementById('toast-container');
  if (!container) return;
  const t = document.createElement('div');
  t.className = `toast ${type}`;
  t.textContent = msg;
  container.appendChild(t);
  requestAnimationFrame(() => { requestAnimationFrame(() => t.classList.add('show')); });
  setTimeout(() => {
    t.classList.remove('show');
    setTimeout(() => t.remove(), 300);
  }, duration);
}

/* â”€â”€â”€ KEYBOARD SHORTCUTS 
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
document.addEventListener('keydown', e => {
  if (e.key === '/' && document.activeElement !== inputEl && !e.ctrlKey && !e.metaKey) {
    e.preventDefault();
    switchView('chat');
    inputEl && inputEl.focus();
  }
  if (e.key === 'Escape' && document.activeElement === inputEl) {
    inputEl.blur();
  }
});

function renderIntegrations() {
  const grid = document.getElementById('integrations-grid');
  if (!grid) return;
  grid.innerHTML = INTEGRATIONS_DATA.map(i => `
    <div class="integration-card ${i.status}" onclick="toggleIntegration('${i.id}')">
      <div class="ic-icon">${i.icon}</div>
      <div class="ic-name">${i.name}</div>
      <div class="ic-desc">${i.desc}</div>
      <span class="ic-status ${i.status}">${i.status==='connected'?'âœ“ Conectado':'Conectar'}</span>
    </div>
  `).join('');
}

function toggleIntegration(id) {
  const item = INTEGRATIONS_DATA.find(i => i.id===id);
  if (!item) return;
  item.status = item.status==='connected' ? 'disconnected' : 'connected';
  renderIntegrations();
}

/* â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
   SALA DOS AGENTES â€” animated room with character sprites
   â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â• */
const ROOM_AGENTS = [
  { id:'orion',   name:'Orion',   emoji:'ðŸ§¬', hx:50, hy:30 },
  { id:'titan',   name:'Titan',   emoji:'ðŸ‘‘', hx:22, hy:48 },
  { id:'khan',    name:'Khan',    emoji:'ðŸ“ž', hx:38, hy:66 },
  { id:'phantom', name:'Phantom', emoji:'ðŸŽ¯', hx:62, hy:66 },
  { id:'blade',   name:'Blade',   emoji:'âš”ï¸', hx:78, hy:48 },
  { id:'vera',    name:'Vera',    emoji:'ðŸ“¢', hx:15, hy:74 },
  { id:'dara',    name:'Dara',    emoji:'ðŸ“Š', hx:85, hy:74 },
  { id:'recap',   name:'Recap',   emoji:'ðŸ”„', hx:50, hy:82 },
];
const ROOM_TASKS = [
  'analisando lead novo', 'escrevendo follow-up', 'revisando copy', 'fechando contrato',
  'pesquisando concorrente', 'gerando relatÃ³rio', 'qualificando prospect', 'criando reel',
  'atualizando CRM', 'preparando proposta', 'estudando vÃ­deo de vendas', 'agendando reuniÃ£o',
];
let _roomTimers = [];
let _roomInited = false;

function initAgentRoom() {
  const floor = document.getElementById('room-floor');
  if (!floor) return;
  if (_roomInited) { _roomTick(); return; }
  _roomInited = true;

  // build desks + agents
  ROOM_AGENTS.forEach(a => {
    const desk = document.createElement('div');
    desk.className = 'room-desk';
    desk.style.left = a.hx + '%';
    desk.style.top  = a.hy + '%';
    floor.appendChild(desk);

    const el = document.createElement('div');
    el.className = 'room-agent';
    el.id = 'ra-' + a.id;
    el.style.left = a.hx + '%';
    el.style.top  = a.hy + '%';
    el.innerHTML = `
      <span class="room-agent-status-dot idle"></span>
      <div class="room-bubble" id="rb-${a.id}"></div>
      <div class="room-agent-body">${a.emoji}</div>
      <div class="room-agent-name">${a.name}</div>`;
    el.onclick = () => roomPoke(a);
    floor.appendChild(el);
  });

  _roomTick();
  _roomTimers.push(setInterval(_roomTick, 3200));
}

function stopAgentRoom() {
  _roomTimers.forEach(clearInterval);
  _roomTimers = [];
}

function _roomTick() {
  // randomly assign work to ~half the agents each tick
  let working = 0, idle = 0;
  ROOM_AGENTS.forEach(a => {
    const el = document.getElementById('ra-' + a.id);
    if (!el) return;
    const dot = el.querySelector('.room-agent-status-dot');
    const roll = Math.random();
    if (roll < 0.55) {
      // working
      el.classList.add('working');
      dot.className = 'room-agent-status-dot working';
      working++;
      if (Math.random() < 0.5) roomBubble(a, ROOM_TASKS[Math.floor(Math.random()*ROOM_TASKS.length)]);
    } else if (roll < 0.9) {
      // idle â€” wander a bit
      el.classList.remove('working');
      dot.className = 'room-agent-status-dot idle';
      idle++;
      if (Math.random() < 0.4) {
        el.style.left = Math.max(8, Math.min(92, a.hx + (Math.random()*16 - 8))) + '%';
        el.style.top  = Math.max(20, Math.min(86, a.hy + (Math.random()*12 - 6))) + '%';
      } else {
        el.style.left = a.hx + '%'; el.style.top = a.hy + '%';
      }
    } else {
      el.classList.remove('working');
      el.classList.add('offline');
      dot.className = 'room-agent-status-dot offline';
    }
    if (roll >= 0.55) el.classList.remove('offline');
  });
  const w = document.getElementById('rstat-working'); if (w) w.textContent = working;
  const i = document.getElementById('rstat-idle');    if (i) i.textContent = idle;
  const t = document.getElementById('rstat-tasks');   if (t) t.textContent = working;
}

function roomBubble(a, text) {
  const b = document.getElementById('rb-' + a.id);
  if (!b) return;
  b.textContent = text;
  b.classList.add('show');
  setTimeout(() => b.classList.remove('show'), 2600);
  roomLog(a.name, text);
}

function roomPoke(a) {
  roomBubble(a, 'pronto pra trabalhar, senhor!');
  const el = document.getElementById('ra-' + a.id);
  if (el) { el.classList.add('working'); el.querySelector('.room-agent-status-dot').className = 'room-agent-status-dot 
working'; }
}

function roomLog(agent, text) {
  const log = document.getElementById('room-log');
  if (!log) return;
  const now = new Date().toLocaleTimeString('pt-BR', { hour:'2-digit', minute:'2-digit', second:'2-digit' });
  const line = document.createElement('div');
  line.className = 'rl-line';
  line.innerHTML = `<span class="rl-time">${now}</span><span class="rl-agent">${agent}</span> Â· ${text}`;
  log.insertBefore(line, log.firstChild);
  while (log.children.length > 30) log.removeChild(log.lastChild);
}

/* â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
   PROJETOS â€” Javis as master orchestrator of all projects
   â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â• */
const PROJECTS_DATA = [
  {
    id:'vempassear', icon:'ðŸŒŠ', name:'Vem Passear Jampa', tag:'Turismo Â· JoÃ£o Pessoa',
    desc:'Squad de turismo: vendas de passeios, conteÃºdo, leads e atendimento no WhatsApp.',
    agents:10, status:'online', statusLabel:'Ativo', grad:'linear-gradient(90deg,#0ea5e9,#06b6d4)',
    open:'/vempassear', squadEndpoint:'/jampa/agents',
  },
  {
    id:'cerebro-jampa', icon:'ðŸ§ ', name:'CÃ©rebro Jampa', tag:'OrquestraÃ§Ã£o comercial',
    desc:'Squad mestre Jampa Jarvis: Orion orquestra Hunter, Atlas, LNS, Nero, Nova e mais.',
    agents:10, status:'online', statusLabel:'Ativo', grad:'linear-gradient(90deg,#7c3aed,#a855f7)',
    open:'/vempassear', squadEndpoint:'/jampa/agents',
  },
  {
    id:'javis-core', icon:'âš¡', name:'Javis Core', tag:'Orquestrador mestre',
    desc:'O cÃ©rebro central. Coordena todos os projetos, voz, cÃ³digo (Claude + Codex) e memÃ³ria.',
    agents:12, status:'online', statusLabel:'VocÃª estÃ¡ aqui', grad:'linear-gradient(90deg,#f59e0b,#ef4444)',
    open:'/', squadEndpoint:'/agents',
  },
  {
    id:'futuro', icon:'âž•', name:'Novo Projeto', tag:'Slot livre',
    desc:'Plugue um novo projeto aqui. Javis cria o squad e orquestra automaticamente.',
    agents:0, status:'planned', statusLabel:'Planejado', grad:'linear-gradient(90deg,#475569,#334155)',
    open:null, squadEndpoint:null,
  },
];

function renderProjects() {
  const grid = document.getElementById('projects-grid');
  if (!grid) return;
  grid.innerHTML = PROJECTS_DATA.map(p => `
    <div class="project-card" style="--accent-grad:${p.grad}">
      <div class="pc-head">
        <div class="pc-icon">${p.icon}</div>
        <div><div class="pc-name">${p.name}</div><div class="pc-tag">${p.tag}</div></div>
      </div>
      <div class="pc-desc">${p.desc}</div>
      <div class="pc-meta">
        <div class="pc-stat"><b>${p.agents}</b>agentes</div>
        <div class="pc-stat"><span class="pc-status ${p.status}">${p.status==='online'?'â—':'â—‹'} 
${p.statusLabel}</span></div>
      </div>
      <div class="pc-actions">
        <button class="pc-btn" ${p.open?`onclick="window.location.href='${p.open}'"`:'disabled'}>${p.open?'Abrir':'Em 
breve'}</button>
        <button class="pc-btn" 
${p.squadEndpoint?`onclick="orquestrarProjeto('${p.id}')"`:'disabled'}>Orquestrar</button>
      </div>
    </div>
  `).join('');
}

function orquestrarProjeto(id) {
  const p = PROJECTS_DATA.find(x => x.id===id);
  if (!p) return;
  switchView('chat');
  const inp = document.getElementById('input');
  if (inp) { inp.value = `Orquestrar projeto ${p.name}: `; inp.focus(); }
}


codex
A estrutura relevante já está clara: `renderProjects()` só popula `#projects-grid`, e `renderTrainingQueue()` tem 
retorno antecipado quando a fila está vazia. Vou inserir o painel sem trocar IDs existentes e garantir que os stats de 
treino atualizem também no estado vazio.
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -LiteralPath 
'_apps/javis-local-interface/frontend/app.js' -Pattern 'function renderTrainingQueue|function renderProjects|const 
PROJECTS_DATA|const _trainingQueue' -Context 4,25" in C:\Users\noteacer\Desktop\javis
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -LiteralPath 
'_apps/javis-local-interface/frontend/index.html' -Pattern '<div class=\"train-layout\">|<div 
class=\"train-queue\"|<div class=\"projects-grid\"' -Context 3,3" in C:\Users\noteacer\Desktop\javis
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Tail 80 -LiteralPath 
'_apps/javis-local-interface/frontend/style.css'" in C:\Users\noteacer\Desktop\javis
 succeeded in 623ms:
.ti-agent,
.ti-status,
.ic-status,
.rstat b,
.room-log,
.room-bubble,
.pc-tag,
.pc-stat b,
.pc-status,
.ai-agent,
.ai-time {
  font-family: var(--font-mono);
}

.tc-val,
.sys-row span:last-child,
.res-stats span {
  color: var(--matrix);
  text-shadow: 0 0 10px rgba(0,255,156,0.28);
}

.gallery-card,
.project-card,
.integration-card {
  transition: border-color 0.2s ease, box-shadow 0.2s ease, transform 0.2s ease, background 0.2s ease;
}

.gallery-card:hover,
.project-card:hover,
.integration-card:hover {
  border-color: var(--violet);
  transform: translateY(-2px);
  box-shadow: var(--shadow-hover), 0 0 22px rgba(124,58,237,0.16);
}

.mission-item:hover,
.wf-node:hover,
.pc-btn:hover,
.filter-btn:hover,
.qbtn:hover {
  box-shadow: 0 0 18px rgba(124,58,237,0.12);
}

.sb-item.active,
.filter-btn.active,
.mission-item.active {
  box-shadow: inset 3px 0 0 var(--violet), 0 0 20px rgba(124,58,237,0.10);
}

/* â”€â”€â”€ TOAST NOTIFICATIONS â”€â”€â”€ */
.toast-container {
  position: fixed;
  bottom: 24px;
  right: 24px;
  z-index: 9999;
  display: flex;
  flex-direction: column;
  gap: 8px;
  pointer-events: none;
}
.toast {
  background: var(--surface2);
  border: 1px solid var(--border);
  border-left: 3px solid var(--violet);
  border-radius: var(--radius);
  padding: 10px 16px;
  font-family: var(--font);
  font-size: 13px;
  color: var(--text);
  max-width: 300px;
  box-shadow: 0 4px 24px rgba(0,0,0,0.4);
  opacity: 0;
  transform: translateX(20px);
  transition: opacity 0.25s, transform 0.25s;
  pointer-events: auto;
}
.toast.show { opacity: 1; transform: translateX(0); }
.toast.success { border-left-color: var(--emerald); }
.toast.warning { border-left-color: var(--amber); }
.toast.error   { border-left-color: var(--red); }

 succeeded in 791ms:

  _apps\javis-local-interface\frontend\index.html:311:
  _apps\javis-local-interface\frontend\index.html:312:      <!-- VIEW: TREINAMENTO -->
  _apps\javis-local-interface\frontend\index.html:313:      <div class="view" id="view-train">
> _apps\javis-local-interface\frontend\index.html:314:        <div class="train-layout">
  _apps\javis-local-interface\frontend\index.html:315:          <h2 class="view-title">SDR Academy</h2>
  _apps\javis-local-interface\frontend\index.html:316:          <p class="train-subtitle">Adicione vídeos para treinar 
os agentes. O SDR vai absorver o conteúdo e aplicar nas vendas.</p>
  _apps\javis-local-interface\frontend\index.html:317:          <div class="train-agent-select">
  _apps\javis-local-interface\frontend\index.html:327:            <input type="text" id="train-url" placeholder="Cole 
o link do YouTube aqui..." />
  _apps\javis-local-interface\frontend\index.html:328:            <button id="train-add-btn">Treinar</button>
  _apps\javis-local-interface\frontend\index.html:329:          </div>
> _apps\javis-local-interface\frontend\index.html:330:          <div class="train-queue" id="train-queue">
  _apps\javis-local-interface\frontend\index.html:331:            <div class="train-empty">Nenhum vídeo em treinamento 
ainda.</div>
  _apps\javis-local-interface\frontend\index.html:332:          </div>
  _apps\javis-local-interface\frontend\index.html:333:        </div>
  _apps\javis-local-interface\frontend\index.html:377:              <p class="view-subtitle">Javis é o cérebro mestre. 
Cada projeto tem seu próprio squad.</p>
  _apps\javis-local-interface\frontend\index.html:378:            </div>
  _apps\javis-local-interface\frontend\index.html:379:          </div>
> _apps\javis-local-interface\frontend\index.html:380:          <div class="projects-grid" id="projects-grid"></div>
  _apps\javis-local-interface\frontend\index.html:381:        </div>
  _apps\javis-local-interface\frontend\index.html:382:      </div>
  _apps\javis-local-interface\frontend\index.html:383:



 succeeded in 826ms:

  _apps\javis-local-interface\frontend\app.js:1660:  `;
  _apps\javis-local-interface\frontend\app.js:1661:}
  _apps\javis-local-interface\frontend\app.js:1662:
  _apps\javis-local-interface\frontend\app.js:1663:/* ─── TRAINING VIEW ─────────────────────────────────────── */
> _apps\javis-local-interface\frontend\app.js:1664:const _trainingQueue = [];
  _apps\javis-local-interface\frontend\app.js:1665:
  _apps\javis-local-interface\frontend\app.js:1666:document.addEventListener('click', async e => {
  _apps\javis-local-interface\frontend\app.js:1667:  if (e.target.id !== 'train-add-btn') return;
  _apps\javis-local-interface\frontend\app.js:1668:  const urlInp = document.getElementById('train-url');
  _apps\javis-local-interface\frontend\app.js:1669:  const agSel  = document.getElementById('train-agent-sel');
  _apps\javis-local-interface\frontend\app.js:1670:  const url    = (urlInp?.value || '').trim();
  _apps\javis-local-interface\frontend\app.js:1671:  if (!url) return;
  _apps\javis-local-interface\frontend\app.js:1672:  const agent = agSel?.value || 'khan';
  _apps\javis-local-interface\frontend\app.js:1673:
  _apps\javis-local-interface\frontend\app.js:1674:  const idx = _trainingQueue.length;
  _apps\javis-local-interface\frontend\app.js:1675:  _trainingQueue.push({ url, title: 'Extraindo transcrição...', 
agent, progress: 5, status: 'studying' });
  _apps\javis-local-interface\frontend\app.js:1676:  if (urlInp) urlInp.value = '';
  _apps\javis-local-interface\frontend\app.js:1677:  renderTrainingQueue();
  _apps\javis-local-interface\frontend\app.js:1678:
  _apps\javis-local-interface\frontend\app.js:1679:  const fakeId = setInterval(() => {
  _apps\javis-local-interface\frontend\app.js:1680:    const item = _trainingQueue[idx];
  _apps\javis-local-interface\frontend\app.js:1681:    if (!item || item.progress >= 85) { clearInterval(fakeId); 
return; }
  _apps\javis-local-interface\frontend\app.js:1682:    item.progress = Math.min(85, item.progress + Math.random() * 5 
+ 2);
  _apps\javis-local-interface\frontend\app.js:1683:    const bar = document.getElementById('ti-bar-' + idx);
  _apps\javis-local-interface\frontend\app.js:1684:    if (bar) bar.style.width = item.progress + '%';
  _apps\javis-local-interface\frontend\app.js:1685:  }, 400);
  _apps\javis-local-interface\frontend\app.js:1686:
  _apps\javis-local-interface\frontend\app.js:1687:  try {
  _apps\javis-local-interface\frontend\app.js:1688:    const res  = await fetch('/train/youtube', {
  _apps\javis-local-interface\frontend\app.js:1689:      method: 'POST',
  _apps\javis-local-interface\frontend\app.js:1712:    renderTrainingQueue();
  _apps\javis-local-interface\frontend\app.js:1713:  }
  _apps\javis-local-interface\frontend\app.js:1714:});
  _apps\javis-local-interface\frontend\app.js:1715:
> _apps\javis-local-interface\frontend\app.js:1716:function renderTrainingQueue() {
  _apps\javis-local-interface\frontend\app.js:1717:  const el = document.getElementById('train-queue');
  _apps\javis-local-interface\frontend\app.js:1718:  if (!el) return;
  _apps\javis-local-interface\frontend\app.js:1719:  if (!_trainingQueue.length) { el.innerHTML='<div 
class="train-empty">Nenhum vídeo em treinamento ainda.</div>'; return; }
  _apps\javis-local-interface\frontend\app.js:1720:  el.innerHTML = _trainingQueue.map((item,i) => `
  _apps\javis-local-interface\frontend\app.js:1721:    <div class="train-item">
  _apps\javis-local-interface\frontend\app.js:1722:      <div class="ti-icon">🎓</div>
  _apps\javis-local-interface\frontend\app.js:1723:      <div class="ti-info">
  _apps\javis-local-interface\frontend\app.js:1724:        <div class="ti-title">${item.title}${item.chars ? ' <small 
style="color:var(--text-dim);font-weight:400">· '+Math.round(item.chars/1000)+'k chars</small>' : ''}</div>
  _apps\javis-local-interface\frontend\app.js:1725:        <div class="ti-agent">Agente: ${item.agent}</div>
  _apps\javis-local-interface\frontend\app.js:1726:        <div class="ti-progress"><div class="ti-progress-fill" 
id="ti-bar-${i}" style="width:${item.progress}%"></div></div>
  _apps\javis-local-interface\frontend\app.js:1727:      </div>
  _apps\javis-local-interface\frontend\app.js:1728:      <div class="ti-status ${item.status}" 
id="ti-st-${i}">${item.status==='done'?'✅ Absorvido':item.status==='error'?'Erro':'📚 Estudando'}</div>
  _apps\javis-local-interface\frontend\app.js:1729:    </div>
  _apps\javis-local-interface\frontend\app.js:1730:  `).join('');
  _apps\javis-local-interface\frontend\app.js:1731:}
  _apps\javis-local-interface\frontend\app.js:1732:
  _apps\javis-local-interface\frontend\app.js:1733:/* ─── INTEGRATIONS ───────────────────────────────────────── */
  _apps\javis-local-interface\frontend\app.js:1734:const INTEGRATIONS_DATA = [
  _apps\javis-local-interface\frontend\app.js:1735:  { id:'gmail',     name:'Gmail',         icon:'📧', 
status:'disconnected', desc:'Envio e recebimento de emails' },
  _apps\javis-local-interface\frontend\app.js:1736:  { id:'whatsapp',  name:'WhatsApp',      icon:'💬', 
status:'disconnected', desc:'Mensagens via WhatsApp Business' },
  _apps\javis-local-interface\frontend\app.js:1737:  { id:'slack',     name:'Slack',         icon:'💼', 
status:'disconnected', desc:'Notificações e alertas de equipe' },
  _apps\javis-local-interface\frontend\app.js:1738:  { id:'notion',    name:'Notion',        icon:'📓', 
status:'disconnected', desc:'Documentação e conhecimento' },
  _apps\javis-local-interface\frontend\app.js:1739:  { id:'sheets',    name:'Google Sheets', icon:'📊', 
status:'disconnected', desc:'Planilhas e relatórios de dados' },
  _apps\javis-local-interface\frontend\app.js:1740:  { id:'instagram', name:'Instagram',     icon:'📸', 
status:'disconnected', desc:'Gestão de conteúdo e DMs' },
  _apps\javis-local-interface\frontend\app.js:1741:  { id:'linkedin',  name:'LinkedIn',      icon:'🔗', 
status:'disconnected', desc:'Prospecção B2B e conexões' },
  _apps\javis-local-interface\frontend\app.js:1912:
  _apps\javis-local-interface\frontend\app.js:1913:/* ═══════════════════════════════════════════════════════════
  _apps\javis-local-interface\frontend\app.js:1914:   PROJETOS — Javis as master orchestrator of all projects
  _apps\javis-local-interface\frontend\app.js:1915:   ═══════════════════════════════════════════════════════════ */
> _apps\javis-local-interface\frontend\app.js:1916:const PROJECTS_DATA = [
  _apps\javis-local-interface\frontend\app.js:1917:  {
  _apps\javis-local-interface\frontend\app.js:1918:    id:'vempassear', icon:'🌊', name:'Vem Passear Jampa', 
tag:'Turismo · João Pessoa',
  _apps\javis-local-interface\frontend\app.js:1919:    desc:'Squad de turismo: vendas de passeios, conteúdo, leads e 
atendimento no WhatsApp.',
  _apps\javis-local-interface\frontend\app.js:1920:    agents:10, status:'online', statusLabel:'Ativo', 
grad:'linear-gradient(90deg,#0ea5e9,#06b6d4)',
  _apps\javis-local-interface\frontend\app.js:1921:    open:'/vempassear', squadEndpoint:'/jampa/agents',
  _apps\javis-local-interface\frontend\app.js:1922:  },
  _apps\javis-local-interface\frontend\app.js:1923:  {
  _apps\javis-local-interface\frontend\app.js:1924:    id:'cerebro-jampa', icon:'🧠', name:'Cérebro Jampa', 
tag:'Orquestração comercial',
  _apps\javis-local-interface\frontend\app.js:1925:    desc:'Squad mestre Jampa Jarvis: Orion orquestra Hunter, Atlas, 
LNS, Nero, Nova e mais.',
  _apps\javis-local-interface\frontend\app.js:1926:    agents:10, status:'online', statusLabel:'Ativo', 
grad:'linear-gradient(90deg,#7c3aed,#a855f7)',
  _apps\javis-local-interface\frontend\app.js:1927:    open:'/vempassear', squadEndpoint:'/jampa/agents',
  _apps\javis-local-interface\frontend\app.js:1928:  },
  _apps\javis-local-interface\frontend\app.js:1929:  {
  _apps\javis-local-interface\frontend\app.js:1930:    id:'javis-core', icon:'⚡', name:'Javis Core', tag:'Orquestrador 
mestre',
  _apps\javis-local-interface\frontend\app.js:1931:    desc:'O cérebro central. Coordena todos os projetos, voz, 
código (Claude + Codex) e memória.',
  _apps\javis-local-interface\frontend\app.js:1932:    agents:12, status:'online', statusLabel:'Você está aqui', 
grad:'linear-gradient(90deg,#f59e0b,#ef4444)',
  _apps\javis-local-interface\frontend\app.js:1933:    open:'/', squadEndpoint:'/agents',
  _apps\javis-local-interface\frontend\app.js:1934:  },
  _apps\javis-local-interface\frontend\app.js:1935:  {
  _apps\javis-local-interface\frontend\app.js:1936:    id:'futuro', icon:'➕', name:'Novo Projeto', tag:'Slot livre',
  _apps\javis-local-interface\frontend\app.js:1937:    desc:'Plugue um novo projeto aqui. Javis cria o squad e 
orquestra automaticamente.',
  _apps\javis-local-interface\frontend\app.js:1938:    agents:0, status:'planned', statusLabel:'Planejado', 
grad:'linear-gradient(90deg,#475569,#334155)',
  _apps\javis-local-interface\frontend\app.js:1939:    open:null, squadEndpoint:null,
  _apps\javis-local-interface\frontend\app.js:1940:  },
  _apps\javis-local-interface\frontend\app.js:1941:];
  _apps\javis-local-interface\frontend\app.js:1942:
> _apps\javis-local-interface\frontend\app.js:1943:function renderProjects() {
  _apps\javis-local-interface\frontend\app.js:1944:  const grid = document.getElementById('projects-grid');
  _apps\javis-local-interface\frontend\app.js:1945:  if (!grid) return;
  _apps\javis-local-interface\frontend\app.js:1946:  grid.innerHTML = PROJECTS_DATA.map(p => `
  _apps\javis-local-interface\frontend\app.js:1947:    <div class="project-card" style="--accent-grad:${p.grad}">
  _apps\javis-local-interface\frontend\app.js:1948:      <div class="pc-head">
  _apps\javis-local-interface\frontend\app.js:1949:        <div class="pc-icon">${p.icon}</div>
  _apps\javis-local-interface\frontend\app.js:1950:        <div><div class="pc-name">${p.name}</div><div 
class="pc-tag">${p.tag}</div></div>
  _apps\javis-local-interface\frontend\app.js:1951:      </div>
  _apps\javis-local-interface\frontend\app.js:1952:      <div class="pc-desc">${p.desc}</div>
  _apps\javis-local-interface\frontend\app.js:1953:      <div class="pc-meta">
  _apps\javis-local-interface\frontend\app.js:1954:        <div class="pc-stat"><b>${p.agents}</b>agentes</div>
  _apps\javis-local-interface\frontend\app.js:1955:        <div class="pc-stat"><span class="pc-status 
${p.status}">${p.status==='online'?'●':'○'} ${p.statusLabel}</span></div>
  _apps\javis-local-interface\frontend\app.js:1956:      </div>
  _apps\javis-local-interface\frontend\app.js:1957:      <div class="pc-actions">
  _apps\javis-local-interface\frontend\app.js:1958:        <button class="pc-btn" 
${p.open?`onclick="window.location.href='${p.open}'"`:'disabled'}>${p.open?'Abrir':'Em breve'}</button>
  _apps\javis-local-interface\frontend\app.js:1959:        <button class="pc-btn" 
${p.squadEndpoint?`onclick="orquestrarProjeto('${p.id}')"`:'disabled'}>Orquestrar</button>
  _apps\javis-local-interface\frontend\app.js:1960:      </div>
  _apps\javis-local-interface\frontend\app.js:1961:    </div>
  _apps\javis-local-interface\frontend\app.js:1962:  `).join('');
  _apps\javis-local-interface\frontend\app.js:1963:}
  _apps\javis-local-interface\frontend\app.js:1964:
  _apps\javis-local-interface\frontend\app.js:1965:function orquestrarProjeto(id) {
  _apps\javis-local-interface\frontend\app.js:1966:  const p = PROJECTS_DATA.find(x => x.id===id);
  _apps\javis-local-interface\frontend\app.js:1967:  if (!p) return;
  _apps\javis-local-interface\frontend\app.js:1968:  switchView('chat');



exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -LiteralPath 
'_apps/javis-local-interface/frontend/app.js' -Pattern 'renderProjects\\(|switchView|renderTrainingQueue\\(' -Context 
2,8" in C:\Users\noteacer\Desktop\javis
 succeeded in 562ms:

  _apps\javis-local-interface\frontend\app.js:1453:const VIEW_TITLES = { chat:'Orquestrador', agents:'Agentes', 
workflows:'Workflows', room:'Sala dos Agentes', projects:'Projetos', train:'SDR Academy', integrations:'Integrações' };
  _apps\javis-local-interface\frontend\app.js:1454:
> _apps\javis-local-interface\frontend\app.js:1455:function switchView(viewId) {
  _apps\javis-local-interface\frontend\app.js:1456:  document.querySelectorAll('.view').forEach(v => 
v.classList.remove('active'));
  _apps\javis-local-interface\frontend\app.js:1457:  document.querySelectorAll('.sb-item[data-view]').forEach(b => 
b.classList.remove('active'));
  _apps\javis-local-interface\frontend\app.js:1458:  const view = document.getElementById('view-' + viewId);
  _apps\javis-local-interface\frontend\app.js:1459:  if (view) view.classList.add('active');
  _apps\javis-local-interface\frontend\app.js:1460:  const btn = 
document.querySelector(`.sb-item[data-view="${viewId}"]`);
  _apps\javis-local-interface\frontend\app.js:1461:  if (btn) btn.classList.add('active');
  _apps\javis-local-interface\frontend\app.js:1462:  const titleEl = document.getElementById('topbar-view-title');
  _apps\javis-local-interface\frontend\app.js:1463:  if (titleEl) titleEl.textContent = VIEW_TITLES[viewId] || viewId;
  _apps\javis-local-interface\frontend\app.js:1466:  if (viewId === 'integrations' && 
!document.getElementById('integrations-grid').children.length) renderIntegrations();
  _apps\javis-local-interface\frontend\app.js:1467:  if (viewId === 'room') initAgentRoom(); else stopAgentRoom();
> _apps\javis-local-interface\frontend\app.js:1468:  if (viewId === 'projects' && 
!document.getElementById('projects-grid').children.length) renderProjects();
  _apps\javis-local-interface\frontend\app.js:1469:}
  _apps\javis-local-interface\frontend\app.js:1470:
  _apps\javis-local-interface\frontend\app.js:1471:document.querySelectorAll('.sb-item[data-view]').forEach(btn => {
> _apps\javis-local-interface\frontend\app.js:1472:  btn.addEventListener('click', () => switchView(btn.dataset.view));
  _apps\javis-local-interface\frontend\app.js:1473:});
  _apps\javis-local-interface\frontend\app.js:1474:
  _apps\javis-local-interface\frontend\app.js:1475:/* ─── AGENT DATA ─────────────────────────────────────────── */
  _apps\javis-local-interface\frontend\app.js:1476:const GALLERY_AGENTS = [
  _apps\javis-local-interface\frontend\app.js:1477:  { id:'orion',   name:'Orion',   role:'@master',     emoji:'🧬', 
squad:'ops',      status:'online',  desc:'Orquestra todos os agentes e decisões estratégicas do sistema.', 
skills:['Orquestração','Decisão','Priorização'], ints:['slack','gmail','notion'] },
  _apps\javis-local-interface\frontend\app.js:1478:  { id:'titan',   name:'Titan',   role:'@cro',        emoji:'👑', 
squad:'ops',      status:'online',  desc:'Orquestra todo o time de vendas, garantindo alinhamento e resultados.', 
skills:['Liderança','Vendas','KPIs'], ints:['slack','gmail','notion'] },
  _apps\javis-local-interface\frontend\app.js:1479:  { id:'khan',    name:'Khan',    role:'@bdr-global', emoji:'📞', 
squad:'vendas',   status:'online',  desc:'Gerencia outbound global, prospecção e qualificação de leads.', 
skills:['Prospecção','Cold Call','Qualificação'], ints:['whatsapp','gmail','sheets'] },
  _apps\javis-local-interface\frontend\app.js:1480:  { id:'phantom', name:'Phantom', role:'@closer',     emoji:'🎯', 
squad:'vendas',   status:'online',  desc:'Conduz negociações e fecha contratos de alto valor.', 
skills:['Fechamento','Negociação','Follow-up'], ints:['whatsapp','gmail'] },
  _apps\javis-local-interface\frontend\app.js:1518:  const a = GALLERY_AGENTS.find(x => x.id === agentId);
  _apps\javis-local-interface\frontend\app.js:1519:  if (!a) return;
> _apps\javis-local-interface\frontend\app.js:1520:  switchView('chat');
  _apps\javis-local-interface\frontend\app.js:1521:  const inp = document.getElementById('input');
  _apps\javis-local-interface\frontend\app.js:1522:  if (inp) { inp.value = `Falar com ${a.name} (${a.role}): `; 
inp.focus(); }
  _apps\javis-local-interface\frontend\app.js:1523:}
  _apps\javis-local-interface\frontend\app.js:1524:
  _apps\javis-local-interface\frontend\app.js:1525:document.addEventListener('click', e => {
  _apps\javis-local-interface\frontend\app.js:1526:  const btn = e.target.closest('.filter-btn');
  _apps\javis-local-interface\frontend\app.js:1527:  if (!btn) return;
  _apps\javis-local-interface\frontend\app.js:1528:  document.querySelectorAll('.filter-btn').forEach(b => 
b.classList.remove('active'));
  _apps\javis-local-interface\frontend\app.js:1675:  _trainingQueue.push({ url, title: 'Extraindo transcrição...', 
agent, progress: 5, status: 'studying' });
  _apps\javis-local-interface\frontend\app.js:1676:  if (urlInp) urlInp.value = '';
> _apps\javis-local-interface\frontend\app.js:1677:  renderTrainingQueue();
  _apps\javis-local-interface\frontend\app.js:1678:
  _apps\javis-local-interface\frontend\app.js:1679:  const fakeId = setInterval(() => {
  _apps\javis-local-interface\frontend\app.js:1680:    const item = _trainingQueue[idx];
  _apps\javis-local-interface\frontend\app.js:1681:    if (!item || item.progress >= 85) { clearInterval(fakeId); 
return; }
  _apps\javis-local-interface\frontend\app.js:1682:    item.progress = Math.min(85, item.progress + Math.random() * 5 
+ 2);
  _apps\javis-local-interface\frontend\app.js:1683:    const bar = document.getElementById('ti-bar-' + idx);
  _apps\javis-local-interface\frontend\app.js:1684:    if (bar) bar.style.width = item.progress + '%';
  _apps\javis-local-interface\frontend\app.js:1685:  }, 400);
  _apps\javis-local-interface\frontend\app.js:1705:      showToast(`✅ ${item.agent} absorveu: ${item.title}`, 
'success');
  _apps\javis-local-interface\frontend\app.js:1706:    }
> _apps\javis-local-interface\frontend\app.js:1707:    renderTrainingQueue();
  _apps\javis-local-interface\frontend\app.js:1708:  } catch (err) {
  _apps\javis-local-interface\frontend\app.js:1709:    clearInterval(fakeId);
  _apps\javis-local-interface\frontend\app.js:1710:    const item = _trainingQueue[idx];
  _apps\javis-local-interface\frontend\app.js:1711:    if (item) { item.title = '❌ Erro de conexão'; item.status = 
'error'; item.progress = 0; }
> _apps\javis-local-interface\frontend\app.js:1712:    renderTrainingQueue();
  _apps\javis-local-interface\frontend\app.js:1713:  }
  _apps\javis-local-interface\frontend\app.js:1714:});
  _apps\javis-local-interface\frontend\app.js:1715:
> _apps\javis-local-interface\frontend\app.js:1716:function renderTrainingQueue() {
  _apps\javis-local-interface\frontend\app.js:1717:  const el = document.getElementById('train-queue');
  _apps\javis-local-interface\frontend\app.js:1718:  if (!el) return;
  _apps\javis-local-interface\frontend\app.js:1719:  if (!_trainingQueue.length) { el.innerHTML='<div 
class="train-empty">Nenhum vídeo em treinamento ainda.</div>'; return; }
  _apps\javis-local-interface\frontend\app.js:1720:  el.innerHTML = _trainingQueue.map((item,i) => `
  _apps\javis-local-interface\frontend\app.js:1721:    <div class="train-item">
  _apps\javis-local-interface\frontend\app.js:1722:      <div class="ti-icon">🎓</div>
  _apps\javis-local-interface\frontend\app.js:1723:      <div class="ti-info">
  _apps\javis-local-interface\frontend\app.js:1724:        <div class="ti-title">${item.title}${item.chars ? ' <small 
style="color:var(--text-dim);font-weight:400">· '+Math.round(item.chars/1000)+'k chars</small>' : ''}</div>
  _apps\javis-local-interface\frontend\app.js:1762:  if (e.key === '/' && document.activeElement !== inputEl && 
!e.ctrlKey && !e.metaKey) {
  _apps\javis-local-interface\frontend\app.js:1763:    e.preventDefault();
> _apps\javis-local-interface\frontend\app.js:1764:    switchView('chat');
  _apps\javis-local-interface\frontend\app.js:1765:    inputEl && inputEl.focus();
  _apps\javis-local-interface\frontend\app.js:1766:  }
  _apps\javis-local-interface\frontend\app.js:1767:  if (e.key === 'Escape' && document.activeElement === inputEl) {
  _apps\javis-local-interface\frontend\app.js:1768:    inputEl.blur();
  _apps\javis-local-interface\frontend\app.js:1769:  }
  _apps\javis-local-interface\frontend\app.js:1770:});
  _apps\javis-local-interface\frontend\app.js:1771:
  _apps\javis-local-interface\frontend\app.js:1772:function renderIntegrations() {
  _apps\javis-local-interface\frontend\app.js:1941:];
  _apps\javis-local-interface\frontend\app.js:1942:
> _apps\javis-local-interface\frontend\app.js:1943:function renderProjects() {
  _apps\javis-local-interface\frontend\app.js:1944:  const grid = document.getElementById('projects-grid');
  _apps\javis-local-interface\frontend\app.js:1945:  if (!grid) return;
  _apps\javis-local-interface\frontend\app.js:1946:  grid.innerHTML = PROJECTS_DATA.map(p => `
  _apps\javis-local-interface\frontend\app.js:1947:    <div class="project-card" style="--accent-grad:${p.grad}">
  _apps\javis-local-interface\frontend\app.js:1948:      <div class="pc-head">
  _apps\javis-local-interface\frontend\app.js:1949:        <div class="pc-icon">${p.icon}</div>
  _apps\javis-local-interface\frontend\app.js:1950:        <div><div class="pc-name">${p.name}</div><div 
class="pc-tag">${p.tag}</div></div>
  _apps\javis-local-interface\frontend\app.js:1951:      </div>
  _apps\javis-local-interface\frontend\app.js:1966:  const p = PROJECTS_DATA.find(x => x.id===id);
  _apps\javis-local-interface\frontend\app.js:1967:  if (!p) return;
> _apps\javis-local-interface\frontend\app.js:1968:  switchView('chat');
  _apps\javis-local-interface\frontend\app.js:1969:  const inp = document.getElementById('input');
  _apps\javis-local-interface\frontend\app.js:1970:  if (inp) { inp.value = `Orquestrar projeto ${p.name}: `; 
inp.focus(); }
  _apps\javis-local-interface\frontend\app.js:1971:}



codex
Vou editar três pontos cirúrgicos: inserir o `train-stats` no layout de treino, ampliar os renders em `app.js`, e 
anexar o CSS no fim do arquivo.
apply patch
patch: completed
C:\Users\noteacer\Desktop\javis\_apps\javis-local-interface\frontend\app.js
C:\Users\noteacer\Desktop\javis\_apps\javis-local-interface\frontend\index.html
C:\Users\noteacer\Desktop\javis\_apps\javis-local-interface\frontend\style.css
diff --git a/_apps/javis-local-interface/frontend/app.js b/_apps/javis-local-interface/frontend/app.js
index 32c818ef6199616780cf8b5a63a408f376621321..e71d0c4ce0df2e6a55dceadd243076f985bbc3a4
--- a/_apps/javis-local-interface/frontend/app.js
+++ b/_apps/javis-local-interface/frontend/app.js
@@ -1716,7 +1716,11 @@
 function renderTrainingQueue() {
   const el = document.getElementById('train-queue');
   if (!el) return;
-  if (!_trainingQueue.length) { el.innerHTML='<div class="train-empty">Nenhum vídeo em treinamento ainda.</div>'; 
return; }
+  if (!_trainingQueue.length) {
+    el.innerHTML='<div class="train-empty">Nenhum vídeo em treinamento ainda.</div>';
+    renderTrainStats();
+    return;
+  }
   el.innerHTML = _trainingQueue.map((item,i) => `
     <div class="train-item">
       <div class="ti-icon">🎓</div>
@@ -1728,8 +1732,24 @@
       <div class="ti-status ${item.status}" id="ti-st-${i}">${item.status==='done'?'✅ 
Absorvido':item.status==='error'?'Erro':'📚 Estudando'}</div>
     </div>
   `).join('');
+  renderTrainStats();
 }
 
+function renderTrainStats() {
+  const doneItems = _trainingQueue.filter(item => item.status === 'done');
+  const totalVideos = doneItems.length;
+  const totalChars = doneItems.reduce((sum, item) => sum + (item.chars || 0), 0);
+  const trainedAgents = new Set(doneItems.map(item => item.agent).filter(Boolean)).size;
+
+  const totalEl = document.getElementById('ts-total');
+  const charsEl = document.getElementById('ts-chars');
+  const agentsEl = document.getElementById('ts-agents');
+
+  if (totalEl) totalEl.textContent = totalVideos;
+  if (charsEl) charsEl.textContent = Math.round(totalChars / 1000) + 'k';
+  if (agentsEl) agentsEl.textContent = trainedAgents;
+}
+
 /* ─── INTEGRATIONS ───────────────────────────────────────── */
 const INTEGRATIONS_DATA = [
   { id:'gmail',     name:'Gmail',         icon:'📧', status:'disconnected', desc:'Envio e recebimento de emails' },
@@ -1943,6 +1963,37 @@
 function renderProjects() {
   const grid = document.getElementById('projects-grid');
   if (!grid) return;
+  const totalTasks = PROJECTS_DATA.reduce((sum, p) => sum + (typeof p.tasks === 'number' ? p.tasks : 3), 0);
+  const analyticsHtml = `
+    <div class="analytics-bar" id="analytics-bar">
+      <div class="ab-card">
+        <div class="ab-val" id="ab-total-tasks">${totalTasks}</div>
+        <div class="ab-lbl">tarefas ativas</div>
+      </div>
+      <div class="ab-card">
+        <div class="ab-val online" id="ab-agents-online">5</div>
+        <div class="ab-lbl">agentes online</div>
+      </div>
+      <div class="ab-card">
+        <div class="ab-val" id="ab-completions">14</div>
+        <div class="ab-lbl">completadas hoje</div>
+      </div>
+      <div class="ab-card">
+        <div class="ab-val" id="ab-efficiency">87%</div>
+        <div class="ab-lbl">eficiência</div>
+      </div>
+      <div class="ab-chart-wrap">
+        <canvas id="ab-chart" width="200" height="60"></canvas>
+        <div class="ab-chart-lbl">Atividade 7 dias</div>
+      </div>
+    </div>
+  `;
+  const existingAnalytics = document.getElementById('analytics-bar');
+  if (existingAnalytics) {
+    existingAnalytics.outerHTML = analyticsHtml;
+  } else {
+    grid.insertAdjacentHTML('beforebegin', analyticsHtml);
+  }
   grid.innerHTML = PROJECTS_DATA.map(p => `
     <div class="project-card" style="--accent-grad:${p.grad}">
       <div class="pc-head">
@@ -1960,6 +2011,35 @@
       </div>
     </div>
   `).join('');
+  _drawAnalyticsChart();
+}
+
+function _drawAnalyticsChart() {
+  const canvas = document.getElementById('ab-chart');
+  if (!canvas) return;
+  const ctx = canvas.getContext('2d');
+  if (!ctx) return;
+
+  const points = Array.from({ length: 7 }, () => Math.floor(Math.random() * 10) + 3);
+  const min = Math.min(...points);
+  const max = Math.max(...points);
+  const pad = 5;
+  const range = max - min || 1;
+  const step = (canvas.width - pad * 2) / (points.length - 1);
+
+  ctx.clearRect(0, 0, canvas.width, canvas.height);
+  ctx.beginPath();
+  points.forEach((value, i) => {
+    const x = pad + i * step;
+    const y = canvas.height - pad - ((value - min) / range) * (canvas.height - pad * 2);
+    if (i === 0) ctx.moveTo(x, y);
+    else ctx.lineTo(x, y);
+  });
+  ctx.strokeStyle = '#7c3aed';
+  ctx.lineWidth = 1.5;
+  ctx.lineCap = 'round';
+  ctx.lineJoin = 'round';
+  ctx.stroke();
 }
 
 function orquestrarProjeto(id) {
diff --git a/_apps/javis-local-interface/frontend/index.html b/_apps/javis-local-interface/frontend/index.html
index d544edec1dae5117c196ed8e4634b1021b1cd62e..04ad24d780a3c341e9be017d7bf43b116321f096
--- a/_apps/javis-local-interface/frontend/index.html
+++ b/_apps/javis-local-interface/frontend/index.html
@@ -327,6 +327,11 @@
             <input type="text" id="train-url" placeholder="Cole o link do YouTube aqui..." />
             <button id="train-add-btn">Treinar</button>
           </div>
+          <div class="train-stats" id="train-stats">
+            <div class="ts-card"><span class="ts-val" id="ts-total">0</span><span class="ts-lbl">vídeos 
treinados</span></div>
+            <div class="ts-card"><span class="ts-val" id="ts-chars">0k</span><span class="ts-lbl">chars 
absorvidos</span></div>
+            <div class="ts-card"><span class="ts-val" id="ts-agents">0</span><span class="ts-lbl">agentes 
treinados</span></div>
+          </div>
           <div class="train-queue" id="train-queue">
             <div class="train-empty">Nenhum vídeo em treinamento ainda.</div>
           </div>
diff --git a/_apps/javis-local-interface/frontend/style.css b/_apps/javis-local-interface/frontend/style.css
index 2985f3d6c40cb1bb6584db10ccb7fdf0fc12a9ce..0730e22668dbf1f7444a3ead9c5ec21bc272670a
--- a/_apps/javis-local-interface/frontend/style.css
+++ b/_apps/javis-local-interface/frontend/style.css
@@ -1391,7 +1391,83 @@
   transition: opacity 0.25s, transform 0.25s;
   pointer-events: auto;
 }
-.toast.show { opacity: 1; transform: translateX(0); }
-.toast.success { border-left-color: var(--emerald); }
-.toast.warning { border-left-color: var(--amber); }
-.toast.error   { border-left-color: var(--red); }
+.toast.show { opacity: 1; transform: translateX(0); }
+.toast.success { border-left-color: var(--emerald); }
+.toast.warning { border-left-color: var(--amber); }
+.toast.error   { border-left-color: var(--red); }
+
+/* ????????? ANALYTICS BAR ????????? */
+.analytics-bar {
+  display: flex;
+  gap: 12px;
+  align-items: center;
+  background: rgba(124,58,237,0.06);
+  border: 1px solid rgba(124,58,237,0.2);
+  border-radius: var(--radius);
+  padding: 14px 20px;
+  margin-bottom: 20px;
+}
+.ab-card {
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  min-width: 80px;
+}
+.ab-val {
+  font-family: var(--font-mono);
+  font-size: 22px;
+  font-weight: 700;
+  color: var(--violet);
+  line-height: 1;
+}
+.ab-val.online { color: var(--emerald); }
+.ab-lbl {
+  font-size: 10px;
+  color: var(--text-dim);
+  text-transform: uppercase;
+  letter-spacing: 0.05em;
+  margin-top: 4px;
+  white-space: nowrap;
+}
+.ab-chart-wrap {
+  flex: 1;
+  display: flex;
+  flex-direction: column;
+  align-items: flex-end;
+  gap: 4px;
+}
+.ab-chart-lbl {
+  font-size: 10px;
+  color: var(--text-dim);
+  text-transform: uppercase;
+  letter-spacing: 0.04em;
+}
+
+/* ????????? TRAIN STATS ????????? */
+.train-stats {
+  display: flex;
+  gap: 16px;
+  margin-bottom: 16px;
+}
+.ts-card {
+  background: var(--surface2);
+  border: 1px solid var(--border);
+  border-radius: var(--radius);
+  padding: 10px 18px;
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  gap: 4px;
+}
+.ts-val {
+  font-family: var(--font-mono);
+  font-size: 20px;
+  font-weight: 700;
+  color: var(--cyan);
+}
+.ts-lbl {
+  font-size: 10px;
+  color: var(--text-dim);
+  text-transform: uppercase;
+  letter-spacing: 0.05em;
+}

diff --git a/_apps/javis-local-interface/frontend/app.js b/_apps/javis-local-interface/frontend/app.js
index 32c818ef6199616780cf8b5a63a408f376621321..e71d0c4ce0df2e6a55dceadd243076f985bbc3a4
--- a/_apps/javis-local-interface/frontend/app.js
+++ b/_apps/javis-local-interface/frontend/app.js
@@ -1716,7 +1716,11 @@
 function renderTrainingQueue() {
   const el = document.getElementById('train-queue');
   if (!el) return;
-  if (!_trainingQueue.length) { el.innerHTML='<div class="train-empty">Nenhum vídeo em treinamento ainda.</div>'; 
return; }
+  if (!_trainingQueue.length) {
+    el.innerHTML='<div class="train-empty">Nenhum vídeo em treinamento ainda.</div>';
+    renderTrainStats();
+    return;
+  }
   el.innerHTML = _trainingQueue.map((item,i) => `
     <div class="train-item">
       <div class="ti-icon">🎓</div>
@@ -1728,8 +1732,24 @@
       <div class="ti-status ${item.status}" id="ti-st-${i}">${item.status==='done'?'✅ 
Absorvido':item.status==='error'?'Erro':'📚 Estudando'}</div>
     </div>
   `).join('');
+  renderTrainStats();
 }
 
+function renderTrainStats() {
+  const doneItems = _trainingQueue.filter(item => item.status === 'done');
+  const totalVideos = doneItems.length;
+  const totalChars = doneItems.reduce((sum, item) => sum + (item.chars || 0), 0);
+  const trainedAgents = new Set(doneItems.map(item => item.agent).filter(Boolean)).size;
+
+  const totalEl = document.getElementById('ts-total');
+  const charsEl = document.getElementById('ts-chars');
+  const agentsEl = document.getElementById('ts-agents');
+
+  if (totalEl) totalEl.textContent = totalVideos;
+  if (charsEl) charsEl.textContent = Math.round(totalChars / 1000) + 'k';
+  if (agentsEl) agentsEl.textContent = trainedAgents;
+}
+
 /* ─── INTEGRATIONS ───────────────────────────────────────── */
 const INTEGRATIONS_DATA = [
   { id:'gmail',     name:'Gmail',         icon:'📧', status:'disconnected', desc:'Envio e recebimento de emails' },
@@ -1943,6 +1963,37 @@
 function renderProjects() {
   const grid = document.getElementById('projects-grid');
   if (!grid) return;
+  const totalTasks = PROJECTS_DATA.reduce((sum, p) => sum + (typeof p.tasks === 'number' ? p.tasks : 3), 0);
+  const analyticsHtml = `
+    <div class="analytics-bar" id="analytics-bar">
+      <div class="ab-card">
+        <div class="ab-val" id="ab-total-tasks">${totalTasks}</div>
+        <div class="ab-lbl">tarefas ativas</div>
+      </div>
+      <div class="ab-card">
+        <div class="ab-val online" id="ab-agents-online">5</div>
+        <div class="ab-lbl">agentes online</div>
+      </div>
+      <div class="ab-card">
+        <div class="ab-val" id="ab-completions">14</div>
+        <div class="ab-lbl">completadas hoje</div>
+      </div>
+      <div class="ab-card">
+        <div class="ab-val" id="ab-efficiency">87%</div>
+        <div class="ab-lbl">eficiência</div>
+      </div>
+      <div class="ab-chart-wrap">
+        <canvas id="ab-chart" width="200" height="60"></canvas>
+        <div class="ab-chart-lbl">Atividade 7 dias</div>
+      </div>
+    </div>
+  `;
+  const existingAnalytics = document.getElementById('analytics-bar');
+  if (existingAnalytics) {
+    existingAnalytics.outerHTML = analyticsHtml;
+  } else {
+    grid.insertAdjacentHTML('beforebegin', analyticsHtml);
+  }
   grid.innerHTML = PROJECTS_DATA.map(p => `
     <div class="project-card" style="--accent-grad:${p.grad}">
       <div class="pc-head">
@@ -1960,6 +2011,35 @@
       </div>
     </div>
   `).join('');
+  _drawAnalyticsChart();
+}
+
+function _drawAnalyticsChart() {
+  const canvas = document.getElementById('ab-chart');
+  if (!canvas) return;
+  const ctx = canvas.getContext('2d');
+  if (!ctx) return;
+
+  const points = Array.from({ length: 7 }, () => Math.floor(Math.random() * 10) + 3);
+  const min = Math.min(...points);
+  const max = Math.max(...points);
+  const pad = 5;
+  const range = max - min || 1;
+  const step = (canvas.width - pad * 2) / (points.length - 1);
+
+  ctx.clearRect(0, 0, canvas.width, canvas.height);
+  ctx.beginPath();
+  points.forEach((value, i) => {
+    const x = pad + i * step;
+    const y = canvas.height - pad - ((value - min) / range) * (canvas.height - pad * 2);
+    if (i === 0) ctx.moveTo(x, y);
+    else ctx.lineTo(x, y);
+  });
+  ctx.strokeStyle = '#7c3aed';
+  ctx.lineWidth = 1.5;
+  ctx.lineCap = 'round';
+  ctx.lineJoin = 'round';
+  ctx.stroke();
 }
 
 function orquestrarProjeto(id) {
diff --git a/_apps/javis-local-interface/frontend/index.html b/_apps/javis-local-interface/frontend/index.html
index d544edec1dae5117c196ed8e4634b1021b1cd62e..04ad24d780a3c341e9be017d7bf43b116321f096
--- a/_apps/javis-local-interface/frontend/index.html
+++ b/_apps/javis-local-interface/frontend/index.html
@@ -327,6 +327,11 @@
             <input type="text" id="train-url" placeholder="Cole o link do YouTube aqui..." />
             <button id="train-add-btn">Treinar</button>
           </div>
+          <div class="train-stats" id="train-stats">
+            <div class="ts-card"><span class="ts-val" id="ts-total">0</span><span class="ts-lbl">vídeos 
treinados</span></div>
+            <div class="ts-card"><span class="ts-val" id="ts-chars">0k</span><span class="ts-lbl">chars 
absorvidos</span></div>
+            <div class="ts-card"><span class="ts-val" id="ts-agents">0</span><span class="ts-lbl">agentes 
treinados</span></div>
+          </div>
           <div class="train-queue" id="train-queue">
             <div class="train-empty">Nenhum vídeo em treinamento ainda.</div>
           </div>
diff --git a/_apps/javis-local-interface/frontend/style.css b/_apps/javis-local-interface/frontend/style.css
index 2985f3d6c40cb1bb6584db10ccb7fdf0fc12a9ce..0730e22668dbf1f7444a3ead9c5ec21bc272670a
--- a/_apps/javis-local-interface/frontend/style.css
+++ b/_apps/javis-local-interface/frontend/style.css
@@ -1391,7 +1391,83 @@
   transition: opacity 0.25s, transform 0.25s;
   pointer-events: auto;
 }
-.toast.show { opacity: 1; transform: translateX(0); }
-.toast.success { border-left-color: var(--emerald); }
-.toast.warning { border-left-color: var(--amber); }
-.toast.error   { border-left-color: var(--red); }
+.toast.show { opacity: 1; transform: translateX(0); }
+.toast.success { border-left-color: var(--emerald); }
+.toast.warning { border-left-color: var(--amber); }
+.toast.error   { border-left-color: var(--red); }
+
+/* ????????? ANALYTICS BAR ????????? */
+.analytics-bar {
+  display: flex;
+  gap: 12px;
+  align-items: center;
+  background: rgba(124,58,237,0.06);
+  border: 1px solid rgba(124,58,237,0.2);
+  border-radius: var(--radius);
+  padding: 14px 20px;
+  margin-bottom: 20px;
+}
+.ab-card {
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  min-width: 80px;
+}
+.ab-val {
+  font-family: var(--font-mono);
+  font-size: 22px;
+  font-weight: 700;
+  color: var(--violet);
+  line-height: 1;
+}
+.ab-val.online { color: var(--emerald); }
+.ab-lbl {
+  font-size: 10px;
+  color: var(--text-dim);
+  text-transform: uppercase;
+  letter-spacing: 0.05em;
+  margin-top: 4px;
+  white-space: nowrap;
+}
+.ab-chart-wrap {
+  flex: 1;
+  display: flex;
+  flex-direction: column;
+  align-items: flex-end;
+  gap: 4px;
+}
+.ab-chart-lbl {
+  font-size: 10px;
+  color: var(--text-dim);
+  text-transform: uppercase;
+  letter-spacing: 0.04em;
+}
+
+/* ????????? TRAIN STATS ????????? */
+.train-stats {
+  display: flex;
+  gap: 16px;
+  margin-bottom: 16px;
+}
+.ts-card {
+  background: var(--surface2);
+  border: 1px solid var(--border);
+  border-radius: var(--radius);
+  padding: 10px 18px;
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  gap: 4px;
+}
+.ts-val {
+  font-family: var(--font-mono);
+  font-size: 20px;
+  font-weight: 700;
+  color: var(--cyan);
+}
+.ts-lbl {
+  font-size: 10px;
+  color: var(--text-dim);
+  text-transform: uppercase;
+  letter-spacing: 0.05em;
+}

codex
Edição aplicada. Agora vou fazer uma verificação estática: diff dos trechos alterados e checagem de sintaxe do 
`app.js` sem executar o app.
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'git diff -- 
_apps/javis-local-interface/frontend/index.html _apps/javis-local-interface/frontend/app.js 
_apps/javis-local-interface/frontend/style.css' in C:\Users\noteacer\Desktop\javis
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "node --check 
'_apps/javis-local-interface/frontend/app.js'" in C:\Users\noteacer\Desktop\javis
 succeeded in 537ms:
diff --git a/_apps/javis-local-interface/frontend/app.js b/_apps/javis-local-interface/frontend/app.js
index 32c818e..e71d0c4 100644
--- a/_apps/javis-local-interface/frontend/app.js
+++ b/_apps/javis-local-interface/frontend/app.js
@@ -1716,7 +1716,11 @@ document.addEventListener('click', async e => {
 function renderTrainingQueue() {
   const el = document.getElementById('train-queue');
   if (!el) return;
-  if (!_trainingQueue.length) { el.innerHTML='<div class="train-empty">Nenhum vídeo em treinamento ainda.</div>'; 
return; }
+  if (!_trainingQueue.length) {
+    el.innerHTML='<div class="train-empty">Nenhum vídeo em treinamento ainda.</div>';
+    renderTrainStats();
+    return;
+  }
   el.innerHTML = _trainingQueue.map((item,i) => `
     <div class="train-item">
       <div class="ti-icon">🎓</div>
@@ -1728,6 +1732,22 @@ function renderTrainingQueue() {
       <div class="ti-status ${item.status}" id="ti-st-${i}">${item.status==='done'?'✅ 
Absorvido':item.status==='error'?'Erro':'📚 Estudando'}</div>
     </div>
   `).join('');
+  renderTrainStats();
+}
+
+function renderTrainStats() {
+  const doneItems = _trainingQueue.filter(item => item.status === 'done');
+  const totalVideos = doneItems.length;
+  const totalChars = doneItems.reduce((sum, item) => sum + (item.chars || 0), 0);
+  const trainedAgents = new Set(doneItems.map(item => item.agent).filter(Boolean)).size;
+
+  const totalEl = document.getElementById('ts-total');
+  const charsEl = document.getElementById('ts-chars');
+  const agentsEl = document.getElementById('ts-agents');
+
+  if (totalEl) totalEl.textContent = totalVideos;
+  if (charsEl) charsEl.textContent = Math.round(totalChars / 1000) + 'k';
+  if (agentsEl) agentsEl.textContent = trainedAgents;
 }
 
 /* ─── INTEGRATIONS ───────────────────────────────────────── */
@@ -1943,6 +1963,37 @@ const PROJECTS_DATA = [
 function renderProjects() {
   const grid = document.getElementById('projects-grid');
   if (!grid) return;
+  const totalTasks = PROJECTS_DATA.reduce((sum, p) => sum + (typeof p.tasks === 'number' ? p.tasks : 3), 0);
+  const analyticsHtml = `
+    <div class="analytics-bar" id="analytics-bar">
+      <div class="ab-card">
+        <div class="ab-val" id="ab-total-tasks">${totalTasks}</div>
+        <div class="ab-lbl">tarefas ativas</div>
+      </div>
+      <div class="ab-card">
+        <div class="ab-val online" id="ab-agents-online">5</div>
+        <div class="ab-lbl">agentes online</div>
+      </div>
+      <div class="ab-card">
+        <div class="ab-val" id="ab-completions">14</div>
+        <div class="ab-lbl">completadas hoje</div>
+      </div>
+      <div class="ab-card">
+        <div class="ab-val" id="ab-efficiency">87%</div>
+        <div class="ab-lbl">eficiência</div>
+      </div>
+      <div class="ab-chart-wrap">
+        <canvas id="ab-chart" width="200" height="60"></canvas>
+        <div class="ab-chart-lbl">Atividade 7 dias</div>
+      </div>
+    </div>
+  `;
+  const existingAnalytics = document.getElementById('analytics-bar');
+  if (existingAnalytics) {
+    existingAnalytics.outerHTML = analyticsHtml;
+  } else {
+    grid.insertAdjacentHTML('beforebegin', analyticsHtml);
+  }
   grid.innerHTML = PROJECTS_DATA.map(p => `
     <div class="project-card" style="--accent-grad:${p.grad}">
       <div class="pc-head">
@@ -1960,6 +2011,35 @@ function renderProjects() {
       </div>
     </div>
   `).join('');
+  _drawAnalyticsChart();
+}
+
+function _drawAnalyticsChart() {
+  const canvas = document.getElementById('ab-chart');
+  if (!canvas) return;
+  const ctx = canvas.getContext('2d');
+  if (!ctx) return;
+
+  const points = Array.from({ length: 7 }, () => Math.floor(Math.random() * 10) + 3);
+  const min = Math.min(...points);
+  const max = Math.max(...points);
+  const pad = 5;
+  const range = max - min || 1;
+  const step = (canvas.width - pad * 2) / (points.length - 1);
+
+  ctx.clearRect(0, 0, canvas.width, canvas.height);
+  ctx.beginPath();
+  points.forEach((value, i) => {
+    const x = pad + i * step;
+    const y = canvas.height - pad - ((value - min) / range) * (canvas.height - pad * 2);
+    if (i === 0) ctx.moveTo(x, y);
+    else ctx.lineTo(x, y);
+  });
+  ctx.strokeStyle = '#7c3aed';
+  ctx.lineWidth = 1.5;
+  ctx.lineCap = 'round';
+  ctx.lineJoin = 'round';
+  ctx.stroke();
 }
 
 function orquestrarProjeto(id) {
diff --git a/_apps/javis-local-interface/frontend/index.html b/_apps/javis-local-interface/frontend/index.html
index d544ede..04ad24d 100644
--- a/_apps/javis-local-interface/frontend/index.html
+++ b/_apps/javis-local-interface/frontend/index.html
@@ -327,6 +327,11 @@
             <input type="text" id="train-url" placeholder="Cole o link do YouTube aqui..." />
             <button id="train-add-btn">Treinar</button>
           </div>
+          <div class="train-stats" id="train-stats">
+            <div class="ts-card"><span class="ts-val" id="ts-total">0</span><span class="ts-lbl">vídeos 
treinados</span></div>
+            <div class="ts-card"><span class="ts-val" id="ts-chars">0k</span><span class="ts-lbl">chars 
absorvidos</span></div>
+            <div class="ts-card"><span class="ts-val" id="ts-agents">0</span><span class="ts-lbl">agentes 
treinados</span></div>
+          </div>
           <div class="train-queue" id="train-queue">
             <div class="train-empty">Nenhum vídeo em treinamento ainda.</div>
           </div>
diff --git a/_apps/javis-local-interface/frontend/style.css b/_apps/javis-local-interface/frontend/style.css
index a88984a..e3e6fe7 100644
--- a/_apps/javis-local-interface/frontend/style.css
+++ b/_apps/javis-local-interface/frontend/style.css
@@ -1395,3 +1395,79 @@ body::before {
 .toast.success { border-left-color: var(--emerald); }
 .toast.warning { border-left-color: var(--amber); }
 .toast.error   { border-left-color: var(--red); }
+
+/* ????????? ANALYTICS BAR ????????? */
+.analytics-bar {
+  display: flex;
+  gap: 12px;
+  align-items: center;
+  background: rgba(124,58,237,0.06);
+  border: 1px solid rgba(124,58,237,0.2);
+  border-radius: var(--radius);
+  padding: 14px 20px;
+  margin-bottom: 20px;
+}
+.ab-card {
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  min-width: 80px;
+}
+.ab-val {
+  font-family: var(--font-mono);
+  font-size: 22px;
+  font-weight: 700;
+  color: var(--violet);
+  line-height: 1;
+}
+.ab-val.online { color: var(--emerald); }
+.ab-lbl {
+  font-size: 10px;
+  color: var(--text-dim);
+  text-transform: uppercase;
+  letter-spacing: 0.05em;
+  margin-top: 4px;
+  white-space: nowrap;
+}
+.ab-chart-wrap {
+  flex: 1;
+  display: flex;
+  flex-direction: column;
+  align-items: flex-end;
+  gap: 4px;
+}
+.ab-chart-lbl {
+  font-size: 10px;
+  color: var(--text-dim);
+  text-transform: uppercase;
+  letter-spacing: 0.04em;
+}
+
+/* ????????? TRAIN STATS ????????? */
+.train-stats {
+  display: flex;
+  gap: 16px;
+  margin-bottom: 16px;
+}
+.ts-card {
+  background: var(--surface2);
+  border: 1px solid var(--border);
+  border-radius: var(--radius);
+  padding: 10px 18px;
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  gap: 4px;
+}
+.ts-val {
+  font-family: var(--font-mono);
+  font-size: 20px;
+  font-weight: 700;
+  color: var(--cyan);
+}
+.ts-lbl {
+  font-size: 10px;
+  color: var(--text-dim);
+  text-transform: uppercase;
+  letter-spacing: 0.05em;
+}
warning: in the working copy of '_apps/javis-local-interface/frontend/app.js', LF will be replaced by CRLF the next 
time Git touches it
warning: in the working copy of '_apps/javis-local-interface/frontend/index.html', LF will be replaced by CRLF the 
next time Git touches it
warning: in the working copy of '_apps/javis-local-interface/frontend/style.css', LF will be replaced by CRLF the next 
time Git touches it

 succeeded in 1901ms:
diff --git a/_apps/javis-local-interface/frontend/app.js b/_apps/javis-local-interface/frontend/app.js
index 32c818ef6199616780cf8b5a63a408f376621321..e71d0c4ce0df2e6a55dceadd243076f985bbc3a4
--- a/_apps/javis-local-interface/frontend/app.js
+++ b/_apps/javis-local-interface/frontend/app.js
@@ -1716,7 +1716,11 @@
 function renderTrainingQueue() {
   const el = document.getElementById('train-queue');
   if (!el) return;
-  if (!_trainingQueue.length) { el.innerHTML='<div class="train-empty">Nenhum vídeo em treinamento ainda.</div>'; 
return; }
+  if (!_trainingQueue.length) {
+    el.innerHTML='<div class="train-empty">Nenhum vídeo em treinamento ainda.</div>';
+    renderTrainStats();
+    return;
+  }
   el.innerHTML = _trainingQueue.map((item,i) => `
     <div class="train-item">
       <div class="ti-icon">🎓</div>
@@ -1728,8 +1732,24 @@
       <div class="ti-status ${item.status}" id="ti-st-${i}">${item.status==='done'?'✅ 
Absorvido':item.status==='error'?'Erro':'📚 Estudando'}</div>
     </div>
   `).join('');
+  renderTrainStats();
 }
 
+function renderTrainStats() {
+  const doneItems = _trainingQueue.filter(item => item.status === 'done');
+  const totalVideos = doneItems.length;
+  const totalChars = doneItems.reduce((sum, item) => sum + (item.chars || 0), 0);
+  const trainedAgents = new Set(doneItems.map(item => item.agent).filter(Boolean)).size;
+
+  const totalEl = document.getElementById('ts-total');
+  const charsEl = document.getElementById('ts-chars');
+  const agentsEl = document.getElementById('ts-agents');
+
+  if (totalEl) totalEl.textContent = totalVideos;
+  if (charsEl) charsEl.textContent = Math.round(totalChars / 1000) + 'k';
+  if (agentsEl) agentsEl.textContent = trainedAgents;
+}
+
 /* ─── INTEGRATIONS ───────────────────────────────────────── */
 const INTEGRATIONS_DATA = [
   { id:'gmail',     name:'Gmail',         icon:'📧', status:'disconnected', desc:'Envio e recebimento de emails' },
@@ -1943,6 +1963,37 @@
 function renderProjects() {
   const grid = document.getElementById('projects-grid');
   if (!grid) return;
+  const totalTasks = PROJECTS_DATA.reduce((sum, p) => sum + (typeof p.tasks === 'number' ? p.tasks : 3), 0);
+  const analyticsHtml = `
+    <div class="analytics-bar" id="analytics-bar">
+      <div class="ab-card">
+        <div class="ab-val" id="ab-total-tasks">${totalTasks}</div>
+        <div class="ab-lbl">tarefas ativas</div>
+      </div>
+      <div class="ab-card">
+        <div class="ab-val online" id="ab-agents-online">5</div>
+        <div class="ab-lbl">agentes online</div>
+      </div>
+      <div class="ab-card">
+        <div class="ab-val" id="ab-completions">14</div>
+        <div class="ab-lbl">completadas hoje</div>
+      </div>
+      <div class="ab-card">
+        <div class="ab-val" id="ab-efficiency">87%</div>
+        <div class="ab-lbl">eficiência</div>
+      </div>
+      <div class="ab-chart-wrap">
+        <canvas id="ab-chart" width="200" height="60"></canvas>
+        <div class="ab-chart-lbl">Atividade 7 dias</div>
+      </div>
+    </div>
+  `;
+  const existingAnalytics = document.getElementById('analytics-bar');
+  if (existingAnalytics) {
+    existingAnalytics.outerHTML = analyticsHtml;
+  } else {
+    grid.insertAdjacentHTML('beforebegin', analyticsHtml);
+  }
   grid.innerHTML = PROJECTS_DATA.map(p => `
     <div class="project-card" style="--accent-grad:${p.grad}">
       <div class="pc-head">
@@ -1960,6 +2011,35 @@
       </div>
     </div>
   `).join('');
+  _drawAnalyticsChart();
+}
+
+function _drawAnalyticsChart() {
+  const canvas = document.getElementById('ab-chart');
+  if (!canvas) return;
+  const ctx = canvas.getContext('2d');
+  if (!ctx) return;
+
+  const points = Array.from({ length: 7 }, () => Math.floor(Math.random() * 10) + 3);
+  const min = Math.min(...points);
+  const max = Math.max(...points);
+  const pad = 5;
+  const range = max - min || 1;
+  const step = (canvas.width - pad * 2) / (points.length - 1);
+
+  ctx.clearRect(0, 0, canvas.width, canvas.height);
+  ctx.beginPath();
+  points.forEach((value, i) => {
+    const x = pad + i * step;
+    const y = canvas.height - pad - ((value - min) / range) * (canvas.height - pad * 2);
+    if (i === 0) ctx.moveTo(x, y);
+    else ctx.lineTo(x, y);
+  });
+  ctx.strokeStyle = '#7c3aed';
+  ctx.lineWidth = 1.5;
+  ctx.lineCap = 'round';
+  ctx.lineJoin = 'round';
+  ctx.stroke();
 }
 
 function orquestrarProjeto(id) {
diff --git a/_apps/javis-local-interface/frontend/index.html b/_apps/javis-local-interface/frontend/index.html
index d544edec1dae5117c196ed8e4634b1021b1cd62e..04ad24d780a3c341e9be017d7bf43b116321f096
--- a/_apps/javis-local-interface/frontend/index.html
+++ b/_apps/javis-local-interface/frontend/index.html
@@ -327,6 +327,11 @@
             <input type="text" id="train-url" placeholder="Cole o link do YouTube aqui..." />
             <button id="train-add-btn">Treinar</button>
           </div>
+          <div class="train-stats" id="train-stats">
+            <div class="ts-card"><span class="ts-val" id="ts-total">0</span><span class="ts-lbl">vídeos 
treinados</span></div>
+            <div class="ts-card"><span class="ts-val" id="ts-chars">0k</span><span class="ts-lbl">chars 
absorvidos</span></div>
+            <div class="ts-card"><span class="ts-val" id="ts-agents">0</span><span class="ts-lbl">agentes 
treinados</span></div>
+          </div>
           <div class="train-queue" id="train-queue">
             <div class="train-empty">Nenhum vídeo em treinamento ainda.</div>
           </div>
diff --git a/_apps/javis-local-interface/frontend/style.css b/_apps/javis-local-interface/frontend/style.css
index 2985f3d6c40cb1bb6584db10ccb7fdf0fc12a9ce..0730e22668dbf1f7444a3ead9c5ec21bc272670a
--- a/_apps/javis-local-interface/frontend/style.css
+++ b/_apps/javis-local-interface/frontend/style.css
@@ -1391,7 +1391,83 @@
   transition: opacity 0.25s, transform 0.25s;
   pointer-events: auto;
 }
-.toast.show { opacity: 1; transform: translateX(0); }
-.toast.success { border-left-color: var(--emerald); }
-.toast.warning { border-left-color: var(--amber); }
-.toast.error   { border-left-color: var(--red); }
+.toast.show { opacity: 1; transform: translateX(0); }
+.toast.success { border-left-color: var(--emerald); }
+.toast.warning { border-left-color: var(--amber); }
+.toast.error   { border-left-color: var(--red); }
+
+/* ????????? ANALYTICS BAR ????????? */
+.analytics-bar {
+  display: flex;
+  gap: 12px;
+  align-items: center;
+  background: rgba(124,58,237,0.06);
+  border: 1px solid rgba(124,58,237,0.2);
+  border-radius: var(--radius);
+  padding: 14px 20px;
+  margin-bottom: 20px;
+}
+.ab-card {
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  min-width: 80px;
+}
+.ab-val {
+  font-family: var(--font-mono);
+  font-size: 22px;
+  font-weight: 700;
+  color: var(--violet);
+  line-height: 1;
+}
+.ab-val.online { color: var(--emerald); }
+.ab-lbl {
+  font-size: 10px;
+  color: var(--text-dim);
+  text-transform: uppercase;
+  letter-spacing: 0.05em;
+  margin-top: 4px;
+  white-space: nowrap;
+}
+.ab-chart-wrap {
+  flex: 1;
+  display: flex;
+  flex-direction: column;
+  align-items: flex-end;
+  gap: 4px;
+}
+.ab-chart-lbl {
+  font-size: 10px;
+  color: var(--text-dim);
+  text-transform: uppercase;
+  letter-spacing: 0.04em;
+}
+
+/* ????????? TRAIN STATS ????????? */
+.train-stats {
+  display: flex;
+  gap: 16px;
+  margin-bottom: 16px;
+}
+.ts-card {
+  background: var(--surface2);
+  border: 1px solid var(--border);
+  border-radius: var(--radius);
+  padding: 10px 18px;
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  gap: 4px;
+}
+.ts-val {
+  font-family: var(--font-mono);
+  font-size: 20px;
+  font-weight: 700;
+  color: var(--cyan);
+}
+.ts-lbl {
+  font-size: 10px;
+  color: var(--text-dim);
+  text-transform: uppercase;
+  letter-spacing: 0.05em;
+}

codex
- [index.html](</c/Users/noteacer/Desktop/javis/_apps/javis-local-interface/frontend/index.html:327>): adicionado 
`#train-stats` antes de `#train-queue`.
- [app.js](</c/Users/noteacer/Desktop/javis/_apps/javis-local-interface/frontend/app.js:1716>): `renderProjects()` 
agora injeta analytics + sparkline; treino ganhou `renderTrainStats()`.
- [style.css](</c/Users/noteacer/Desktop/javis/_apps/javis-local-interface/frontend/style.css:1398>): CSS de 
`.analytics-bar` e `.train-stats` anexado. `node --check` passou.
diff --git a/_apps/javis-local-interface/frontend/app.js b/_apps/javis-local-interface/frontend/app.js
index 32c818ef6199616780cf8b5a63a408f376621321..e71d0c4ce0df2e6a55dceadd243076f985bbc3a4
--- a/_apps/javis-local-interface/frontend/app.js
+++ b/_apps/javis-local-interface/frontend/app.js
@@ -1716,7 +1716,11 @@
 function renderTrainingQueue() {
   const el = document.getElementById('train-queue');
   if (!el) return;
-  if (!_trainingQueue.length) { el.innerHTML='<div class="train-empty">Nenhum vídeo em treinamento ainda.</div>'; 
return; }
+  if (!_trainingQueue.length) {
+    el.innerHTML='<div class="train-empty">Nenhum vídeo em treinamento ainda.</div>';
+    renderTrainStats();
+    return;
+  }
   el.innerHTML = _trainingQueue.map((item,i) => `
     <div class="train-item">
       <div class="ti-icon">🎓</div>
@@ -1728,8 +1732,24 @@
       <div class="ti-status ${item.status}" id="ti-st-${i}">${item.status==='done'?'✅ 
Absorvido':item.status==='error'?'Erro':'📚 Estudando'}</div>
     </div>
   `).join('');
+  renderTrainStats();
 }
 
+function renderTrainStats() {
+  const doneItems = _trainingQueue.filter(item => item.status === 'done');
+  const totalVideos = doneItems.length;
+  const totalChars = doneItems.reduce((sum, item) => sum + (item.chars || 0), 0);
+  const trainedAgents = new Set(doneItems.map(item => item.agent).filter(Boolean)).size;
+
+  const totalEl = document.getElementById('ts-total');
+  const charsEl = document.getElementById('ts-chars');
+  const agentsEl = document.getElementById('ts-agents');
+
+  if (totalEl) totalEl.textContent = totalVideos;
+  if (charsEl) charsEl.textContent = Math.round(totalChars / 1000) + 'k';
+  if (agentsEl) agentsEl.textContent = trainedAgents;
+}
+
 /* ─── INTEGRATIONS ───────────────────────────────────────── */
 const INTEGRATIONS_DATA = [
   { id:'gmail',     name:'Gmail',         icon:'📧', status:'disconnected', desc:'Envio e recebimento de emails' },
@@ -1943,6 +1963,37 @@
 function renderProjects() {
   const grid = document.getElementById('projects-grid');
   if (!grid) return;
+  const totalTasks = PROJECTS_DATA.reduce((sum, p) => sum + (typeof p.tasks === 'number' ? p.tasks : 3), 0);
+  const analyticsHtml = `
+    <div class="analytics-bar" id="analytics-bar">
+      <div class="ab-card">
+        <div class="ab-val" id="ab-total-tasks">${totalTasks}</div>
+        <div class="ab-lbl">tarefas ativas</div>
+      </div>
+      <div class="ab-card">
+        <div class="ab-val online" id="ab-agents-online">5</div>
+        <div class="ab-lbl">agentes online</div>
+      </div>
+      <div class="ab-card">
+        <div class="ab-val" id="ab-completions">14</div>
+        <div class="ab-lbl">completadas hoje</div>
+      </div>
+      <div class="ab-card">
+        <div class="ab-val" id="ab-efficiency">87%</div>
+        <div class="ab-lbl">eficiência</div>
+      </div>
+      <div class="ab-chart-wrap">
+        <canvas id="ab-chart" width="200" height="60"></canvas>
+        <div class="ab-chart-lbl">Atividade 7 dias</div>
+      </div>
+    </div>
+  `;
+  const existingAnalytics = document.getElementById('analytics-bar');
+  if (existingAnalytics) {
+    existingAnalytics.outerHTML = analyticsHtml;
+  } else {
+    grid.insertAdjacentHTML('beforebegin', analyticsHtml);
+  }
   grid.innerHTML = PROJECTS_DATA.map(p => `
     <div class="project-card" style="--accent-grad:${p.grad}">
       <div class="pc-head">
@@ -1960,6 +2011,35 @@
       </div>
     </div>
   `).join('');
+  _drawAnalyticsChart();
+}
+
+function _drawAnalyticsChart() {
+  const canvas = document.getElementById('ab-chart');
+  if (!canvas) return;
+  const ctx = canvas.getContext('2d');
+  if (!ctx) return;
+
+  const points = Array.from({ length: 7 }, () => Math.floor(Math.random() * 10) + 3);
+  const min = Math.min(...points);
+  const max = Math.max(...points);
+  const pad = 5;
+  const range = max - min || 1;
+  const step = (canvas.width - pad * 2) / (points.length - 1);
+
+  ctx.clearRect(0, 0, canvas.width, canvas.height);
+  ctx.beginPath();
+  points.forEach((value, i) => {
+    const x = pad + i * step;
+    const y = canvas.height - pad - ((value - min) / range) * (canvas.height - pad * 2);
+    if (i === 0) ctx.moveTo(x, y);
+    else ctx.lineTo(x, y);
+  });
+  ctx.strokeStyle = '#7c3aed';
+  ctx.lineWidth = 1.5;
+  ctx.lineCap = 'round';
+  ctx.lineJoin = 'round';
+  ctx.stroke();
 }
 
 function orquestrarProjeto(id) {
diff --git a/_apps/javis-local-interface/frontend/index.html b/_apps/javis-local-interface/frontend/index.html
index d544edec1dae5117c196ed8e4634b1021b1cd62e..04ad24d780a3c341e9be017d7bf43b116321f096
--- a/_apps/javis-local-interface/frontend/index.html
+++ b/_apps/javis-local-interface/frontend/index.html
@@ -327,6 +327,11 @@
             <input type="text" id="train-url" placeholder="Cole o link do YouTube aqui..." />
             <button id="train-add-btn">Treinar</button>
           </div>
+          <div class="train-stats" id="train-stats">
+            <div class="ts-card"><span class="ts-val" id="ts-total">0</span><span class="ts-lbl">vídeos 
treinados</span></div>
+            <div class="ts-card"><span class="ts-val" id="ts-chars">0k</span><span class="ts-lbl">chars 
absorvidos</span></div>
+            <div class="ts-card"><span class="ts-val" id="ts-agents">0</span><span class="ts-lbl">agentes 
treinados</span></div>
+          </div>
           <div class="train-queue" id="train-queue">
             <div class="train-empty">Nenhum vídeo em treinamento ainda.</div>
           </div>
diff --git a/_apps/javis-local-interface/frontend/style.css b/_apps/javis-local-interface/frontend/style.css
index 2985f3d6c40cb1bb6584db10ccb7fdf0fc12a9ce..0730e22668dbf1f7444a3ead9c5ec21bc272670a
--- a/_apps/javis-local-interface/frontend/style.css
+++ b/_apps/javis-local-interface/frontend/style.css
@@ -1391,7 +1391,83 @@
   transition: opacity 0.25s, transform 0.25s;
   pointer-events: auto;
 }
-.toast.show { opacity: 1; transform: translateX(0); }
-.toast.success { border-left-color: var(--emerald); }
-.toast.warning { border-left-color: var(--amber); }
-.toast.error   { border-left-color: var(--red); }
+.toast.show { opacity: 1; transform: translateX(0); }
+.toast.success { border-left-color: var(--emerald); }
+.toast.warning { border-left-color: var(--amber); }
+.toast.error   { border-left-color: var(--red); }
+
+/* ????????? ANALYTICS BAR ????????? */
+.analytics-bar {
+  display: flex;
+  gap: 12px;
+  align-items: center;
+  background: rgba(124,58,237,0.06);
+  border: 1px solid rgba(124,58,237,0.2);
+  border-radius: var(--radius);
+  padding: 14px 20px;
+  margin-bottom: 20px;
+}
+.ab-card {
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  min-width: 80px;
+}
+.ab-val {
+  font-family: var(--font-mono);
+  font-size: 22px;
+  font-weight: 700;
+  color: var(--violet);
+  line-height: 1;
+}
+.ab-val.online { color: var(--emerald); }
+.ab-lbl {
+  font-size: 10px;
+  color: var(--text-dim);
+  text-transform: uppercase;
+  letter-spacing: 0.05em;
+  margin-top: 4px;
+  white-space: nowrap;
+}
+.ab-chart-wrap {
+  flex: 1;
+  display: flex;
+  flex-direction: column;
+  align-items: flex-end;
+  gap: 4px;
+}
+.ab-chart-lbl {
+  font-size: 10px;
+  color: var(--text-dim);
+  text-transform: uppercase;
+  letter-spacing: 0.04em;
+}
+
+/* ????????? TRAIN STATS ????????? */
+.train-stats {
+  display: flex;
+  gap: 16px;
+  margin-bottom: 16px;
+}
+.ts-card {
+  background: var(--surface2);
+  border: 1px solid var(--border);
+  border-radius: var(--radius);
+  padding: 10px 18px;
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  gap: 4px;
+}
+.ts-val {
+  font-family: var(--font-mono);
+  font-size: 20px;
+  font-weight: 700;
+  color: var(--cyan);
+}
+.ts-lbl {
+  font-size: 10px;
+  color: var(--text-dim);
+  text-transform: uppercase;
+  letter-spacing: 0.05em;
+}

- [index.html](</c/Users/noteacer/Desktop/javis/_apps/javis-local-interface/frontend/index.html:327>): adicionado `#train-stats` antes de `#train-queue`.
- [app.js](</c/Users/noteacer/Desktop/javis/_apps/javis-local-interface/frontend/app.js:1716>): `renderProjects()` agora injeta analytics + sparkline; treino ganhou `renderTrainStats()`.
tokens used
75.855
- [style.css](</c/Users/noteacer/Desktop/javis/_apps/javis-local-interface/frontend/style.css:1398>): CSS de `.analytics-bar` e `.train-stats` anexado. `node --check` passou.
